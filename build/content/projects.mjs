import { code, table, flow, diagram, callout } from "../render.mjs";

export const PROJECTS = [];
const P = (o) => PROJECTS.push({ kind: "project", group: "projects", ...o });

// helper for the common "how to explain in interview" question
const explainQ = (name, line) => ({
  q: `How would you explain the ${name} project in an interview?`,
  difficulty: "Intermediate", round: "behavioral", seniority: "Senior", frequency: "high", region: "Both",
  model: `Lead with the problem and the metric: ${line} Then the architecture in one breath, then the one hard tradeoff you made and how you evaluated it.`,
  senior: "I structure it as: problem → users → architecture → the hardest decision and why → how I measured success → what I'd do next. I keep it to 2 minutes and let the interviewer pull threads.",
  followups: ["What was the hardest part?", "What would you change with more time?", "How did you know it worked?"],
  redflag: "Listing technologies with no problem, metric, or tradeoff.",
});

// 0 ───────── INDORAMA DIGITIZATION (real-world project — custom page, no Q&A yet) ─────────
PROJECTS.push({ kind: "custom", group: "myprojects", num: "★", title: "Indorama Digitization Project",
  path: "projects/indorama-digitization.html",
  kw: "indorama digitization project industrial ai oil gas fertilizer plant equipment monitoring backend ml apis fastapi elasticsearch time-series kubernetes docker mcube anomaly health score trip score instrumentation kpi production ml engineer real world experience portfolio",
  data: { indorama: true, eyebrow: "Hands-on Projects · Real-world experience",
    title: "Indorama – Digitization Project",
    lead: "Backend ML APIs · Equipment Monitoring · Kubernetes Deployment — your strongest production-AI credential, and how to use it to win GenAI interviews." } });

// 0b ───────── INDORAMA INTERVIEW QUESTIONS (custom page) ─────────
const ul = (items) => "<ul>" + items.map((i) => `<li>${i}</li>`).join("") + "</ul>";
PROJECTS.push({ kind: "custom", group: "myprojects", num: "★Q", title: "Indorama Interview Questions",
  path: "projects/indorama-interview-questions.html",
  kw: "indorama project interview questions backend ml api time-series elasticsearch anomaly health score trip instrumentation kpi fastapi kubernetes docker mcube dashboard deployment debugging genai transition rag agents llmops production ml",
  data: { indoramaQ: true, eyebrow: "Hands-on Projects · Project deep-dive prep",
    title: "Indorama Project Interview Questions",
    lead: "Production ML APIs, industrial equipment monitoring, Kubernetes deployment, and GenAI transition positioning — 10 project-specific questions with model + senior answers.",
    questions: [
      { q: "Walk me through the Indorama Digitization project end to end.", difficulty: "Intermediate", round: "project deep-dive", seniority: "Senior",
        why: "Openers test whether you can compress a real project into a clear, structured narrative and whether you actually owned the work.",
        model: ["Lead with the business problem, then the pipeline:", ul(["<strong>Problem</strong> — digitize plant operations so operators could monitor equipment health, anomalies and KPIs in real time.", "<strong>Pipeline</strong> — fetch time-series data from Elasticsearch/DB → clean/pivot/validate → feature engineering & KPI calculation → ML inference / anomaly logic → produce health/trip/instrumentation scores → expose via FastAPI REST APIs → integrate with TCG Mcube dashboards.", "<strong>Ops</strong> — packaged and deployed with Docker/Kubernetes; health checks, env config, debugging."])],
        senior: "I frame it as end-to-end production ML and name the one hard part (e.g. reliable time-series ingestion/validation across many loops) and how I handled scale (hundreds of loop/equipment APIs). I keep it to ~90 seconds and let them pull threads.",
        followups: ["What was the hardest part?", "What did you personally own vs the team?", "How many APIs/equipment types?"],
        redflag: "Listing technologies with no problem statement, no flow, and no clear personal contribution.",
        mention: ["Multiple plant loops & equipment", "Elasticsearch/DB time-series source", "Health/trip/instrumentation/anomaly scores + KPIs", "FastAPI + Mcube dashboard integration"],
        genai: "Same end-to-end shape as a RAG service: ingest → transform → model → serve via API → integrate with a UI → monitor. You can map each stage directly when asked to design a GenAI system." },

      { q: "How did you design the backend ML API architecture?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Tests whether you can design maintainable backend services around models, not just call a model in a script.",
        model: ["<ul><li>A data-access layer (Elasticsearch/DB fetch) separated from transform and inference layers.</li><li>Reusable transform/feature code shared across loops to avoid duplicating logic per endpoint.</li><li>Per-loop/equipment endpoints returning structured JSON for the dashboard.</li><li>Config-driven so new loops/equipment could be added without rewriting the stack.</li></ul>"],
        senior: "I'd stress separation of concerns and reuse: hundreds of endpoints only stay maintainable if fetch/transform/score logic is shared and parameterized, with consistent JSON contracts and error handling. I'd mention timeouts, input validation and observability as first-class.",
        followups: ["How did you avoid duplicating logic across hundreds of APIs?", "How did you version the APIs?", "How did you handle a slow/failing data source?"],
        redflag: "One giant script per endpoint with copy-pasted logic and no shared layers.",
        mention: ["Hundreds of loop/equipment REST APIs", "Shared fetch/transform/score layers", "Structured JSON contracts"],
        genai: "The same layered design is exactly how you build a production RAG/agent API: a retrieval layer, an orchestration layer, and a thin API layer with consistent contracts — not model calls scattered through the code." },

      { q: "How did you handle time-series data from Elasticsearch / databases?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Time-series + Elasticsearch is messy; this checks real data-engineering experience, not toy datasets.",
        model: ["<ul><li>Queried windows of plant sensor data per tag/loop from Elasticsearch/DB.</li><li>Handled gaps, irregular sampling, and out-of-range/garbage sensor values.</li><li>Pivoted long sensor streams into a wide, model-ready table aligned on timestamps.</li><li>Validated ranges/units before inference so bad data didn't silently corrupt scores.</li></ul>"],
        senior: "I'd call out the failure modes that matter at plant scale: missing tags, clock/sampling misalignment, and sensor drift — and how validation gates protect downstream scores. I'd mention query efficiency (windowing, only pulling needed tags) for latency.",
        followups: ["How did you handle missing or irregular timestamps?", "How did you keep Elasticsearch queries fast?", "What did you do with out-of-range sensor values?"],
        redflag: "Assuming clean, evenly-sampled data and ignoring validation.",
        mention: ["Elasticsearch/DB time-series fetch", "Gap handling, pivoting, range validation", "Per-tag windowed queries"],
        genai: "This is the same discipline as document ingestion for RAG: fetch from a store, clean, validate quality, and shape into a model-ready form. 'Bad data in → bad scores out' maps to 'bad chunks in → hallucinations out'." },

      { q: "Walk me through your data cleaning, pivoting, validation, and feature engineering.", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Feature engineering is where most production ML quality is won or lost; interviewers probe the details.",
        model: ["<ul><li><strong>Cleaning</strong> — drop/flag nulls, clip impossible values, de-duplicate, align units.</li><li><strong>Pivoting</strong> — reshape long (tag, time, value) streams into a wide table per timestamp.</li><li><strong>Validation</strong> — range/consistency checks; reject or flag windows that fail.</li><li><strong>Features</strong> — rolling stats, rates of change, deviations from baseline/expected, lag features for KPIs.</li></ul>"],
        senior: "I'd emphasize that validation is a gate, not an afterthought, and that features encoded domain meaning (deviation from expected operating range drives anomaly/health). I'd mention making transforms reusable and testable across loops.",
        followups: ["Which features mattered most for anomaly detection?", "How did you test the transforms?", "How did you encode domain knowledge?"],
        redflag: "Vague 'I cleaned the data' with no specific steps or validation.",
        mention: ["Pivot long→wide", "Range/consistency validation gate", "Rolling/deviation features"],
        genai: "Maps to chunking + metadata enrichment + retrieval preparation in RAG — both are 'shape raw source into the form the model needs, with quality gates'." },

      { q: "How did the model inference and anomaly scoring work?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Checks you understand serving models in production and the logic behind anomaly detection.",
        model: ["<ul><li>Loaded model(s) once per service; ran inference on the prepared feature window.</li><li>Anomaly logic compared observed behavior against expected/normal operating ranges (model output and/or rule thresholds).</li><li>Produced a score per equipment/loop and returned it as structured JSON for the dashboard.</li></ul>"],
        senior: "I'd be precise about anomaly = deviation from expected, and discuss thresholds and false-positive cost (operators ignore noisy alerts), plus how I kept inference latency acceptable for near-real-time dashboards.",
        followups: ["How did you set anomaly thresholds?", "How did you handle false positives?", "Model-based vs rule-based — what did you use where?"],
        redflag: "Treating anomaly detection as a black box with no notion of thresholds or false-positive cost.",
        mention: ["Per-equipment anomaly score", "Expected-range deviation logic", "Latency-aware inference"],
        genai: "Anomaly/quality scoring maps to LLM evaluation: a faithfulness/quality score is conceptually the same as a health/anomaly score — a number that flags when output deviates from 'good', with the same false-positive tradeoffs." },

      { q: "Explain the health score, trip score, instrumentation score, and KPI calculation logic.", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Tests depth on the domain-specific outputs you built — generic answers reveal you didn't own them.",
        model: ["<ul><li><strong>Health score</strong> — aggregate condition of equipment from multiple signals/sub-scores.</li><li><strong>Trip score</strong> — likelihood/severity related to a trip (unplanned shutdown) condition.</li><li><strong>Instrumentation score</strong> — reliability/validity of the sensors/instruments feeding the loop.</li><li><strong>KPIs</strong> — calculated process metrics (efficiency, deviations) from the cleaned time-series.</li></ul>", "Each was computed from validated features and exposed via its own API field."],
        senior: "I'd explain how the scores compose (instrumentation quality gates trust in the health/trip scores — bad sensors shouldn't trigger false trips), and how I made the logic auditable so operators trusted it.",
        followups: ["How do these scores relate to each other?", "What if instrumentation is unreliable — does health score still apply?", "How did you validate the scores were trustworthy?"],
        redflag: "Reciting names without explaining what each measures or how they combine.",
        mention: ["Composite health from sub-scores", "Instrumentation score gates trust", "KPIs from validated features"],
        genai: "Composing sub-scores into a trustworthy overall score is exactly LLM-eval design: faithfulness + relevance + safety combine into an overall quality gate, and you must guard against one bad signal poisoning the verdict." },

      { q: "How did you design the REST APIs and JSON response structure?", difficulty: "Intermediate", round: "system design", seniority: "Senior",
        why: "Dashboard integration lives or dies on clean, consistent API contracts.",
        model: ["<ul><li>Consistent endpoint pattern per loop/equipment.</li><li>Structured JSON: identifiers, timestamp/window, the scores (health/anomaly/trip/instrumentation), KPIs, and status/error fields.</li><li>Stable schema so the dashboard could render without special-casing.</li><li>Validation and clear error responses for bad/missing data.</li></ul>"],
        senior: "I'd stress contract stability and predictability — the dashboard team depends on it, so a consistent schema and explicit error/empty states matter more than cleverness. I'd mention versioning and documenting the contract.",
        followups: ["How did you keep the schema consistent across hundreds of APIs?", "How did you signal 'data unavailable' vs 'score is low'?", "How did you version responses?"],
        redflag: "Inconsistent ad-hoc JSON per endpoint that the frontend must special-case.",
        mention: ["Consistent JSON contract", "Explicit status/error fields", "Stable schema for the dashboard"],
        genai: "Identical to designing a GenAI API: a structured response (answer, citations, confidence/quality, status) with a stable contract the frontend can rely on — and explicit handling for 'no answer / low confidence'." },

      { q: "How did you deploy and debug the services on Kubernetes?", difficulty: "Advanced", round: "deployment", seniority: "Senior",
        why: "Separates engineers who shipped to production from those who only ran code locally.",
        model: ["<ul><li>Packaged APIs as Docker images; deployed on Kubernetes.</li><li>Environment-specific config (data source endpoints, credentials) via config/secrets.</li><li>Health/readiness checks so traffic only hit ready pods.</li><li>Debugging via pod logs, checking config/connectivity to the data source, and reproducing failing requests.</li></ul>"],
        senior: "I'd give a concrete debugging story: e.g. a service failing readiness due to a data-source connectivity/config issue, traced via logs and fixed in config — and mention resource limits, restarts, and rollout safety.",
        followups: ["Describe one production bug you debugged.", "Readiness vs liveness probes — what's the difference?", "How did you manage secrets/config across environments?"],
        redflag: "Only knowing 'it ran in a container' with no health checks, logs, or a real debugging example.",
        mention: ["Docker images + K8s deploy", "Health/readiness checks", "A concrete log-based debugging story"],
        genai: "Production GenAI apps deploy the same way — containerized FastAPI services on Kubernetes with health checks, config/secrets, and log-based debugging. This experience transfers 1:1." },

      { q: "How did you integrate the APIs with the TCG Mcube dashboard?", difficulty: "Intermediate", round: "system design", seniority: "Senior",
        why: "Enterprise integration is a real differentiator — it shows you delivered something users actually used.",
        model: ["<ul><li>The Mcube application consumed the backend APIs and rendered health/anomaly/KPI views per loop/equipment.</li><li>Agreed a stable JSON contract with the dashboard/frontend side.</li><li>Did integration testing to confirm the dashboard rendered scores and trends correctly.</li><li>Coordinated on error/empty states so the UI degraded gracefully.</li></ul>"],
        senior: "I'd emphasize the contract + cross-team coordination and that real users (plant operators) depended on it, so reliability and clear empty/error states mattered. I'd mention how integration issues were caught and resolved.",
        followups: ["How did you coordinate the API contract with the dashboard team?", "What happened when an API returned no data?", "How did you test the integration?"],
        redflag: "Treating integration as 'I returned JSON and someone else's problem' with no contract or testing.",
        mention: ["Mcube consumed the APIs", "Agreed JSON contract", "Integration testing + graceful empty states"],
        genai: "This is enterprise GenAI frontend integration: a RAG/agent backend exposing a contract that an internal app/dashboard consumes, with the same need for stable contracts and graceful degradation." },

      { q: "How does this production ML project map to GenAI systems (RAG, agents, LLMOps, monitoring)?", difficulty: "Senior", round: "behavioral", seniority: "Senior/Staff",
        why: "For a GenAI role with a non-GenAI project, this is the decisive question — can you bridge it credibly without overclaiming?",
        model: ["Be explicit and honest: it wasn't a GenAI project, but the production engineering is the same:", ul(["ML APIs → LLM/RAG/agent APIs.", "Elasticsearch data fetch → document/vector retrieval.", "Feature engineering → chunking + metadata enrichment.", "Health/anomaly scores → eval/quality/hallucination scores.", "Equipment dashboard → GenAI observability dashboard.", "Kubernetes deploy + debugging → production GenAI deployment + RAG/agent debugging."])],
        senior: "I'd say production GenAI is mostly the engineering around the model — pipelines, serving, observability, deployment, integration — which is exactly what I did here; the new part I'd add is retrieval quality, prompting and LLM-specific evaluation, which I've been building hands-on.",
        followups: ["What's genuinely new for you in GenAI?", "Would you call this a GenAI project? (Say no, honestly.)", "How would you turn this into a RAG system?"],
        redflag: "Overclaiming the project used GenAI/LLMs when it didn't — instant credibility loss.",
        mention: ["The full mapping table", "Honest framing: production AI/ML, not GenAI", "What you're learning to close the gap"],
        genai: "This question IS the GenAI connection — see the mapping table below; rehearse it so the bridge sounds natural and honest." },
    ],
  } });

// 0c ───────── MY REAL PROJECT STORIES (compact hub, custom page) ─────────
PROJECTS.push({ kind: "custom", group: "myprojects", num: "★S", title: "My Real Project Stories",
  path: "projects/my-real-project-stories.html",
  kw: "my real project stories indorama yield optimizer real-time optimization differential evolution multiprocessing oil gas digital twin paypal marketing analytics clustering segmentation nvidia edge deployment flask docker transfer learning project deep dive behavioral resume screening genai bridge positioning",
  data: { realStories: true, eyebrow: "Hands-on Projects · Project deep-dive prep",
    title: "My Real Project Stories",
    lead: "Real ML, backend, analytics, optimization, and deployment projects mapped to GenAI interview positioning." } });

// 0d ───────── ATS RESUME SCREENING ASSISTANT (practical GenAI HR project, custom page) ─────────
PROJECTS.push({ kind: "custom", group: "myprojects", num: "★A", title: "ATS Resume Screening Assistant",
  path: "projects/ats-resume-screening-assistant.html",
  kw: "ats resume screening assistant rag hr candidate shortlisting job description matching skill extraction embeddings semantic match explainable score fastapi vector db pdf parsing structured extraction responsible ai guardrails human in the loop genai hr project recruiter shortlisting",
  data: { ats: true, eyebrow: "Hands-on Projects · GenAI portfolio project",
    title: "ATS Resume Screening Assistant",
    lead: "RAG-based HR candidate shortlisting project for resume screening, skill extraction, candidate ranking, and explainable match scoring.",
    questions: [
      { q: "Explain the ATS Resume Screening Assistant end to end.", difficulty: "Intermediate", round: "project deep-dive", seniority: "Senior",
        why: "Tests whether you can describe a real GenAI application as a system, not a single prompt.",
        model: ["Lead with problem + flow: HR needs faster, explainable shortlisting. The system takes a job description and many resumes, parses and structures each resume (skills, experience, education, tools), embeds resumes and JD, retrieves/semantically matches candidates, then uses an LLM to produce an explainable match score, missing-skill analysis, and a recruiter summary — with a human reviewing before any decision."],
        senior: "I frame it as a RAG-style pipeline with structured extraction and human-in-the-loop, and call out the responsible-AI constraints up front (decision-support, not decision-maker) because for HR that's a first-class requirement, not an add-on.",
        followups: ["What's the hardest part?", "Where does a human step in?", "How big are the resumes / how many?"],
        redflag: "Describing it as 'the LLM picks the best candidate' — fully automated hiring with no human or guardrails.",
        mention: ["JD + multi-resume input", "Structured extraction", "Embeddings + LLM explanation", "Human-in-the-loop review"],
        genai: "It's a clean showcase of RAG retrieval + structured output + evaluation + guardrails + FastAPI — the exact stack GenAI roles hire for." },
      { q: "How do you match resumes against job descriptions?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Probes whether your matching is principled (semantic + structured) or naive keyword overlap.",
        model: ["<ul><li>Extract structured requirements from the JD (skills, must-haves, experience).</li><li>Extract a structured profile from each resume.</li><li>Embed and semantically match resume content to JD requirements (hybrid: embeddings + keyword for exact tools/certs).</li><li>Score per requirement, then aggregate into an overall match with the LLM explaining the gaps.</li></ul>"],
        senior: "I score per-requirement with evidence rather than one opaque number, combine semantic similarity with exact-term matching (a specific cert/tool must match exactly), and keep the LLM's role to explanation/aggregation over retrieved evidence — not free-form judgment.",
        followups: ["Semantic vs exact for a required certification?", "How do you weight must-have vs nice-to-have?"],
        redflag: "Pure keyword overlap, or letting the LLM 'decide' with no structured evidence.",
        mention: ["Per-requirement scoring with evidence", "Hybrid semantic + exact matching"],
        genai: "Hybrid retrieval + reranking + grounded LLM reasoning — directly transferable to any RAG product." },
      { q: "Why use embeddings instead of only keyword matching?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Mid–Senior",
        why: "Checks you understand semantic matching and its limits.",
        model: ["Keyword matching misses paraphrase and transferable skills — 'built REST APIs' vs 'developed backend services', or 'PyTorch' experience for a 'deep learning' requirement. Embeddings capture meaning, so a strong candidate phrased differently isn't dropped.", "But you still need keyword/exact matching for hard requirements like a specific certification or tool — so the answer is hybrid."],
        senior: "I'd note embeddings can over-match loosely related text, so I keep exact matching for must-haves and use embeddings for transferable/semantic fit — and validate the mix on labeled JD-resume pairs.",
        followups: ["When does semantic matching hurt you?", "How do you handle synonyms for tools?"],
        redflag: "Claiming embeddings alone solve everything, ignoring exact-match needs.",
        mention: ["Transferable-skill matching", "Why hybrid (exact + semantic)"],
        genai: "This is the dense-vs-sparse-vs-hybrid retrieval discussion every RAG interview has." },
      { q: "How do you prevent hallucinated skills?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "HR + hallucination = real harm; tests grounding discipline.",
        model: ["<ul><li>Ground extraction strictly in the resume text — extract only what's present, with the source span.</li><li>Use structured output (schema) so the model returns fields, not prose.</li><li>Verify each extracted skill maps to evidence in the resume; drop unsupported ones.</li><li>Require citations/evidence in the match explanation.</li></ul>"],
        senior: "I add a verification pass that checks each claimed skill against the source text and flags anything unsupported, and I treat a hallucinated skill as a tracked error rate in evaluation — not an occasional annoyance.",
        followups: ["How do you measure the hallucination rate?", "What if the resume is ambiguous?"],
        redflag: "Trusting the LLM's extracted skills with no grounding or verification.",
        mention: ["Evidence-grounded extraction", "Verification pass", "Hallucinated-skill rate as a metric"],
        genai: "Grounding + faithfulness verification is the core RAG anti-hallucination pattern." },
      { q: "How do you evaluate the ranking quality?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Separates demo-builders from engineers who measure quality.",
        model: ["<ul><li>Build 20–50 JD-resume pairs and have humans label strong/medium/weak.</li><li>Compare the system's ranking to the human ranking (rank correlation / agreement).</li><li>Track extraction accuracy, hallucinated-skill rate, missing-skill correctness, and explanation quality.</li><li>Track false positives (weak candidate ranked high) and false negatives.</li></ul>"],
        senior: "I optimize for not missing strong candidates (false negatives are costly) and treat the labeled set as a regression gate. I'd also run a fairness check that scores don't correlate with protected proxies.",
        followups: ["Which error is worse here, FP or FN?", "How do you keep the labeled set fresh?"],
        redflag: "'It looked reasonable on a few resumes.'",
        mention: ["Human-labeled JD-resume pairs", "Ranking agreement + error rates", "Fairness check"],
        genai: "Golden datasets + LLM/eval metrics + human calibration — the LLM-evaluation skill set." },
      { q: "What guardrails are needed for an HR use case?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff",
        why: "HR is high-risk/regulated; tests responsible-AI maturity.",
        model: ["<ul><li>Decision-support only — never auto-reject/hire; recruiter makes the call.</li><li>Ignore/mask protected attributes (gender, age, religion, caste, race, photo, address, marital status).</li><li>Mask PII where possible.</li><li>Explain every score; store an audit trail; allow recruiter override.</li></ul>"],
        senior: "I'd design the system so protected attributes never enter the features, run disparate-impact audits, keep humans in the loop on every decision, and log decisions for auditability — and I'd refuse to ship full automation.",
        followups: ["How do you audit for bias?", "Would you ever auto-reject?"],
        redflag: "Auto-rejecting candidates, or using/ranking on protected or proxy attributes.",
        mention: ["Decision-support not decision-maker", "Protected-attribute exclusion + audit trail", "Recruiter override"],
        genai: "Responsible-AI + guardrails + human-in-the-loop — increasingly its own interview round." },
      { q: "How would you design the FastAPI backend?", difficulty: "Advanced", round: "system design", seniority: "Senior",
        why: "Tests production API design around a GenAI pipeline.",
        model: ["Endpoints: create job (POST /api/jobs), upload resumes (POST /api/resumes/upload), run screening (POST /api/screen), list ranked candidates (GET /api/jobs/{id}/candidates), candidate scorecard (GET /api/candidates/{id}/scorecard), recruiter feedback (POST /api/feedback).", "Separate layers: parsing/extraction, embedding/retrieval, scoring/explanation, and a thin API layer with structured (validated) JSON responses, async for long screening runs, and persistence in Postgres/SQLite."],
        senior: "I'd make screening async (job queued, results polled/streamed), validate all LLM output against schemas with a repair loop, store an audit trail, and keep the LLM/vector providers behind a swappable interface.",
        followups: ["How do you handle a long screening run?", "How do you make LLM output reliable?"],
        redflag: "One synchronous endpoint doing parse+embed+LLM with no validation or persistence.",
        mention: ["The 6 endpoints + their purpose", "Async screening", "Schema-validated JSON"],
        genai: "Production GenAI API design — exactly what applied GenAI roles build." },
      { q: "How would you deploy and monitor this system?", difficulty: "Advanced", round: "deployment", seniority: "Senior",
        why: "Tests whether you can run a GenAI app in production, not just build it.",
        model: ["<ul><li>Containerize with Docker; deploy the FastAPI service (and a Streamlit/React dashboard).</li><li>Trace each screening run (resumes processed, tokens, cost, latency, errors).</li><li>Monitor extraction/hallucination rate, ranking agreement, and recruiter override rate.</li><li>Add fallbacks for LLM/provider failures and validate output schemas.</li></ul>"],
        senior: "I'd reuse the same observability discipline as any service — traces, cost/latency dashboards, alerting — plus GenAI-specific quality monitoring (sampled faithfulness, override rate as a quality signal) and an audit log for compliance.",
        followups: ["What would you alert on?", "How do you handle a provider outage mid-run?"],
        redflag: "Monitoring only uptime, ignoring extraction/ranking quality and cost.",
        mention: ["Docker deploy", "Traces + quality + cost monitoring", "Audit log"],
        genai: "LLMOps/observability + deployment — a core GenAI production skill." },
    ],
  } });

// 0e ───────── DOCUMENT QnA RAG APP (scalable GenAI/RAG portfolio project, custom page) ─────────
PROJECTS.push({ kind: "custom", group: "myprojects", num: "★D", title: "Document QnA RAG App",
  path: "projects/document-qna-rag-app.html",
  kw: "document qna rag app scalable question answering thousands pdfs async ingestion batch embedding vector db qdrant faiss pgvector reranking hybrid search citations latency optimization caching streaming celery redis langfuse fastapi enterprise knowledge search document intelligence genai portfolio project",
  data: { docqna: true, eyebrow: "My Projects & Experience · GenAI portfolio project",
    title: "Document QnA RAG App",
    lead: "Scalable RAG-based question-answering system over large PDF collections with citations, low-latency retrieval, and production-style architecture.",
    questions: [
      { q: "Explain the Document QnA RAG app end to end.", difficulty: "Intermediate", round: "project deep-dive", seniority: "Senior",
        why: "Tests whether you can describe a RAG system as an end-to-end pipeline, separating offline ingestion from online query.",
        model: ["Two halves. <strong>Offline (ingestion):</strong> upload/index PDFs → parse (OCR if scanned) → clean + extract metadata → chunk → batch-embed → store in a vector DB with metadata. <strong>Online (query):</strong> embed the question → hybrid/vector search with metadata filters → rerank the top candidates → assemble a compact context → LLM generates a grounded answer with citations → stream the response and log a trace."],
        senior: "I stress the design principle: all expensive work (parse/chunk/embed/index) happens asynchronously before query time, so the query path only does embed → retrieve → rerank → generate → stream. That's what keeps it usable at large scale.",
        followups: ["Where does it get slow?", "How do you keep the index fresh?", "What's the trickiest part of ingestion?"],
        redflag: "Describing it as 'load all PDFs and ask the LLM' — processing documents at query time.",
        mention: ["Async ingestion vs query path", "Hybrid retrieval + rerank", "Citations + trace logging"],
        genai: "This is the canonical production RAG pipeline — the exact architecture applied GenAI roles build and probe." },
      { q: "How do you handle thousands of PDFs without making the app slow?", difficulty: "Advanced", round: "system design", seniority: "Senior",
        why: "Scale is the headline claim — they test whether your architecture actually supports it.",
        model: ["<ul><li>Don't process PDFs at query time — ingest and index offline/asynchronously.</li><li>Store chunks + metadata in a vector DB; use ANN search for fast retrieval.</li><li>Batch-embed during ingestion; use background workers for large jobs.</li><li>Narrow the search with document-level metadata filters; rerank only a small candidate set.</li><li>Cache frequent queries/contexts; stream answers.</li></ul>", "The strongest answer should include actual benchmark numbers once available — number of PDFs indexed, chunks generated, vector DB used, retrieval latency, and p95 answer latency. Until then, frame it as a scalable design and do not invent numbers."],
        senior: "I'd frame it honestly: the architecture is <em>designed</em> for thousands of documents (async ingestion, ANN, metadata filtering, caching) — and I'd quote tested scale/latency only if I've actually benchmarked it. Query cost stays roughly flat with corpus size because retrieval returns a small top-k regardless of total documents.",
        followups: ["What's your tested scale?", "Why doesn't query latency grow with corpus size?"],
        redflag: "Claiming benchmarked scale with no numbers, or implying all docs are scanned per query.",
        mention: ["Offline async ingestion", "ANN + metadata filter", "Top-k keeps query cost flat"],
        genai: "Scalable RAG/system-design thinking — a senior GenAI signal." },
      { q: "How do you reduce latency during question answering?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Latency is the most common production complaint about RAG; tests practical optimization.",
        model: ["Move expensive work out of the query path (parse/chunk/embed/index happen at ingestion). At query time only: embed the question, retrieve a small candidate set (ANN), optionally rerank top results, build a compact prompt, and stream the answer.", "Add Redis caching, metadata filtering, smaller/faster embedding models, prompt compression, top-k tuning, and monitor p95."],
        senior: "I optimize against a p95 SLO and measure per-stage latency from traces first (retrieval vs rerank vs LLM) so I fix the real bottleneck. Streaming improves perceived latency but not total compute — I'm explicit about that.",
        followups: ["Which stage is usually slowest?", "Does streaming reduce total time?"],
        redflag: "Guessing optimizations without measuring per-stage latency.",
        mention: ["Work out of query path", "Per-stage latency from traces", "Streaming = perceived not total"],
        genai: "LLMOps latency optimization — core to production GenAI roles." },
      { q: "What tech stack did you use and why?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior",
        why: "Tests whether your tool choices are reasoned, not cargo-culted.",
        model: ["Python + FastAPI backend; LangChain/LlamaIndex for RAG plumbing; an embedding model + a vector DB; Postgres for metadata; Redis for caching; Celery/RQ for async ingestion; Docker/Kubernetes for deploy; Langfuse/LangSmith for tracing.", "<ul><li>FAISS for local/demo vector search; Qdrant/Pinecone/pgvector for service/production-style.</li><li>Redis caches repeated queries/intermediate results.</li><li>Celery/RQ processes large ingestion asynchronously.</li><li>Langfuse/LangSmith traces prompts, retrieval, latency, quality.</li></ul>"],
        senior: "I'd justify by constraints: pgvector if already on Postgres, Qdrant for large filtered workloads, FAISS only for local demos. I keep the LLM/vector providers behind a swappable interface so nothing is locked in.",
        followups: ["FAISS vs Qdrant for production?", "Why a separate queue for ingestion?"],
        redflag: "Listing trendy tools with no reasoning or scale context.",
        mention: ["Vector DB choice by constraint", "Redis + Celery roles", "Tracing with Langfuse/LangSmith"],
        genai: "Production GenAI stack fluency." },
      { q: "How did you choose chunk size and overlap?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Chunking is the highest-leverage RAG quality decision.",
        model: ["Tune against a labeled query set, not by habit. Smaller chunks → precise matching but answers can split; larger → more context but diluted embeddings and wasted window. Start ~400–512 tokens with ~10–20% overlap; respect the embedding model's max length.", "Often best: parent-document retrieval — embed small chunks, return the larger surrounding passage."],
        senior: "I check whether answers span chunk boundaries and measure recall@k across a few sizes, and I keep page/section metadata so citations stay precise.",
        followups: ["What overlap and why?", "How does chunk size affect citations?"],
        redflag: "A fixed number with no measurement.",
        mention: ["Measured chunk sizing", "Parent-document retrieval", "Metadata for citations"],
        genai: "Directly transferable RAG-quality skill." },
      { q: "Which vector database would you use and why?", difficulty: "Intermediate", round: "system design", seniority: "Senior",
        why: "Tests pragmatic infra choice vs hype.",
        model: ["<ul><li><strong>FAISS</strong> — local/offline/demo, in-process.</li><li><strong>pgvector</strong> — already on Postgres, moderate scale, SQL filters.</li><li><strong>Qdrant/Pinecone</strong> — large scale, heavy metadata filtering, service-style.</li></ul>", "Choose by scale, filtering needs, and ops appetite."],
        senior: "I avoid adopting a heavy vector DB before scale needs it — pgvector covers a lot. For this project's 'thousands of PDFs' goal with metadata filters, Qdrant or pgvector are the realistic picks; FAISS is for the local demo.",
        followups: ["When is FAISS not enough?", "How do you do metadata filtering?"],
        redflag: "Defaulting to the trendiest DB regardless of scale.",
        mention: ["FAISS demo vs Qdrant/pgvector production", "Metadata filtering need"],
        genai: "Embeddings/vector-DB design — high-frequency RAG topic." },
      { q: "Dense search vs hybrid search — which is better for document Q&A?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Tests understanding of retrieval beyond plain embeddings.",
        model: ["Dense (embeddings) captures meaning/paraphrase; sparse (BM25) catches exact terms — IDs, codes, names, acronyms common in documents. Hybrid fuses both and is the production default for document Q&A.", "Rerank the fused candidates for precision."],
        senior: "I weight by content: documents with many exact identifiers (legal/finance/technical) lean more on keyword; conversational FAQ leans dense. I validate the mix on labeled pairs.",
        followups: ["How do you fuse the two lists?", "When does dense-only fail?"],
        redflag: "Insisting embeddings alone are always enough.",
        mention: ["Hybrid default for docs", "Fusion + rerank"],
        genai: "Dense/sparse/hybrid is a guaranteed RAG interview topic." },
      { q: "Why and when would you use reranking?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Tests precision/latency tradeoff awareness.",
        model: ["First-pass retrieval is fast but imprecise. A cross-encoder reranker reads query+chunk together and reorders the top candidates so only the best few enter the prompt — keeping context small and precise. Run it only on the top ~20–50, not the whole corpus.", "Tradeoff: it adds latency, so it's applied to a small candidate set."],
        senior: "I measure the recall@k / faithfulness lift to justify the added latency, and keep the candidate set small so reranking stays within the latency budget.",
        followups: ["Why not rerank everything?", "How much latency does it add?"],
        redflag: "Stuffing many un-reranked chunks into the prompt.",
        mention: ["Rerank small candidate set", "Smaller, precise context"],
        genai: "Reranking is a core production RAG lever." },
      { q: "How do you add citations to answers?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior",
        why: "Citations are what make document Q&A trustworthy and auditable.",
        model: ["Pass each retrieved chunk with an id + source metadata (file, page). Instruct the model to cite the id behind each claim (e.g. [1]), and map ids back to source snippets in the response. Display the source snippet alongside the answer."],
        senior: "I verify the cited chunk actually supports the claim (not just that a citation exists), and treat an uncited factual claim as a guardrail failure to flag.",
        followups: ["How do you verify the citation supports the claim?", "How do you cite a specific page?"],
        redflag: "Citations as decoration with no check that they back the claim.",
        mention: ["Id-tagged chunks", "Page-level source snippets", "Citation verification"],
        genai: "Grounded, cited generation is central to enterprise RAG." },
      { q: "How do you reduce hallucination?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Hallucination in document Q&A directly erodes trust.",
        model: ["<ul><li>Ground strictly in retrieved context; instruct 'answer only from the documents, else say you couldn't find it'.</li><li>Better retrieval (hybrid + rerank) so the right context is present.</li><li>Coverage gate — if top retrieval score is too low, abstain.</li><li>Require citations; verify faithfulness.</li></ul>"],
        senior: "I make hallucination a tracked metric (sampled faithfulness judge) with a deploy gate, not a hope, and sample production traffic to keep measuring it.",
        followups: ["What if retrieval returns nothing relevant?", "How do you measure hallucination?"],
        redflag: "Trying to fix it with prompt wording like 'be accurate' alone.",
        mention: ["Cite-or-abstain", "Coverage gate", "Faithfulness as a metric"],
        genai: "Anti-hallucination grounding is the core RAG safety pattern." },
      { q: "How do you evaluate a RAG system?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior",
        why: "Separates demo-builders from engineers who measure quality.",
        model: ["Split the metrics. Retrieval: precision@k / recall@k on a golden set of question→source. Generation: groundedness, citation accuracy, hallucination rate, correctness. Also track latency p50/p95 and failed-ingestion rate.", "Compare dense vs hybrid and with/without reranking; add a human feedback loop."],
        senior: "I run eval as a gate (no change ships without passing the golden set), grow the set from real bad answers, and review failures to update chunking/retrieval — a closed loop.",
        followups: ["How big a golden set?", "How do you trust an LLM judge?"],
        redflag: "'It looked right on a few questions.'",
        mention: ["Split retrieval vs generation metrics", "Dense-vs-hybrid + rerank comparisons", "Feedback loop"],
        genai: "LLM-evaluation discipline — a strong seniority signal." },
      { q: "How do you handle document updates, deletion, and re-indexing?", difficulty: "Advanced", round: "system design", seniority: "Senior",
        why: "Freshness/lifecycle is what separates a one-shot demo from a real system.",
        model: ["<ul><li>Track each document + its chunks with ids so you can update/delete precisely.</li><li>On update: re-ingest the changed document, re-embed its chunks, replace them; on delete: remove its chunks (tombstone) so they leave search.</li><li>Invalidate caches for affected queries; record ingestion status per document.</li></ul>"],
        senior: "I do incremental re-indexing (only the changed document, not the whole corpus) and surface 'last updated' in citations so users can judge recency. Stale index is a silent trust-killer, so I monitor ingestion lag.",
        followups: ["Full vs incremental re-index?", "How do you avoid serving deleted-doc chunks from cache?"],
        redflag: "Full re-index of everything on any change, or no deletion path.",
        mention: ["Per-document chunk ids", "Incremental re-index + tombstoning", "Cache invalidation"],
        genai: "Index lifecycle = production RAG maturity." },
    ],
  } });

// 1 ───────── BUILD FIRST RAG APP (rich) ─────────
P({
  num: "01", title: "Build First RAG App", path: "projects/build-first-rag.html",
  kw: "first rag app project build retrieval augmented generation fastapi pgvector citations eval portfolio",
  data: {
    eyebrow: "Hands-on Projects",
    title: "Project · Build Your First RAG App",
    lead: "The single most important portfolio project. A minimal-but-honest RAG service: ingest documents, retrieve with hybrid search + reranking, answer with citations and an abstain path, and evaluate it. Everything on the <a href='../pages/rag-mastery.html'>RAG Mastery</a> page, made real.",
    pills: [{ t: "Start here", cls: "red" }, { t: "Portfolio core", cls: "purple" }],
    proves: ["You can build the default GenAI architecture end to end, and — more importantly — that you think about retrieval quality, grounding and evaluation, not just 'embed and stuff into a prompt'."],
    architecture: diagram(
      "flowchart TD\n  subgraph Ingest\n    D[Docs] --> C[Chunk] --> E[Embed] --> V[(pgvector/Qdrant)]\n  end\n  subgraph API[FastAPI]\n    Q[/ask/] --> H[Hybrid retrieve] --> R[Rerank] --> P[Grounded prompt] --> L[LLM] --> A[Answer + citations]\n  end\n  V --> H\n  A --> EV[Eval: recall@k + faithfulness]",
      flow([{ t: "Ingest", s: "chunk·embed·index" }, { t: "Retrieve + rerank", accent: true }, { t: "Ground + generate + cite" }, { t: "Evaluate" }], "lr")),
    stack: { headers: ["Layer", "Default", "Alternatives"], rows: [
      ["API", "FastAPI", "Flask, Litestar"],
      ["Vector store", "pgvector", "Qdrant, FAISS (local)"],
      ["Embeddings", "bge-small / OpenAI text-embedding-3", "instructor, e5"],
      ["Reranker", "bge-reranker (cross-encoder)", "Cohere rerank"],
      ["LLM", "Claude / GPT / local via Ollama", "any chat model"],
      ["Eval", "RAGAS-style + custom judge", "promptfoo, DIY"],
    ] },
    api: ["<strong>POST /ask</strong> → {question} ⇒ {answer, citations:[{source, snippet}], used_context:bool}. <strong>POST /ingest</strong> → {doc} ⇒ {chunks_indexed}. Keep responses structured so you can evaluate and trace them."],
    dataflow: ["Ingest: document → chunks (structure-aware + overlap) → embeddings → vector store with metadata (source, page). Query: embed query → hybrid retrieve (dense + BM25) → rerank top-50 to top-5 → grounded prompt with citations + abstain rule → LLM → validate citations exist → return."],
    evalplan: ["Build a 30-case golden set (question → relevant source). Measure retrieval (recall@5, MRR) and generation (faithfulness via LLM-judge, abstain-correctness on out-of-scope questions). Run it in CI; fail on regression. Publish the numbers in the README."],
    failures: ["Pure semantic retrieval misses exact terms → add BM25/hybrid", "Chunks too big/small → tune with the golden set", "Model answers from prior knowledge → enforce context-only + abstain", "No citations → can't audit; require them", "Latency from reranking everything → only rerank top candidates"],
    demo: ["1) Ask an in-scope question → correct answer with citations. 2) Ask an out-of-scope question → it abstains. 3) Show the eval dashboard/numbers. 4) Toggle hybrid off to show recall drop. A 3-minute demo that includes the abstain case and the eval numbers beats a flashy chat UI."],
    readme: ["<strong>README sections:</strong> Problem & who it's for · Architecture diagram · Quickstart (docker compose up) · Design decisions (chunking, hybrid, rerank — with rationale) · Evaluation (golden set + numbers) · Limitations & next steps. The 'Evaluation' and 'Design decisions' sections are what hiring engineers actually read."],
    explain: ["“I built a RAG service over <docs>. The interesting part wasn't the happy path — it was getting retrieval right (hybrid + rerank lifted recall@5 from X to Y) and making it trustworthy (citations + abstain), which I proved with a 30-case eval that runs in CI.”"],
    questions: [
      explainQ("First RAG App", "“It answers questions over a document set with cited, grounded answers, and I measure recall@5 and faithfulness.”"),
      { q: "Why hybrid search and reranking instead of just embeddings?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Embeddings miss exact terms (IDs, names); BM25 catches them. Reranking reorders candidates with a cross-encoder for precision so the few chunks in context are the right ones. I measured the recall@5 gain.",
        senior: "I drove it with a labeled set rather than belief — hybrid + rerank gave a measurable recall and faithfulness lift, and it kept context small (cheaper, less 'lost in the middle'). I'd show the before/after numbers.",
        followups: ["How did you fuse dense and sparse scores?", "What reranker and why?"],
        redflag: "Adding components with no measured justification." },
      { q: "How do you know the answers are actually grounded?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Citations let me trace each claim to a source, and a faithfulness judge scores whether claims are supported by retrieved context, run over the golden set and sampled in production.",
        senior: "I gate deploys on faithfulness not dropping, and I added 'should abstain' cases so I measure both correct answers and correct refusals. Groundedness is a number I track, not a hope.",
        followups: ["How do you trust the judge?"],
        redflag: "'It looked right when I tried it.'" },
    ],
    checklist: ["Hybrid retrieval + reranker", "Citations + abstain path", "30-case golden set in CI", "README with design decisions + eval numbers", "One-command run (docker compose)"],
  },
});

// 2 ───────── BUILD FIRST AGENT APP (rich) ─────────
P({
  num: "02", title: "Build First Agent App", path: "projects/build-first-agent.html",
  kw: "first agent app tool calling react bounded loop guardrails fastapi project portfolio",
  data: {
    eyebrow: "Hands-on Projects",
    title: "Project · Build Your First Agent App",
    lead: "A reliable tool-using agent — bounded loop, schema-validated tools, output guardrails — that proves you can build agents that don't loop forever or call the wrong tool. The concepts from <a href='../pages/agentic-ai.html'>Agentic AI</a>, shipped.",
    pills: [{ t: "Agent foundation", cls: "purple" }],
    proves: ["You understand the agent loop as engineering: 'model proposes, runtime disposes', bounded steps, validated tool calls, and guardrails — not a magic autonomous black box."],
    architecture: diagram(null, flow([{ t: "Goal" }, { t: "LLM decides tool", accent: true }, { t: "Validate input + guard" }, { t: "Execute tool" }, { t: "Observe → loop (bounded)" }, { t: "Guard output" }], "lr")),
    stack: { headers: ["Layer", "Default", "Alternatives"], rows: [
      ["Orchestration", "Plain Python loop or LangGraph", "LangChain agents, custom"],
      ["Tools", "2–3 typed Python functions", "MCP servers"],
      ["LLM", "tool-calling model (Claude/GPT)", "Hermes/local via Ollama"],
      ["API", "FastAPI", "—"],
    ] },
    api: ["<strong>POST /agent</strong> → {goal} ⇒ {answer, steps:[{tool, input, output}], stopped_reason}. Returning the step trace makes the agent debuggable and demoable."],
    dataflow: ["Goal → model emits a tool call → validate args against schema → execute (with timeout) → append observation → repeat until final answer or MAX_STEPS → guard output → return with the step trace."],
    evalplan: ["Build task cases with expected tool sequences / final answers. Measure task success rate, average steps, and tool-error rate. Add adversarial cases (injection via tool output, ambiguous goals) as a permanent gate."],
    failures: ["Infinite loop → MAX_STEPS budget", "Wrong/invalid tool args → schema validation + repair", "Trusting tool output → treat as untrusted (injection)", "Over-privileged tool → least privilege", "Cost blowup → per-run token budget"],
    demo: ["1) A goal needing 2 tool calls → show the step trace. 2) A goal that should fail gracefully (no tool fits) → it stops cleanly. 3) An injection attempt in a tool result → guard catches it."],
    readme: ["Problem · Architecture (the loop) · Tools (with schemas) · Reliability (bounded loop, validation, guardrails) · Evaluation (task success rate) · A note on where you'd use a workflow instead. That last note signals maturity."],
    explain: ["“I built a tool-using agent with a bounded loop and validated tool calls. The point was reliability: I show task success rate, and I can explain exactly how it avoids the classic agent failures — looping, bad tool args, and injection via tool output.”"],
    questions: [
      explainQ("First Agent App", "“It's a tool-using agent that completes multi-step goals reliably, with a bounded loop and guardrails, measured by task success rate.”"),
      { q: "How does your agent avoid looping forever or calling the wrong tool?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "A max-steps budget bounds the loop, every tool call is validated against a schema (with a repair retry), and tools are least-privilege and idempotent. Output is guarded before return.",
        senior: "I add per-run token/cost budgets and a step trace for observability, and I treat tool outputs as untrusted input. Reliability is engineering around the loop; the prompt is the least important part.",
        followups: ["Where would you add human-in-the-loop?", "How do you test it?"],
        redflag: "No bound on the loop; trusting the model to stop." },
    ],
    checklist: ["Bounded loop + token budget", "Schema-validated tools + repair", "Output guardrail + injection test", "Step trace returned", "Task-success eval"],
  },
});

// concise project factory for the remaining 7
function quickProject(o) {
  P({ num: o.num, title: o.title, path: o.path, kw: o.kw, data: {
    eyebrow: "Hands-on Projects", title: "Project · " + o.title, lead: o.lead,
    pills: o.pills || [{ t: "Project" }],
    proves: o.proves,
    architecture: o.flow ? diagram(null, flow(o.flow, "lr")) : undefined,
    stack: o.stack,
    api: o.api, dataflow: o.dataflow, evalplan: o.evalplan, failures: o.failures,
    demo: o.demo, readme: o.readme || ["Problem & users · Architecture · Quickstart · Design decisions · Evaluation · Limitations & next steps."],
    explain: o.explain,
    questions: o.questions,
    checklist: o.checklist,
  } });
}

quickProject({
  num: "03", title: "PDF Q&A App", path: "projects/pdf-qna.html",
  kw: "pdf qna question answering rag chunking citations upload",
  lead: "RAG specialized to PDFs: parsing, layout-aware chunking, page-level citations. The most common 'show me a RAG demo' ask, done with rigor.",
  proves: ["You can handle messy real documents (tables, headers, multi-column) — not just clean text — and cite back to the exact page."],
  flow: [{ t: "PDF upload" }, { t: "Parse + layout-aware chunk", accent: true }, { t: "Embed + index" }, { t: "Retrieve + answer + page citation" }],
  stack: { headers: ["Concern", "Tool"], rows: [["PDF parse", "PyMuPDF / unstructured"], ["Chunk", "Structure-aware + page metadata"], ["Rest", "Same as First RAG App"]] },
  api: ["POST /upload (pdf) → indexed; POST /ask → {answer, citations:[{page, snippet}]}."],
  dataflow: ["PDF → parse text + page numbers → chunk preserving structure → embed → index with page metadata → retrieve → answer citing pages."],
  evalplan: ["Golden set of Q→(answer, page). Measure answer correctness + citation-page accuracy."],
  failures: ["Tables/multi-column mangled by naive parsing", "Citations point to wrong page", "Scanned PDFs need OCR"],
  demo: ["Upload a real PDF, ask a question whose answer is in a table, get the right answer with the right page citation."],
  explain: ["“RAG over PDFs with page-level citations; the hard part was layout-aware parsing so tables and multi-column text survive chunking.”"],
  questions: [explainQ("PDF Q&A", "“Upload a PDF, ask questions, get answers cited to the exact page.”"),
    { q: "What's hard about RAG over PDFs specifically?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: "Parsing: PDFs have tables, multi-column layouts, headers/footers and sometimes are scanned. Naive text extraction destroys structure and pollutes chunks; you need layout-aware parsing (and OCR for scans) plus page metadata for citations.",
      senior: "I evaluate parsing quality separately because most PDF-RAG failures are upstream of retrieval. I keep page/section metadata through the pipeline so citations are precise and access control is possible.",
      followups: ["How do you handle scanned PDFs?", "How do you cite a table cell?"], redflag: "Assuming PDF = clean text." },
    { q: "How do you evaluate this PDF Q&A app?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
      model: ["Build a golden set of question → (answer, page). Measure answer correctness AND citation-page accuracy separately. Add 'not in the document' cases to test the abstain path."],
      senior: "A senior evaluates parsing quality upstream too (did the table survive extraction?), since most PDF-RAG failures are pre-retrieval.", followups: ["How do you test parsing separately?"], redflag: "Only checking a few answers look right." },
    { q: "A user uploads a 300-page PDF — what changes?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Chunk and index it; never stuff the whole PDF into the prompt.</li><li>Show ingestion progress; index asynchronously.</li><li>Retrieval + rerank keeps only the few relevant chunks per question, so size doesn't blow up cost.</li></ul>"],
      senior: "A senior notes per-document scaling (one big doc) differs from corpus scaling (many docs) and handles both — async ingestion and tight retrieval keep latency/cost flat regardless of document size.", followups: ["How do you keep latency flat as docs grow?"], redflag: "Trying to fit the whole PDF in the context window." }],
  checklist: ["Layout-aware parsing", "Page-level citations", "Eval on citation accuracy", "Handles a table-based question"],
});

quickProject({
  num: "04", title: "SQL Agent", path: "projects/sql-agent.html",
  kw: "sql agent text to sql database tool calling read only guardrails",
  lead: "A natural-language → SQL agent over a real schema. The canonical 'agent with a dangerous tool' project — so it's really about safety and correctness.",
  pills: [{ t: "Agent + safety", cls: "amber" }],
  proves: ["You can build an action-taking agent safely: read-only, schema-aware, validated, with guardrails on a tool that could do damage."],
  flow: [{ t: "NL question" }, { t: "Agent: inspect schema", accent: true }, { t: "Generate SQL" }, { t: "Validate (read-only, limits)" }, { t: "Execute + summarize" }],
  stack: { headers: ["Concern", "Tool"], rows: [["DB", "Postgres / SQLite"], ["Safety", "Read-only role, query allow-list, row limits"], ["Agent", "Tool-calling loop / LangGraph"]] },
  api: ["POST /query → {question} ⇒ {sql, rows, summary}. Return the generated SQL for transparency."],
  dataflow: ["Question → provide schema to model → generate SQL → validate (SELECT-only, add LIMIT, block DDL/DML) → execute on read-only connection → summarize results."],
  evalplan: ["Golden set of question→expected SQL/result. Measure execution accuracy and safety (zero unsafe queries pass the guard)."],
  failures: ["Generating destructive SQL → read-only + validation", "Hallucinated columns → ground in schema, validate", "Huge result sets → enforce LIMIT", "Injection via question → validate, parameterize"],
  demo: ["Ask an analytical question → see the SQL + results. Try to make it DROP a table → guard blocks it."],
  explain: ["“A text-to-SQL agent on a read-only connection with query validation. The real work is safety: it can never run anything but a bounded SELECT, and I show the SQL for trust.”"],
  questions: [explainQ("SQL Agent", "“It turns natural-language questions into safe, read-only SQL over a real schema and summarizes the results.”"),
    { q: "How do you make a SQL-generating agent safe?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
      model: "Read-only DB role, validate generated SQL (SELECT-only, no DDL/DML), enforce LIMITs and timeouts, parameterize, and surface the SQL for transparency. Treat the question as untrusted.",
      senior: "Safety lives in what the tool can't do, not in the prompt: a least-privilege role makes destructive queries impossible regardless of what the model emits. I also add row/cost limits and log every query for audit.",
      followups: ["What if it needs write access?", "How do you ground it in the schema?"], redflag: "Giving the agent a read-write connection and trusting the prompt." },
    { q: "How do you get correct SQL on a large or ambiguous schema?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
      model: ["<ul><li>Give the model the relevant schema (table/column descriptions) — retrieve it (RAG over the schema) rather than dumping everything.</li><li>Provide example question→SQL pairs (few-shot).</li><li>Self-check / repair: on an execution error, feed it back and retry once.</li></ul>"],
      senior: "A senior invests in a semantic layer of approved metrics/joins — it removes most ambiguity and is the highest-leverage fix — and measures execution accuracy on a golden set.", followups: ["What's a semantic layer?", "How do you handle ambiguous metrics?"], redflag: "Dumping the whole schema and hoping." },
    { q: "How would you explain this project's value in an interview?", difficulty: "Intermediate", round: "behavioral", seniority: "Senior", frequency: "high", region: "Both",
      model: ["Lead with the safety story: 'A text-to-SQL agent on a read-only connection with query validation — the interesting engineering is that it physically cannot run anything destructive, and it shows the SQL for trust.'"],
      senior: "A senior frames it as 'agent with a dangerous tool, made safe by least privilege' — a transferable production pattern, not a demo.", followups: ["What was the hardest part?"], redflag: "Describing it as 'I connected GPT to a database'." }],
  checklist: ["Read-only role + SQL validation", "Row limits + timeouts", "Returns the SQL", "Safety eval (no unsafe query passes)"],
});

quickProject({
  num: "05", title: "Email Triage Agent", path: "projects/email-triage.html",
  kw: "email triage agent classification structured output routing automation",
  lead: "Classify and route incoming emails (category, urgency, suggested action) with structured output and a human-in-the-loop for low-confidence cases.",
  proves: ["You can ship a practical automation with structured output, confidence handling and human-in-the-loop — the bread-and-butter of applied GenAI."],
  flow: [{ t: "Email" }, { t: "Classify → structured output", accent: true }, { t: "Confidence gate" }, { t: "Route / draft reply / escalate" }],
  stack: { headers: ["Concern", "Tool"], rows: [["Extraction", "Tool calling + Pydantic schema"], ["Routing", "Rules on structured fields"], ["HITL", "Queue low-confidence for review"]] },
  api: ["POST /triage → {email} ⇒ {category, urgency, needs_human, draft_reply?}."],
  dataflow: ["Email → structured classification (category, urgency, confidence) → if confidence low or action risky → human queue; else auto-route + optional draft reply."],
  evalplan: ["Labeled emails → measure classification accuracy, and precision on the 'auto-handle' decisions (you care most about not auto-acting wrongly)."],
  failures: ["Over-confident misclassification → confidence gate + HITL", "Sensitive content auto-handled → policy guard", "Format drift → schema validation"],
  demo: ["Feed varied emails → show structured decisions; a low-confidence one routes to human; a clear one gets an auto draft."],
  explain: ["“An email triage agent that outputs structured decisions and only auto-acts when confident, escalating the rest. It's about safe automation, not full autonomy.”"],
  questions: [explainQ("Email Triage", "“It classifies and routes emails with structured output, auto-handling confident cases and escalating the rest.”"),
    { q: "How do you decide when to auto-act vs escalate to a human?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: "Use a confidence signal and risk of the action: auto-handle high-confidence, low-risk cases; escalate low-confidence or high-impact ones. Tune the threshold on labeled data against the cost of a wrong auto-action.",
      senior: "I optimize for precision on the auto-handled slice because a wrong automated action is costlier than asking a human. I monitor the escalation rate and the auto-action error rate as live metrics.",
      followups: ["How do you get a calibrated confidence?"], redflag: "Auto-acting on everything for a nicer demo." },
    { q: "How do you evaluate the triage agent?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["Labeled emails → measure classification accuracy, and especially <strong>precision on the auto-handled slice</strong> (a wrong auto-action is the costly error). Track escalation rate too."],
      senior: "A senior optimizes for precision on auto-actions over raw accuracy, because the business cost of a wrong automated reply outweighs an unnecessary escalation.", followups: ["Why precision over accuracy here?"], redflag: "Reporting only overall accuracy." },
    { q: "How would you make this safe for sensitive emails (legal, HR)?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Detect sensitive categories and force human review (never auto-act).</li><li>Redact PII before sending to the model and in logs.</li><li>Policy guard on any drafted reply before it leaves.</li></ul>"],
      senior: "A senior designs the confidence/risk gate so high-impact categories always escalate regardless of model confidence.", followups: ["How do you redact PII without losing context?"], redflag: "Auto-replying to sensitive emails to boost automation rate." }],
  checklist: ["Structured output + validation", "Confidence/risk gate + HITL", "Accuracy + auto-action precision eval"],
});

quickProject({
  num: "06", title: "Research Assistant Agent", path: "projects/research-assistant.html",
  kw: "research assistant agent multi step web search planner executor synthesis citations",
  lead: "A multi-step agent that plans, searches/reads multiple sources, and synthesizes a cited answer. Showcases planner-executor control and source attribution.",
  pills: [{ t: "Planner-executor", cls: "purple" }],
  proves: ["You can orchestrate multi-step reasoning with tools while keeping cost bounded and outputs attributed."],
  flow: [{ t: "Question" }, { t: "Plan sub-questions", accent: true }, { t: "Search + read sources" }, { t: "Synthesize with citations" }, { t: "Self-check" }],
  stack: { headers: ["Concern", "Tool"], rows: [["Control", "Planner-executor / LangGraph"], ["Tools", "Web/search + fetch + read"], ["Output", "Synthesis with citations"]] },
  api: ["POST /research → {question} ⇒ {report, sources:[...], steps:[...]}."],
  dataflow: ["Question → plan sub-questions → for each: search + fetch + extract → synthesize with citations → self-critique for unsupported claims → return report + sources."],
  evalplan: ["Rubric-based: coverage, correctness, citation validity (do sources support claims?), and cost/steps per query."],
  failures: ["Unbounded exploration → step/cost budget", "Unsupported synthesis → require citations + self-check", "Injection from fetched pages → untrusted content"],
  demo: ["Ask a real question → show the plan, the sources used, and the cited synthesis. Show the step budget kept cost bounded."],
  explain: ["“A planner-executor research agent that decomposes a question, gathers sources, and writes a cited synthesis with a self-check for unsupported claims — within a bounded step budget.”"],
  questions: [explainQ("Research Assistant", "“It decomposes a question, gathers and reads sources, and writes a cited synthesis.”"),
    { q: "How do you keep a multi-step research agent's cost and quality under control?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: "Plan up front (planner-executor) so steps are predictable, set a step/token budget, require citations and run a self-critique to drop unsupported claims, and treat fetched content as untrusted.",
      senior: "I make the plan explicit and bounded so cost is predictable, and I add a verification pass that checks each claim against a cited source — synthesis without attribution is where these agents quietly hallucinate.",
      followups: ["Why planner-executor over ReAct here?"], redflag: "Open-ended ReAct with no budget or citation requirement." },
    { q: "How do you stop the research agent from hallucinating a synthesis?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Require every claim in the synthesis to cite a fetched source.</li><li>Run a self-critique pass that flags unsupported claims and removes them.</li><li>Treat fetched web content as untrusted (injection risk).</li></ul>"],
      senior: "A senior adds a verification step that checks each claim against its cited source — synthesis without attribution is exactly where these agents quietly fabricate.", followups: ["How is fetched content an injection vector?"], redflag: "Trusting the synthesis because it 'sounds authoritative'." },
    { q: "How would you explain this project in an interview?", difficulty: "Intermediate", round: "behavioral", seniority: "Senior", frequency: "high", region: "Both",
      model: ["'A planner-executor research agent: it decomposes a question, gathers and reads sources, and writes a cited synthesis with a self-check — all within a bounded step budget so cost is predictable.'"],
      senior: "A senior emphasizes the control pattern choice (planner-executor for predictability) and the citation/verification discipline as the production-relevant parts.", followups: ["What would you improve?"], redflag: "Describing it as 'it googles and summarizes'." }],
  checklist: ["Planner-executor with step budget", "Citations + self-critique", "Untrusted-content handling", "Rubric eval (coverage, citation validity)"],
});

quickProject({
  num: "07", title: "Customer Support RAG Bot", path: "projects/customer-support-rag.html",
  kw: "customer support rag bot knowledge base escalation guardrails citations deflection",
  lead: "Production-shaped RAG: answer from a help center with citations, abstain + escalate to a human when unsure, with guardrails and feedback capture. The most 'business-relevant' RAG project.",
  pills: [{ t: "Production-shaped", cls: "green" }],
  proves: ["You can ship customer-facing RAG responsibly: grounding, abstain/escalate, guardrails, and a feedback loop — the things businesses actually need."],
  flow: [{ t: "User question" }, { t: "Retrieve KB (hybrid)", accent: true }, { t: "Coverage gate" }, { t: "Answer + cite OR escalate" }, { t: "Capture feedback" }],
  stack: { headers: ["Concern", "Tool"], rows: [["RAG", "Same as First RAG App"], ["Escalation", "Confidence/coverage threshold → human handoff"], ["Feedback", "thumbs + reason → eval set"]] },
  api: ["POST /support → {question, user} ⇒ {answer?, citations?, escalate:bool}. Enforce per-user/tenant access at retrieval."],
  dataflow: ["Question → hybrid retrieve over KB (tenant-scoped) → if coverage below threshold → escalate; else grounded answer with citations → output guard → capture user feedback → feed failures into eval set."],
  evalplan: ["Golden set + 'deflection' metric (correctly auto-answered vs correctly escalated). Track faithfulness and escalation precision."],
  failures: ["Confident wrong answer → coverage gate + abstain", "Cross-tenant leak → retrieval access control", "Stale KB → re-index pipeline", "Injection in KB articles → untrusted content"],
  demo: ["In-scope question → cited answer; ambiguous/unknown → graceful escalation; show feedback capture and the eval loop."],
  explain: ["“Customer-support RAG with citations, an abstain-and-escalate path, tenant-scoped retrieval and a feedback loop into evaluation. It's tuned for trust and deflection, not just answering.”"],
  questions: [explainQ("Customer Support RAG Bot", "“It answers support questions from the help center with citations, escalating to a human when it isn't confident.”"),
    { q: "How do you decide when the bot should answer vs escalate to a human?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
      model: "A coverage/confidence gate on retrieval + answer: if the top retrieval scores or a faithfulness check fall below threshold, abstain and escalate. Tune the threshold against the cost of a wrong answer vs an unnecessary escalation.",
      senior: "I optimize for trust: in support, a confident wrong answer is worse than an escalation, so I bias toward abstaining and measure escalation precision. I also close the loop — every escalation and thumbs-down becomes an eval case and often a KB gap to fill.",
      followups: ["How do you prevent cross-tenant data leaks?", "How do you measure deflection without hurting CSAT?"], redflag: "Always answering to maximize 'deflection' regardless of correctness." },
    { q: "How do you prevent the bot leaking another customer's or internal-only data?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
      model: ["<ul><li>Scope retrieval to the customer's tenant/locale via metadata filters — restricted docs are never candidates.</li><li>Keep internal-only articles out of the customer-facing index (or tag and filter them).</li><li>Output guard scans for internal terms/PII before replying.</li></ul>"],
      senior: "A senior enforces tenancy at the retrieval layer (not the prompt) and may use separate indexes for hard isolation.", followups: ["Separate index vs metadata filter?"], redflag: "Trusting the prompt to keep internal info hidden." },
    { q: "How do you keep the knowledge base fresh and measure quality over time?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Incremental re-index on article changes; show 'last updated' in citations.</li><li>Track deflection, escalation precision, faithfulness on sampled traffic, and CSAT.</li><li>Feed every thumbs-down and escalation into the eval set and as a KB-gap signal.</li></ul>"],
      senior: "A senior closes the loop: production failures become eval cases and content-gap tickets, so the bot and the KB improve together.", followups: ["How do you find KB gaps?"], redflag: "Static KB with no freshness or feedback loop." }],
  checklist: ["Tenant-scoped retrieval", "Coverage gate → abstain/escalate", "Citations + output guard", "Feedback loop into eval", "Deflection + faithfulness metrics"],
});

quickProject({
  num: "08", title: "Resume Screening RAG App", path: "projects/resume-screening-rag.html",
  kw: "resume screening rag app candidate matching jd structured extraction fairness bias",
  lead: "Match resumes to a job description with retrieval + structured extraction, returning evidence-backed scores — and explicitly handling bias/fairness, which interviewers will probe.",
  pills: [{ t: "Has a fairness angle", cls: "amber" }],
  proves: ["You can combine extraction + retrieval for a real business task AND reason about fairness, explainability and the limits of automated decisions."],
  flow: [{ t: "JD + resumes" }, { t: "Extract structured fields", accent: true }, { t: "Retrieve/Match evidence" }, { t: "Score with citations" }, { t: "Human review" }],
  stack: { headers: ["Concern", "Tool"], rows: [["Extraction", "Schema-based (skills, experience)"], ["Matching", "Embedding similarity + criteria checks"], ["Explainability", "Evidence snippets per score"]] },
  api: ["POST /screen → {jd, resume} ⇒ {match_score, evidence:[...], gaps:[...]}. Never an auto-reject — assistive only."],
  dataflow: ["JD → required criteria. Resume → structured extraction + embedding. Match each criterion to resume evidence → score with citations → present to a human reviewer."],
  evalplan: ["Agreement with human reviewers on a labeled set; plus a fairness check (does score correlate with protected-proxy attributes it shouldn't?)."],
  failures: ["Bias amplification → audit, blind fields, human-in-the-loop", "Keyword gaming → semantic + evidence checks", "Over-trust → assistive only, never auto-reject"],
  demo: ["Screen a resume against a JD → show per-criterion evidence and gaps. Show that it ranks/assists but a human decides."],
  explain: ["“A resume–JD matcher that returns evidence-backed scores per criterion. I deliberately scoped it as assistive with human review, and I audit it for bias — automated hiring decisions are a liability.”"],
  questions: [explainQ("Resume Screening RAG", "“It matches resumes to a JD with evidence-backed, explainable scores to assist (not replace) a human reviewer.”"),
    { q: "What are the fairness/ethical risks here and how do you handle them?", difficulty: "Senior", round: "behavioral", seniority: "Senior/Staff", frequency: "emerging", region: "Both",
      model: "Risk of amplifying bias in historical data, proxy discrimination, and over-trust in scores. Mitigate with human-in-the-loop (assistive only), explainable evidence per score, auditing scores against protected proxies, and never auto-rejecting.",
      senior: "I'd push back on full automation entirely — the system assists ranking with transparent evidence and gets audited for disparate impact. Naming the legal/ethical exposure and designing around it is the point; a candidate who automates rejection silently is the red flag.",
      followups: ["How would you audit for bias?", "Would you ever auto-reject?"], redflag: "Building an auto-reject pipeline with no fairness consideration." },
    { q: "How do you make the match scores explainable and trustworthy?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Score per JD criterion, not one opaque number.</li><li>Attach the resume evidence (quoted snippet) behind each criterion score.</li><li>Show gaps explicitly so a human can judge.</li></ul>"],
      senior: "A senior makes every score traceable to evidence and frames the tool as assistive ranking — a human always decides.", followups: ["Why per-criterion instead of one score?"], redflag: "A single black-box 'match %'." },
    { q: "How do you evaluate this without a clean ground truth?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "emerging", region: "Both",
      model: ["<ul><li>Measure agreement with human reviewers on a labeled set.</li><li>Run a fairness check: does the score correlate with attributes it shouldn't (proxies for protected classes)?</li><li>Track reviewer override rate.</li></ul>"],
      senior: "A senior treats fairness auditing as a first-class metric alongside accuracy, and would refuse to ship full automation.", followups: ["What's a proxy attribute?"], redflag: "Only measuring 'accuracy' against past hiring decisions (which encode bias)." }],
  checklist: ["Structured extraction + evidence", "Assistive only (human decides)", "Bias audit", "Agreement-with-human eval"],
});

quickProject({
  num: "09", title: "Production GenAI Monitoring Dashboard", path: "projects/monitoring-dashboard.html",
  kw: "monitoring dashboard observability tracing langfuse cost latency quality drift alerts llmops project",
  lead: "The LLMOps capstone: a dashboard over traces of a live RAG/agent app showing cost, latency, quality scores and drift, with alerts. Proves the operational maturity platform teams want.",
  pills: [{ t: "Platform signal", cls: "blue" }],
  proves: ["You operate GenAI like a real service: end-to-end tracing, cost/latency/quality monitoring, drift detection and alerting."],
  flow: [{ t: "App requests" }, { t: "Trace spans → store", accent: true }, { t: "Score sample (judge)" }, { t: "Dashboard + alerts" }],
  stack: { headers: ["Concern", "Tool"], rows: [["Tracing", "Langfuse / OpenTelemetry / DIY spans"], ["Scoring", "LLM-as-judge on sampled traces"], ["Dashboard", "Grafana / Streamlit / custom"], ["Alerts", "Threshold + anomaly"]] },
  api: ["Instrument the existing RAG/agent app to emit spans (retrieval, LLM, tools, tokens, cost, latency); dashboard reads the trace store."],
  dataflow: ["Each request emits a trace tree → stored → a sampler runs faithfulness/quality judges → metrics aggregated (cost/req, p95 latency, abstain rate, quality) → dashboard + alerts on regressions/drift."],
  evalplan: ["The dashboard IS partly the eval surface; validate that injected regressions (e.g. degraded retrieval) actually trip the quality alert."],
  failures: ["PII in traces → redaction", "Sampling misses rare failures → adjust sampling", "Alert fatigue → tune thresholds, anomaly detection"],
  demo: ["Show a live trace drill-down (why was this answer slow/expensive/bad), the cost/latency/quality charts, and trigger an alert by degrading retrieval."],
  explain: ["“A monitoring dashboard over my RAG app's traces: cost/req, p95 latency, faithfulness on sampled traffic, and drift alerts. I can drill into any request to see retrieval, prompts and tokens — which is how I debug quality in production.”"],
  questions: [explainQ("Monitoring Dashboard", "“It traces every request and surfaces cost, latency, quality and drift with alerts, so production issues are visible and debuggable.”"),
    { q: "What would you monitor for a production GenAI app and why?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
      model: "Cost per request, p95 latency, error/abstain rates, sampled quality (faithfulness/relevance), and input/output drift — plus per-step traces to debug. Alert on regressions and anomalies.",
      senior: "I tie monitoring to the eval set: the same judges that gate deploys score sampled production traffic, so quality is continuously measured, not just at release. I attach trace ids end-to-end and log retrieved context so most debugging is a single trace lookup. Drift detection on inputs warns me before quality drops.",
      followups: ["How do you detect quality drift without labels?", "How do you handle PII in traces?"], redflag: "Monitoring only uptime/latency and ignoring quality." },
    { q: "Walk me through using this dashboard to debug a bad answer.", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
      model: ["Open the request's trace and check in order: retrieved chunks (right ones?), the actual prompt, the model output, then any guardrail step. The logged context tells you instantly whether it was a retrieval or generation failure."],
      senior: "A senior designed the trace specifically so this takes seconds, then turns the bad case into a golden-set regression test.", followups: ["What do you log to make this fast?"], redflag: "Having no per-step trace to inspect." },
    { q: "How do you detect quality regressions in production without labels?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["<ul><li>Run an LLM-as-judge faithfulness check on a sample of live traffic; alert on score drops.</li><li>Watch proxy signals: abstain rate, citation coverage, thumbs-down, latency.</li><li>Detect input drift (new kinds of questions).</li></ul>"],
      senior: "A senior reuses the same judges that gate deploys to score production, so offline and online quality share one definition.", followups: ["How do you trust the judge at scale?"], redflag: "Assuming no labels means quality can't be measured." }],
  checklist: ["End-to-end trace tree", "Sampled quality scoring", "Cost/latency/quality/drift charts", "Alerting + a demonstrated trip", "PII redaction in traces"],
});

export default PROJECTS;
