import { code, table, flow, diagram, callout } from "../render.mjs";
import { SRC } from "./sources.mjs";

export const CORE3 = [];

// ───────────────────── 09 · LLM EVALUATION ─────────────────────
CORE3.push({
  kind: "learn", group: "core", num: "09", title: "LLM Evaluation", path: "pages/llm-evaluation.html",
  kw: "llm evaluation golden dataset llm as judge faithfulness groundedness answer relevance ragas offline online eval metrics regression ci",
  data: {
    eyebrow: "Core Track",
    title: "LLM Evaluation",
    lead: "How you prove a GenAI feature works and keep it working. Golden datasets, LLM-as-judge, the metrics that matter (faithfulness, relevance, correctness), and treating evaluation as CI — the discipline that separates engineers from prompt tinkerers.",
    audience: "Engineers who must ship and maintain LLM features responsibly.",
    goal: "Design an evaluation harness and defend your quality numbers in an interview.",
    learn: ["Why eval is hard for non-deterministic systems", "Golden datasets: building and sizing them", "LLM-as-judge and how to trust it", "Core metrics: faithfulness, relevance, correctness", "Offline vs online evaluation", "Eval as CI / regression gating"],
    pills: [{ t: "Senior signal", cls: "purple" }, { t: "High frequency", cls: "red" }],
    whyInterviews: ["Anyone can make a demo look good once. Interviewers ask 'how do you know it works?' to find engineers who think in measurement. A strong eval answer is one of the clearest seniority signals in the loop."],
    answer60: "“Evaluation is how I keep a non-deterministic system trustworthy. I build a small golden dataset of inputs with expected outputs/criteria, score outputs with a mix of deterministic checks and LLM-as-judge (faithfulness, relevance, correctness), and run it like CI on every prompt/model/retrieval change. In production I sample live traffic and run the same judges, plus capture user feedback — so quality is a number I can track, not a vibe.”",
    answer3: [
      "Two surfaces: <strong>offline</strong> (a curated golden set run pre-deploy, like a test suite) and <strong>online</strong> (sampled production traffic scored continuously). For RAG I split retrieval metrics (recall@k, MRR) from generation metrics (faithfulness, answer relevance, correctness).",
      "<strong>LLM-as-judge</strong> scales scoring but must be trusted: I use a clear rubric, low temperature, pairwise or reference-based scoring where possible, and I calibrate the judge against a sample of human labels (and watch for position/verbosity bias). I version the judge prompt like any other.",
      "The discipline that matters most: <strong>eval as CI</strong>. Every prompt edit, model swap or retrieval change runs the golden set and must not regress. That's what lets a team iterate on LLM features without fear, and it's exactly what interviewers are listening for.",
    ],
    concept: [
      "<h3>Metrics that matter</h3>",
      table(["Metric", "Measures", "How"], [
        ["Faithfulness / groundedness", "Are claims supported by context?", "LLM-as-judge vs retrieved context"],
        ["Answer relevance", "Does it address the question?", "Judge or embedding similarity to question"],
        ["Correctness", "Right vs ground truth", "Golden answers / exact / judge"],
        ["Context recall@k / MRR", "Did retrieval find the right docs?", "Labeled query→doc set"],
        ["Toxicity / safety", "Unsafe output?", "Classifier / guardrail"],
      ]),
      "<h3>Golden dataset</h3><p>30–200 representative cases is enough to start — coverage matters more than size. Include happy paths, edge cases, adversarial inputs, and 'should abstain' cases. Grow it from real failures (every production bug becomes a test case).</p>",
      code("python", "# Eval as CI: fail the build on regression\ncases = load_golden(\"eval/golden.jsonl\")\nscores = [judge(faithfulness, run_app(c.input), c) for c in cases]\nmean = sum(scores) / len(scores)\nassert mean >= BASELINE - 0.02, f\"Faithfulness regressed: {mean:.3f}\""),
      callout("tip", "Every production failure becomes a test", "The fastest way to a strong golden set is to add each real-world miss as a case. Your eval set then encodes exactly the mistakes you've already made — and guarantees you don't repeat them."),
    ],
    diagram: diagram(
      "flowchart LR\n  Change[Prompt/model/retrieval change] --> Off[Offline: golden set + judges]\n  Off -->|pass| Deploy\n  Deploy --> On[Online: sample traffic + judges + user feedback]\n  On --> Golden[New failures -> golden set]\n  Golden --> Off",
      flow([{ t: "Change" }, { t: "Offline golden set", s: "gate the deploy", accent: true }, { t: "Online sampling" }, { t: "Failures → golden set" }], "lr"),
      "A closed loop: offline gates deploys, online finds new failures, failures grow the golden set."
    ),
    tradeoffs: { headers: ["Approach", "Pro", "Con"], rows: [
      ["Human eval", "Gold standard quality", "Slow, expensive, doesn't scale"],
      ["LLM-as-judge", "Scales, cheap, fast", "Bias; must calibrate vs humans"],
      ["Deterministic checks", "Reliable, free", "Only works for structured/exact outputs"],
      ["Online only", "Real distribution", "Catches regressions after users do"],
    ] },
    questions: [
      { q: "How do you evaluate a GenAI feature before and after shipping it?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Offline golden set with judges as a pre-deploy gate; online sampling of live traffic scored by the same judges plus user-feedback signals. Split metrics by stage (retrieval vs generation for RAG).",
        senior: "I run eval as CI so no change ships without passing the golden set, and I close the loop by turning production failures into new cases. I calibrate LLM judges against human labels and track the judge's agreement over time. The goal is that 'is it better?' is always answerable with a number.",
        followups: ["How big is your golden set?", "How do you trust the judge?", "Offline passes but users complain — what now?"],
        redflag: "'We manually check a few outputs.'",
        task: "Add an offline eval CI gate + online sampling to your RAG app." },
      { q: "How do you trust LLM-as-judge?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: "Use a clear rubric, low temperature, reference-based or pairwise comparisons, and calibrate against a human-labeled sample. Watch for position, verbosity and self-preference bias.",
        senior: "I measure judge–human agreement (e.g. on 50 labeled cases) and only trust the judge where agreement is high; for high-stakes calls I keep humans in the loop. I also use a stronger/different model as judge than the one being judged to reduce self-preference, and I version the judge prompt and re-validate it when I change it.",
        followups: ["What biases affect LLM judges?", "Same model as judge and generator — problem?"],
        redflag: "Treating judge scores as ground truth with no calibration." },
      { q: "What's a golden dataset and how do you build one?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Mid–Senior", frequency: "high", region: "Both",
        model: "A curated set of representative inputs with expected outputs or pass/fail criteria, used to measure quality and catch regressions. Build from real queries, edge cases, adversarial inputs, and abstain cases; grow it from production failures.",
        senior: "I prioritize coverage over size — 50 well-chosen cases beat 5,000 near-duplicates. I tag cases by category so I can see which kind of query regressed, and I keep an 'adversarial' slice (injections, out-of-scope) as a permanent gate.",
        followups: ["How do you keep it from going stale?"],
        redflag: "Thinking you need thousands of cases before you can start." },
      { q: "Offline vs online evaluation — what's the difference and why both?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["<ul><li><strong>Offline</strong> — run a curated golden set before deploy, like a test suite. Gates releases; catches regressions early.</li><li><strong>Online</strong> — score a sample of real production traffic plus user feedback. Catches what your golden set didn't anticipate.</li></ul>", "You need both: offline can't cover the real distribution; online catches issues after users hit them."],
        senior: "A senior runs the same judges in both, so offline and online share one quality definition, and feeds online failures back into the offline golden set — a closed loop.",
        followups: ["How do you score online traffic without labels?", "How do you detect input drift?"],
        redflag: "Relying only on offline (misses real usage) or only on online (catches regressions after users do).", source: SRC.ragas },
      { q: "How do you evaluate retrieval separately from generation in RAG?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["<ul><li><strong>Retrieval</strong> — on a labeled set of question→relevant-doc, measure recall@k, MRR, nDCG. Did the right chunks come back?</li><li><strong>Generation</strong> — given the retrieved context, measure faithfulness (claims supported), answer relevance, and correctness vs a golden answer.</li></ul>", "Splitting them tells you which half to fix when quality drops."],
        senior: "A senior insists on this split because a single blended score hides whether a regression is retrieval or generation — the two have completely different fixes (chunking/hybrid vs prompt/grounding).",
        followups: ["What if retrieval is good but the answer is wrong?", "How do you build the labeled retrieval set?"],
        redflag: "One combined 'quality' number with no way to localize a failure.", source: SRC.ragas },
    ],
    hands: ["Build a 40-case golden set for your RAG app (tagged by category, with adversarial cases), wire faithfulness + recall@k judges, and gate a CI run on no-regression."],
    portfolio: ["An eval harness with published numbers is one of the strongest portfolio signals. Add an 'Evaluation' section to your <a href='../projects/build-first-rag.html'>RAG app</a> README and screenshots to the <a href='../projects/monitoring-dashboard.html'>monitoring dashboard</a>."],
    mistakes: ["Eyeballing outputs", "Trusting LLM judges without calibration", "One blended metric that hides retrieval vs generation", "Never updating the golden set"],
    checklist: ["I can design offline + online eval", "I can build and size a golden set", "I can calibrate an LLM judge", "I run eval as CI"],
    sources: ["Section 9 (evaluation & observability), section 20 of the source material."],
  },
});

// ───────────────────── 10 · LLMOPS & OBSERVABILITY ─────────────────────
CORE3.push({
  kind: "learn", group: "core", num: "10", title: "LLMOps & Observability", path: "pages/llmops-observability.html",
  kw: "llmops observability tracing langfuse langsmith latency cost optimization caching streaming fallback models deployment monitoring rate limiting",
  data: {
    eyebrow: "Core Track",
    title: "LLMOps & Observability",
    lead: "Running GenAI in production: tracing every request, controlling latency and cost (caching, streaming, model routing, fallbacks), and monitoring quality and drift. This is where backend engineers have a real edge — and where applied/platform interviews dig in.",
    audience: "Engineers responsible for GenAI features in production.",
    goal: "Instrument, optimize and operate an LLM system and defend the tradeoffs.",
    learn: ["Tracing LLM apps (spans, Langfuse/LangSmith concepts)", "Latency optimization: streaming, parallelism, smaller models", "Cost optimization: caching, routing, prompt compression", "Fallback models & graceful degradation", "Monitoring: quality, cost, latency, drift", "Deployment patterns for LLM services"],
    pills: [{ t: "Backend edge", cls: "green" }, { t: "Platform loops", cls: "blue" }],
    whyInterviews: ["LLM calls are slow, expensive and non-deterministic — the operational concerns are real and constant. Platform/applied interviews probe whether you can keep a GenAI feature fast, cheap, observable and reliable, not just functional."],
    answer60: "“LLMOps is treating LLM features like production systems. I trace every request end-to-end (retrieval, prompts, tool calls, tokens, cost, latency) with something like Langfuse or LangSmith. I cut cost with caching, model routing (small model for easy cases, large for hard), and prompt trimming; I cut latency with streaming and parallel retrieval. I add fallback models for provider failures, and I monitor quality, cost, latency and drift with alerts — the same rigor as any service, plus token economics.”",
    answer3: [
      "<strong>Observability:</strong> a single LLM 'request' is really a tree — retrieval, rerank, one or more model calls, tool calls. I trace it as spans capturing inputs, outputs, token counts, cost and latency per step, so I can debug 'why was this answer bad/slow/expensive?'. That's what Langfuse/LangSmith give you.",
      "<strong>Cost & latency levers:</strong> caching (exact and semantic) for repeated queries; model routing so cheap models handle easy traffic; prompt/context compression and fewer reranked chunks; streaming for perceived latency; running retrieval and independent tool calls in parallel; batching offline work.",
      "<strong>Reliability:</strong> fallback models and providers, timeouts and retries with backoff, rate-limit handling, and graceful degradation (e.g. serve a cached or smaller-model answer under load). <strong>Monitoring:</strong> dashboards + alerts on cost per request, p95 latency, error/abstain rates, and sampled quality scores, plus drift detection on inputs and outputs.",
    ],
    concept: [
      "<h3>Trace tree</h3>",
      flow([{ t: "Request span" }, { t: "Retrieve span", s: "latency, #chunks" }, { t: "LLM span", s: "tokens, cost, latency", accent: true }, { t: "Tool span" }, { t: "Scores: quality, cost" }], "lr"),
      "<h3>Cost & latency playbook</h3>",
      table(["Lever", "Cuts", "Tradeoff"], [
        ["Exact + semantic cache", "Cost & latency", "Staleness; cache invalidation"],
        ["Model routing / cascade", "Cost", "Routing logic; misroute risk"],
        ["Smaller / distilled model", "Cost & latency", "Possible quality drop — must eval"],
        ["Prompt/context compression", "Cost & latency", "Risk dropping needed context"],
        ["Streaming", "Perceived latency", "Harder to validate mid-stream"],
        ["Parallel retrieval/tools", "Latency", "Complexity"],
        ["Fallback model/provider", "Downtime", "Quality varies across providers"],
      ]),
      code("python", "# Minimal trace + cache + fallback sketch\n@trace(\"answer\")\ndef answer(q):\n    if (hit := cache.get(q)): return hit            # exact cache\n    ctx = retrieve(q)                                # traced span\n    try:\n        out = call(PRIMARY_MODEL, prompt(q, ctx), timeout=8)\n    except (Timeout, ProviderError):\n        out = call(FALLBACK_MODEL, prompt(q, ctx))   # graceful degradation\n    cache.set(q, out, ttl=3600)\n    return out"),
      callout("note", "Where your backend background pays off", "Tracing, caching, rate limits, retries, SLOs, dashboards — you already know these. The new parts are token economics and non-deterministic quality. Lead with the transfer."),
    ],
    diagram: diagram(
      "flowchart LR\n  R[Request] --> C{Cache hit?}\n  C -->|yes| Out\n  C -->|no| Route{Easy or hard?}\n  Route -->|easy| Small[Small model]\n  Route -->|hard| Big[Large model]\n  Small --> Trace[Trace + score]\n  Big --> Trace\n  Trace --> Mon[Monitor: cost/latency/quality/drift]",
      flow([{ t: "Request" }, { t: "Cache → route", s: "small vs large model", accent: true }, { t: "Trace + score" }, { t: "Monitor + alert" }], "lr"),
      "Cache, route, trace, monitor — the operational spine of a GenAI service."
    ),
    tradeoffs: { headers: ["Goal", "First reach for", "Then"], rows: [
      ["Lower cost", "Caching + routing", "Smaller model (eval first), prompt trim"],
      ["Lower latency", "Streaming + parallelism", "Smaller model, retrieval tuning"],
      ["Higher reliability", "Timeouts + retries + fallback", "Graceful degradation, circuit breakers"],
    ] },
    questions: [
      { q: "How do you reduce the cost of an LLM feature without hurting quality?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: "Cache repeated queries (exact + semantic), route easy traffic to cheaper models and reserve the large model for hard cases, trim prompts/context, and batch offline work. Validate any model downgrade against the eval set so quality doesn't silently drop.",
        senior: "I instrument cost per request first so I optimize where the money actually is. The biggest wins are usually caching and routing. Crucially, every cost optimization runs through the eval gate — I never trade quality blindly. I also separate one-time indexing cost from per-query cost in the analysis.",
        followups: ["How does semantic caching work and when is it risky?", "How do you decide the routing threshold?"],
        redflag: "Downgrading the model with no eval to confirm quality held.",
        task: "Add caching + routing to your app and report cost/request before/after on the eval set." },
      { q: "What do you trace in an LLM app and why?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "The full request tree: retrieved chunks, prompts, model calls, tool calls, token counts, cost and latency per step, plus quality scores. So you can debug bad/slow/expensive answers and monitor trends. Tools like Langfuse/LangSmith do this.",
        senior: "I attach a trace/session id end-to-end and log the retrieved context with each answer — that single thing makes most RAG debugging tractable, because I can see whether a failure was retrieval or generation. I also sample traces for offline judging so observability and evaluation share one pipeline.",
        followups: ["How do you debug a single bad answer from a trace?", "How do you handle PII in traces?"],
        redflag: "Logging only the final answer with no intermediate steps." },
      { q: "How do you keep an LLM feature reliable when the provider is slow or down?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "Timeouts with retries/backoff, a fallback model or provider, rate-limit handling, and graceful degradation (cached or smaller-model answer, or an honest 'try again'). Circuit breakers to avoid hammering a failing provider.",
        senior: "I design for provider failure as a normal event: health checks, a secondary provider with a known-acceptable quality floor (validated on eval), and degradation tiers. I also separate 'must answer now' paths from 'can queue' paths so I can shed load gracefully under incidents.",
        followups: ["How do you ensure the fallback model's quality is acceptable?"],
        redflag: "Assuming one provider with no fallback or timeout." },
      { q: "What metrics do you track for a GenAI app in production?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Four families:", "<ul><li><strong>Quality</strong> — sampled faithfulness/relevance scores, abstain rate, user feedback (thumbs).</li><li><strong>Cost</strong> — tokens and $ per request, per feature, per tenant.</li><li><strong>Latency</strong> — p50/p95/p99, broken down per step (retrieval, model, tools).</li><li><strong>Reliability</strong> — error rate, timeout/fallback rate, rate-limit hits.</li></ul>", "Plus input/output drift over time."],
        senior: "A senior ties quality monitoring to the same judges that gate deploys, so production is continuously scored — not just availability. They alert on regressions and anomalies, not just thresholds.",
        followups: ["How do you measure quality without ground-truth labels in prod?", "How do you detect drift?"],
        redflag: "Monitoring only uptime and latency while ignoring answer quality.", source: SRC.langfuse },
      { q: "How do you handle fallback models?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: ["Have a secondary model/provider ready and trigger it on timeout, error, or rate-limit from the primary.", "<ul><li>Validate the fallback's quality on your eval set first — know its floor.</li><li>Use a circuit breaker so you stop hammering a failing provider.</li><li>Optionally degrade further: cached answer, smaller model, or an honest retry message.</li></ul>"],
        senior: "A senior designs degradation tiers and treats provider failure as a normal event, with the fallback's acceptable-quality threshold known in advance, not assumed.",
        followups: ["How do you know the fallback is good enough?", "Same prompt across providers — any gotchas?"],
        redflag: "A fallback model that's never been evaluated, so quality silently tanks during incidents." },
      { q: "How do you monitor for hallucinations in production?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["You can't label everything, so sample: run a faithfulness judge on a percentage of live traffic, comparing the answer to the retrieved context. Track the unsupported-claim rate over time and alert on spikes.", "Combine with cheap signals: citation coverage, abstain rate, and user thumbs-down."],
        senior: "A senior wires this into the same trace pipeline (so each flagged answer is debuggable end-to-end) and feeds confirmed hallucinations back into the golden set.",
        followups: ["How do you trust the judge?", "What sampling rate?"],
        redflag: "Assuming hallucinations are caught by users reporting them.", source: SRC.ragas },
      { q: "Walk me through debugging a single bad answer in production.", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Pull the trace for that request and check, in order:", "<ul><li><strong>Retrieval</strong> — were the right chunks retrieved? (If not, it's a retrieval bug — chunking/hybrid/rerank.)</li><li><strong>Prompt</strong> — what context and instructions were actually sent?</li><li><strong>Generation</strong> — did the model ignore the context or hit a length/overflow issue?</li><li><strong>Guardrails</strong> — did anything get filtered or altered?</li></ul>"],
        senior: "A senior relies on having logged the retrieved context with the answer, so they isolate retrieval-vs-generation in seconds, then reproduce it as a golden-set case so it can't regress.",
        followups: ["What do you log to make this fast?", "How do you turn it into a regression test?"],
        redflag: "Re-running it a few times and guessing, with no trace to inspect.", source: SRC.langfuse },
    ],
    hands: ["Instrument your RAG/agent app with tracing (Langfuse or a simple span logger), add exact+semantic caching and a fallback model, and build a small dashboard of cost/request, p95 latency and abstain rate."],
    portfolio: ["This is the <a href='../projects/monitoring-dashboard.html'>Production GenAI Monitoring Dashboard</a> project. Showing traces + cost/latency charts is a strong platform signal."],
    mistakes: ["No tracing → unexplainable failures", "Optimizing cost without an eval gate", "Single provider, no fallback/timeout", "Caching with no invalidation strategy"],
    checklist: ["I can describe a trace tree and why each span matters", "I have a cost and a latency playbook", "I can design fallback + graceful degradation", "I monitor quality, cost, latency, drift"],
    sources: ["Section 9 (observability), section 10 (LLMOps/production), Langfuse deep-dive."],
  },
});

// ───────────────────── 11 · GUARDRAILS & SECURITY ─────────────────────
CORE3.push({
  kind: "learn", group: "core", num: "11", title: "Guardrails & Security", path: "pages/guardrails-security.html",
  kw: "guardrails security prompt injection defense pii safety input validation output validation jailbreak tool security least privilege content moderation",
  data: {
    eyebrow: "Core Track",
    title: "Guardrails & Security",
    lead: "The rules an answer must pass before it ships, and the attacks you must assume. Input/output validation, prompt-injection defense, PII handling, and tool/agent security — increasingly a dedicated interview round as companies move GenAI to production.",
    audience: "Engineers shipping GenAI to real users / enterprise.",
    goal: "Design layered guardrails and reason about prompt injection and agent security.",
    learn: ["The guardrail layers: input, retrieval, output, action", "Prompt injection (direct & indirect) and defenses", "PII and data-leak prevention", "Tool/agent security: least privilege, human-in-the-loop", "Jailbreaks and content safety", "Why guardrails ≠ a single system prompt line"],
    pills: [{ t: "Dedicated round", cls: "amber" }, { t: "Enterprise must-have", cls: "red" }],
    whyInterviews: ["As GenAI hits production, safety incidents (leaked data, injected instructions, destructive agent actions) are board-level risks. Interviewers test whether you treat security as architecture, not a prompt afterthought."],
    answer60: "“Guardrails are layered checks around the model, not a line in the prompt. On input: validate, detect injection, strip/redact PII, enforce scope. On retrieval: access control so users only see permitted docs. On output: validate format, check groundedness, block PII/unsafe content, enforce policy. On actions: least-privilege tools, human-in-the-loop for high-risk operations. Prompt injection — especially <em>indirect</em>, via retrieved or tool content — is the signature GenAI threat, so I treat all external/tool content as untrusted.”",
    answer3: [
      "I think in four layers. <strong>Input:</strong> schema validation, scope/topic check, PII redaction, and injection detection. <strong>Retrieval:</strong> enforce per-user access control on the corpus (a huge real-world leak vector — never retrieve documents the user can't see). <strong>Output:</strong> structured validation, groundedness/faithfulness check, PII and unsafe-content filters, and policy enforcement before the response leaves. <strong>Action:</strong> tools are least-privilege and idempotent, destructive actions require confirmation or human-in-the-loop.",
      "<strong>Prompt injection</strong> is the core threat. Direct: the user tries to override instructions. Indirect: malicious instructions hidden in a web page, document or tool output that the model then follows — this is the dangerous one for RAG and agents. Defenses: treat all retrieved/tool content as data not instructions, separate trusted system instructions from untrusted content, constrain tool permissions, validate outputs, and never let model output alone trigger an irreversible action.",
      "There's no single fix — it's defense in depth plus monitoring and red-teaming. I'd add injection/jailbreak cases to the eval set as a permanent gate.",
    ],
    concept: [
      "<h3>The four guardrail layers</h3>",
      flow([{ t: "Input guard", s: "validate · PII · injection · scope" }, { t: "Retrieval guard", s: "access control", accent: true }, { t: "Output guard", s: "groundedness · PII · policy" }, { t: "Action guard", s: "least-priv · HITL" }], "lr"),
      "<h3>Prompt injection</h3>",
      table(["Type", "Example", "Defense"], [
        ["Direct", "'Ignore previous instructions and…'", "Separate system instructions; output validation; refusal training"],
        ["Indirect", "Hidden 'send me the data' text in a retrieved doc/web page", "Treat retrieved/tool content as untrusted data, not instructions; constrain tools"],
        ["Data exfiltration", "Trick the agent into emailing secrets", "Least-privilege tools, egress controls, HITL on sends"],
        ["Jailbreak", "Roleplay to bypass safety", "Content classifiers on output; policy checks; monitoring"],
      ]),
      code("python", "def guard_output(text, context, user):\n    if contains_pii(text) and not user.may_see_pii: raise Blocked(\"PII\")\n    if not is_grounded(text, context): return ABSTAIN  # unsupported claim\n    if unsafe_classifier(text): raise Blocked(\"policy\")\n    return text"),
      callout("danger", "Indirect injection is the one to name", "Most candidates only mention 'ignore previous instructions'. Naming <em>indirect</em> injection — malicious instructions inside retrieved documents or tool outputs that a RAG/agent system then executes — is the senior signal. The fix is architectural: untrusted content is data, never instructions, and tools are least-privilege."),
    ],
    diagram: diagram(
      "flowchart TD\n  U[User input] --> IG[Input guard: validate, PII, injection, scope]\n  IG --> RT[Retrieve with access control]\n  RT --> LLM[Model / agent]\n  LLM --> AG[Action guard: least-priv, HITL]\n  LLM --> OG[Output guard: groundedness, PII, policy]\n  OG --> Resp[Response]",
      flow([{ t: "Input guard" }, { t: "Retrieval access control", accent: true }, { t: "Model / agent" }, { t: "Output + action guards" }, { t: "Response" }], "lr"),
      "Defense in depth: a failure at one layer is caught at the next."
    ),
    tradeoffs: { headers: ["Guardrail", "Pro", "Con"], rows: [
      ["Strict output validation", "Safety, format guarantees", "Latency; may block valid answers"],
      ["Human-in-the-loop", "Catches high-risk actions", "Slower, costs human time"],
      ["Aggressive PII redaction", "Privacy", "Can remove needed context"],
      ["Least-privilege tools", "Limits blast radius", "More tools/roles to manage"],
    ] },
    questions: [
      { q: "What is prompt injection and how do you defend against it?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "An attack where malicious instructions in user input (direct) or in retrieved/tool content (indirect) make the model ignore its real instructions. Defend by separating trusted instructions from untrusted content, treating retrieved/tool data as data not commands, validating outputs, and constraining tool permissions. Defense in depth, not one trick.",
        senior: "I emphasize indirect injection as the dangerous case for RAG/agents — a poisoned document can hijack the system. Architecturally: untrusted content never carries authority, tools are least-privilege and idempotent, irreversible actions need confirmation/HITL, and I add injection cases to the eval gate and monitor for anomalies. There's no silver bullet, so I assume some attempts succeed and limit blast radius.",
        followups: ["How would you exfiltrate data from a naive RAG agent?", "Direct vs indirect — which is worse for agents?", "How do you test for injection?"],
        redflag: "'Just tell the model in the system prompt to ignore injection attempts.'",
        task: "Add an injection test suite to your RAG/agent app and an output guard.", source: SRC.promptInjection },
      { q: "How do you stop a RAG system from leaking data a user shouldn't see?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: "Enforce access control at retrieval: filter the corpus by the user's permissions before/while retrieving, so restricted documents are never even candidates. Don't rely on the model to 'not mention' restricted content.",
        senior: "Access control belongs in the retrieval layer with metadata-based filters tied to the user's identity/roles, ideally enforced in the vector store query itself, plus an output PII/secret check as a backstop. I also segregate indexes for hard tenancy boundaries. The model is the last place I'd trust to enforce permissions.",
        followups: ["Per-document ACLs vs separate indexes — when each?", "How do you handle a shared multi-tenant index?"],
        redflag: "Relying on a prompt instruction to keep restricted data hidden." },
      { q: "How do you secure an agent that can take actions (send email, run SQL)?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "Least-privilege, idempotent tools; validate every tool argument; require confirmation/human-in-the-loop for destructive or irreversible actions; egress controls; full audit logging; and treat tool outputs as untrusted (injection).",
        senior: "I scope each tool to the minimum capability (read-only SQL with row limits, send-email only to internal/allow-listed addresses), put irreversible actions behind explicit approval, and log every action with the reasoning that triggered it for audit. I assume the model can be manipulated, so safety comes from what the tools <em>can't</em> do, not from the model behaving.",
        followups: ["Where exactly do you put the human-in-the-loop?", "How do you make a SQL tool safe?"],
        redflag: "Giving an agent broad write/delete access and trusting it to behave.", source: SRC.owaspLLM },
      { q: "How do you handle PII in a GenAI app?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: ["<ul><li><strong>Input</strong> — detect and redact/tokenize PII before it reaches the model or logs.</li><li><strong>Retrieval</strong> — access control so PII docs only reach permitted users.</li><li><strong>Output</strong> — scan responses for PII/secret leakage before returning.</li><li><strong>Logs/traces</strong> — redact PII so observability doesn't become a leak.</li><li>Respect data-residency and retention rules.</li></ul>"],
        senior: "A senior balances redaction against utility (over-redaction breaks answers), keeps an audit trail, and treats traces/caches as PII surfaces people forget about.",
        followups: ["Doesn't redaction remove needed context?", "How do you handle PII in cached responses?"],
        redflag: "Sending raw PII to a third-party model and logging it in plaintext.", source: SRC.nist },
      { q: "What guardrails should an enterprise GenAI app have?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "high", region: "Both",
        model: ["A layered set, not one check:", "<ul><li><strong>Input</strong> — validation, scope/topic check, PII redaction, injection detection.</li><li><strong>Retrieval</strong> — per-user access control.</li><li><strong>Output</strong> — groundedness check, PII/secret filter, policy/brand compliance, citation presence.</li><li><strong>Action</strong> — least-privilege tools, human-in-the-loop on irreversible actions.</li><li><strong>Cross-cutting</strong> — audit logging, eval gate including adversarial cases, monitoring.</li></ul>"],
        senior: "A senior maps these to a recognized framework (OWASP LLM Top 10 / NIST AI RMF) so the controls are defensible to security and compliance, and makes them enforced platform-wide rather than per-app.",
        followups: ["How do you enforce these across many apps?", "Which framework do you reference?"],
        redflag: "Treating guardrails as a single system-prompt instruction.", source: SRC.owaspLLM },
    ],
    hands: ["Add input PII redaction, retrieval access-control filters, and an output groundedness+PII guard to your RAG app. Write 10 injection test cases (direct + indirect) and confirm they're caught."],
    portfolio: ["A 'Security & guardrails' README section with your injection test results is rare and impressive. Build it into the <a href='../projects/customer-support-rag.html'>support bot</a> and <a href='../projects/sql-agent.html'>SQL agent</a>."],
    mistakes: ["Treating guardrails as one prompt line", "Forgetting indirect injection", "Enforcing permissions in the prompt instead of retrieval", "Over-privileged agent tools"],
    checklist: ["I can name the four guardrail layers", "I can explain direct vs indirect injection + defenses", "I enforce access control at retrieval", "I can secure an action-taking agent"],
    sources: ["Guardrails deep-dive; sections 10, 11 of the source material."],
  },
});

// ───────────────────── 12 · GENAI SYSTEM DESIGN (overview) ─────────────────────
CORE3.push({
  kind: "learn", group: "core", num: "12", title: "GenAI System Design", path: "pages/genai-system-design.html",
  kw: "genai system design interview framework architecture clarifying requirements retrieval orchestration safety evaluation observability cost latency tradeoffs enterprise",
  data: {
    eyebrow: "Core Track",
    title: "GenAI System Design",
    lead: "The reusable framework for any GenAI system-design interview — clarify, requirements, architecture, ingestion, retrieval, orchestration, safety, evaluation, observability, then tradeoffs and failure modes. Learn it once here; apply it on the seven <a href='../systemdesign/enterprise-knowledge-assistant.html'>worked designs</a>.",
    audience: "Senior+ engineers facing GenAI system-design rounds.",
    goal: "Drive a 45-minute GenAI design interview with a structured, senior narrative.",
    learn: ["Why GenAI design differs from classic system design", "A universal 11-step framework", "The standard layered architecture", "How to lead with tradeoffs, failure modes and evaluation", "How to time-box a 10-minute explanation"],
    pills: [{ t: "Senior/Staff", cls: "purple" }, { t: "Design round", cls: "blue" }],
    whyInterviews: ["For 5–10 yr engineers, the design round often decides the level (and offer). GenAI design adds probabilistic behavior, retrieval quality, hallucination, cost/latency of model calls, safety and evaluation on top of classic concerns — and interviewers want to see you handle all of it without prompting."],
    answer60: "“I drive GenAI design with one framework: clarify the product and constraints; set functional and non-functional requirements (quality, latency, cost, safety); draw a layered architecture — ingestion, retrieval, model/orchestration, safety, evaluation, observability; then I lead with tradeoffs and failure modes. The LLM is one component inside a real system that handles auth, retrieval, validation, monitoring, cost control and feedback — saying that up front sets the level.”",
    answer3: [
      "I open by clarifying: who are the users, what's the data, what's the quality/latency/cost/safety bar, what scale? Then I state functional requirements (what it does) and non-functional (p95 latency, cost/query, accuracy/groundedness target, privacy/compliance).",
      "Then the layered architecture: <strong>ingestion</strong> (load, chunk, embed, index, refresh), <strong>retrieval</strong> (hybrid + rerank, access control), <strong>orchestration</strong> (prompts, model routing, agents/tools), <strong>safety</strong> (input/output guards, injection defense), <strong>evaluation</strong> (golden set, online sampling), <strong>observability</strong> (tracing, cost/latency/quality dashboards). I make the LLM clearly one box among many.",
      "Finally — and this is the seniority differentiator — I lead with <strong>tradeoffs</strong> (RAG vs fine-tune, model size vs cost/latency, caching vs freshness), <strong>failure modes</strong> (bad retrieval, hallucination, provider outage, injection) and how I'd detect and mitigate each. I keep checking back with the interviewer and adjust to their priorities.",
    ],
    concept: [
      "<h3>The universal framework</h3><ol><li>Clarify product, users, use cases</li><li>Quality / safety / latency / cost goals</li><li>High-level architecture</li><li>Data & knowledge ingestion</li><li>Retrieval / memory / tools</li><li>Prompt & model orchestration (routing)</li><li>Output validation & guardrails</li><li>Evaluation & feedback loops</li><li>Observability, reliability, scaling, deployment</li><li>Tradeoffs</li><li>Failure modes</li></ol>",
      "<h3>The standard layered architecture</h3>",
      flow([{ t: "Ingestion", s: "load·chunk·embed·index" }, { t: "Retrieval", s: "hybrid·rerank·ACL" }, { t: "Orchestration", s: "prompts·routing·agents", accent: true }, { t: "Safety", s: "guards·injection" }, { t: "Eval + Observability", s: "golden set·tracing" }], "lr"),
      callout("key", "Lead with tradeoffs and failure modes", "Juniors describe the happy path. Seniors describe what breaks and how they'd know. Spend a third of the interview on tradeoffs, failure modes, evaluation and observability — that's where level is decided."),
    ],
    diagram: diagram(
      "flowchart TD\n  Clarify --> Reqs[Functional + Non-functional]\n  Reqs --> Arch[Layered architecture]\n  Arch --> Ingest --> Retrieve --> Orchestrate --> Safety --> Eval --> Obs\n  Obs --> Tradeoffs --> Failures",
      flow([{ t: "Clarify" }, { t: "Requirements" }, { t: "Layered architecture", accent: true }, { t: "Tradeoffs + failure modes" }], "lr"),
      "Same skeleton for every GenAI design question; the body changes per domain."
    ),
    tradeoffs: { headers: ["Decision", "Lever A", "Lever B", "Driven by"], rows: [
      ["Knowledge", "RAG", "Fine-tune", "Change rate, auditability"],
      ["Model", "Large (quality)", "Small/routed (cost/latency)", "Quality bar vs budget"],
      ["Freshness", "Re-index often", "Cache aggressively", "Staleness tolerance"],
      ["Autonomy", "Workflow", "Agent", "How dynamic the path is"],
    ] },
    questions: [
      { q: "Design a GenAI assistant over our internal documents. Walk me through it.", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "high", region: "Both",
        model: "Clarify users/data/scale/quality bar. Then: ingestion (chunk, embed, index, refresh, ACLs), hybrid retrieval + rerank with access control, grounded generation with citations and abstain, output guardrails, evaluation (golden set + online sampling), observability (tracing, cost/latency/quality). Close with tradeoffs and failure modes.",
        senior: "I'd front-load the non-functional bar (latency, cost/query, groundedness target, compliance) because it drives every choice, enforce access control at retrieval, and spend real time on evaluation and failure modes (bad retrieval, hallucination, injection, provider outage). I keep the LLM as one component and make the system observable and updatable.",
        followups: ["How do you keep the index fresh?", "How do you handle 100 docs vs 100M?", "How do you measure if it's good?"],
        redflag: "'User asks the LLM and it answers' — no retrieval quality, safety or evaluation.",
        task: "Write the full design doc — see <a href='../systemdesign/enterprise-knowledge-assistant.html'>Enterprise Knowledge Assistant</a>." },
      { q: "How is GenAI system design different from classic system design?", difficulty: "Intermediate", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "It adds probabilistic model behavior, retrieval quality, hallucination risk, prompt/version management, token cost and model latency, safety/injection, and evaluation/feedback loops — on top of the usual APIs, storage, caching and scaling.",
        senior: "The mental shift is that correctness is statistical, so evaluation and observability become first-class design components, not afterthoughts, and cost/latency are dominated by model calls. I design the feedback loop (production failures → eval set → improvement) into the architecture from the start.",
        followups: ["Where does evaluation live in the architecture?"],
        redflag: "Treating it as identical to a CRUD service design." },
      { q: "Design a customer support RAG bot.", difficulty: "Senior", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Customer-facing RAG over a help center. Run the framework, and emphasize the differences from an internal assistant: external users (higher abuse/injection risk), brand risk from wrong answers, and a clean handoff to human agents.", "Key pieces: hybrid retrieval over KB, a coverage gate that escalates when unsure, citations, strong input guards, ticket creation with full context, and a feedback loop into evaluation."],
        senior: "A senior optimizes for trust over raw deflection (a confident wrong answer is worse than an escalation) and measures escalation precision and CSAT, not just how many tickets were auto-handled.",
        followups: ["Answer vs escalate — how do you decide?", "How do you prevent cross-customer leaks?"],
        redflag: "Maximizing auto-answer rate with no abstain/escalation path.", task: "<a href='../systemdesign/customer-support-bot.html'>Customer Support Bot design</a>." },
      { q: "Design a legal/finance document Q&A system.", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "medium", region: "Both",
        model: ["High-stakes RAG where a confident wrong answer creates liability. Priorities flip toward precision and auditability:", "<ul><li>Mandatory clause-level citations; never answer without a source.</li><li>Conservative abstain — guess nothing.</li><li>Version-aware ingestion (cite the exact document version).</li><li>A faithfulness gate that blocks unsupported claims, plus a full audit trail.</li></ul>"],
        senior: "A senior accepts higher cost, latency, and abstain rate as the correct trade in a regulated domain, adds a verification pass and human-in-the-loop before any action, and references compliance/retention requirements.",
        followups: ["Would you ever answer without a citation?", "How do you make it reproducible?"],
        redflag: "Optimizing for coverage/UX where wrong answers carry legal risk.", task: "<a href='../systemdesign/document-qa-regulated.html'>Document Q&A (regulated) design</a>." },
      { q: "Design a production RAG platform used by many teams.", difficulty: "Senior", round: "system design", seniority: "Staff", frequency: "emerging", region: "Both",
        model: ["A shared platform, not one app: shared ingestion, retrieval and a model gateway (routing, caching, fallback, rate limits), a shared eval harness and guardrail layer every app inherits, per-tenant isolation, cost attribution, and central observability."],
        senior: "A senior makes standards executable — an app can't onboard without passing baseline eval and inheriting guardrails — designs for noisy-neighbor and cost attribution from day one, and keeps the gateway highly available.",
        followups: ["How do you attribute cost per tenant?", "How do you isolate a shared cache?"],
        redflag: "One shared index for all tenants with no isolation story.", task: "<a href='../systemdesign/production-rag-platform.html'>Production RAG Platform design</a>." },
    ],
    hands: ["Take any product idea and produce a 1-page design doc following the 11-step framework. Then pick one of the seven worked designs and write it fully."],
    portfolio: ["Two design docs in your portfolio (e.g. <a href='../systemdesign/enterprise-knowledge-assistant.html'>knowledge assistant</a> + <a href='../systemdesign/production-rag-platform.html'>RAG platform</a>) are excellent senior signals and great mock-interview fodder."],
    mistakes: ["Describing only the happy path", "Forgetting evaluation/observability as design components", "No access control / safety layer", "Not clarifying requirements before drawing boxes"],
    checklist: ["I can run the 11-step framework from memory", "I lead with tradeoffs + failure modes", "I treat eval & observability as architecture", "I time-box a 10-minute explanation"],
    sources: ["Section 11 (system design patterns), section 19 (staff-level tradeoffs)."],
  },
});

export default CORE3;
