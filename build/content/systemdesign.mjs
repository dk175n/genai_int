import { code, table, flow, diagram, callout } from "../render.mjs";

export const SYSDESIGN = [];
const SD = (o) => SYSDESIGN.push({ kind: "sysdesign", group: "sysdesign", ...o });

const arch = (nodes) => diagram(null, flow(nodes, "lr"));
const stdInterruptions = {
  headers: ["Interviewer says…", "How to answer"],
  rows: [
    ["“What if retrieval returns nothing relevant?”", "Coverage gate → abstain or escalate; never force an answer. Measure abstain-correctness."],
    ["“This will be too expensive.”", "Cache + route to a smaller model for easy traffic; show cost/req and that quality held on eval."],
    ["“How do you know it's any good?”", "Golden set offline as a deploy gate + sampled online judging; split retrieval vs generation metrics."],
    ["“What about data the user shouldn't see?”", "Access control enforced at retrieval, not the prompt; per-tenant filters or separate indexes."],
    ["“What happens when the model provider goes down?”", "Timeouts, fallback model/provider, graceful degradation to cached/smaller answers."],
  ],
};

// 1 ───────── ENTERPRISE KNOWLEDGE ASSISTANT (rich) ─────────
SD({
  num: "01", title: "Enterprise Knowledge Assistant", path: "systemdesign/enterprise-knowledge-assistant.html",
  kw: "enterprise knowledge assistant rag internal docs access control system design confluence sharepoint",
  data: {
    eyebrow: "System Design",
    title: "Design · Enterprise Knowledge Assistant",
    lead: "“Build an assistant that answers employee questions over our internal knowledge (wiki, docs, tickets), with citations and respecting permissions.” The canonical enterprise RAG design — run it with the <a href='../pages/genai-system-design.html'>framework</a>.",
    pills: [{ t: "Most common design Q", cls: "red" }, { t: "Senior/Staff", cls: "purple" }],
    intro: ["A read-mostly RAG system over heterogeneous internal sources. The defining constraints are <strong>access control</strong> (employees must only see what they're entitled to), <strong>freshness</strong> (sources change daily), and <strong>trust</strong> (citations + abstain)."],
    clarify: ["Which sources, and how do their permissions work (per-doc ACLs? groups)?", "How fresh must answers be — minutes or daily?", "Expected scale: #docs, #users, QPS?", "Quality bar and consequence of a wrong answer?", "On-prem / data-residency / compliance constraints?", "Do we need conversational follow-ups or single-shot Q&A?"],
    functional: ["Answer NL questions over internal sources with citations", "Respect per-user permissions", "Abstain/escalate when unsure", "Conversational follow-ups", "Capture feedback (thumbs + reason)", "Admin view of unanswered/escalated questions (KB gaps)"],
    nonfunctional: ["p95 latency target (e.g. < 4s)", "Cost/query budget", "Groundedness/faithfulness target (e.g. ≥ 0.9 on golden set)", "No cross-permission data leakage (hard requirement)", "Index freshness SLA", "Auditability of every answer"],
    architecture: arch([{ t: "Connectors", s: "wiki·docs·tickets" }, { t: "Ingestion", s: "chunk·embed·ACL metadata" }, { t: "Retrieval", s: "hybrid·rerank·ACL filter", accent: true }, { t: "Orchestration", s: "ground·cite·abstain" }, { t: "Safety + Eval + Obs" }]),
    ingestion: ["Connectors per source (with incremental sync). Parse → structure-aware chunk → embed → store with rich metadata: source, URL, last-modified, and <strong>ACL fields</strong> (owner group / visibility) carried from the source system. Incremental re-index on change events or a schedule to meet the freshness SLA; tombstone deleted docs so they leave the index."],
    retrieval: ["Hybrid (dense + BM25) + cross-encoder rerank. <strong>Access control is applied as a metadata filter on the query</strong>, tied to the requesting user's groups — restricted docs are never candidates. For hard boundaries, separate indexes per security domain. Return top-k with source metadata for citations."],
    orchestration: ["Grounded prompt: context-only answers, cite sources, abstain if coverage is weak. Single capable model; optionally route trivial FAQ to a cheaper model. Conversation memory bounded (summarize old turns). No tools needed for v1 (read-only Q&A)."],
    safety: ["Input: scope/PII checks, injection detection. Retrieval: ACL enforcement (the key control). Output: groundedness check, PII/secret filter, citation presence. Treat retrieved content as untrusted (indirect injection from a malicious doc)."],
    evaluation: ["Golden set per source/domain (question → answer + allowed sources). Offline gate on faithfulness + recall@k + abstain-correctness; CI on every prompt/retrieval change. Online: sample traffic for faithfulness judging; track thumbs-down and escalations as new eval cases and KB gaps."],
    observability: ["Trace each query (retrieved chunks + ACL filter applied + tokens + cost + latency). Dashboards: cost/query, p95 latency, faithfulness, abstain/escalation rate, freshness lag. Alerts on quality regressions and any ACL-filter anomaly."],
    costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [
      ["Cache frequent Q&A", "↓ cost & latency", "Staleness — invalidate on re-index"],
      ["Route FAQ to small model", "↓ cost", "Misroute risk — eval-gated"],
      ["Rerank only top-50", "↓ latency", "Slight recall trade"],
      ["Bigger context", "↑ recall", "↑ cost/latency, 'lost in middle'"],
    ] },
    failures: ["ACL filter bug → cross-permission leak (highest severity; test relentlessly)", "Stale index → wrong/outdated answers (freshness SLA + tombstoning)", "Bad retrieval → hallucination (hybrid+rerank, abstain gate)", "Indirect injection from a poisoned doc → untrusted content, output guard", "Provider outage → fallback model + cached answers"],
    tenmin: ["Minute 1–2: clarify sources, permissions, freshness, scale, quality bar. Minute 3–5: draw the five layers; emphasize ACL-at-retrieval and citations. Minute 6–8: evaluation + observability as first-class. Minute 9–10: tradeoffs (cost/latency, freshness vs cache) and the top failure modes with mitigations. Keep returning to the permission requirement — it's what makes this 'enterprise'."],
    interruptions: stdInterruptions,
    questions: [
      { q: "How do you guarantee an employee never sees a document they're not allowed to?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "high", region: "Both",
        model: "Enforce access control at retrieval via metadata filters tied to the user's groups (and/or separate indexes per security domain), so restricted docs are never retrieved. Add an output secret/PII backstop. Never rely on the prompt.",
        senior: "I carry ACLs from the source system into chunk metadata at ingestion, filter on them in the vector query, and test it as a hard invariant (red-team queries that should return nothing). For strict isolation I use per-domain indexes. The model is the last line I'd trust for permissions.",
        followups: ["Per-doc ACLs vs separate indexes — when each?", "How do ACL changes propagate to the index?"],
        redflag: "Telling the model not to reveal restricted info." },
      { q: "How do you keep answers fresh as the wiki changes constantly?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "Incremental sync on change events (or scheduled), re-embed changed docs, tombstone deletes, and invalidate caches on re-index. Define a freshness SLA and monitor index lag.",
        senior: "I treat ingestion as a pipeline with its own monitoring (lag, failures) because stale answers erode trust silently. Cache TTLs are tied to the freshness SLA, and I surface 'last updated' in citations so users can judge recency.",
        followups: ["How do you handle a deleted/again-restricted doc still in cache?"],
        redflag: "Full nightly re-index of everything with no incremental path at scale." },
    ],
  },
});

// concise factory for the other six
function quickSD(o) {
  SD({ num: o.num, title: o.title, path: o.path, kw: o.kw, data: {
    eyebrow: "System Design", title: "Design · " + o.title, lead: o.lead, pills: o.pills || [{ t: "Design round", cls: "blue" }],
    intro: o.intro, clarify: o.clarify, functional: o.functional, nonfunctional: o.nonfunctional,
    architecture: o.flow ? arch(o.flow) : undefined,
    ingestion: o.ingestion, retrieval: o.retrieval, orchestration: o.orchestration, safety: o.safety,
    evaluation: o.evaluation, observability: o.observability, costlatency: o.costlatency, failures: o.failures,
    tenmin: o.tenmin, interruptions: o.interruptions || stdInterruptions, questions: o.questions,
  } });
}

quickSD({
  num: "02", title: "Customer Support Bot", path: "systemdesign/customer-support-bot.html",
  kw: "customer support bot rag escalation deflection multichannel system design",
  lead: "A customer-facing support assistant over a help center: answer with citations, escalate when unsure, integrate with ticketing. Trust and deflection are the design axes.",
  intro: ["Customer-facing RAG. Differs from the internal assistant in that users are external (higher injection/abuse risk), wrong answers hurt the brand, and it must hand off to human agents cleanly."],
  clarify: ["Channels (web chat, email, in-app)?", "Tone/brand constraints?", "Ticketing system to integrate with?", "What's the cost of a wrong answer vs an escalation?", "Multilingual?"],
  functional: ["Answer from KB with citations", "Escalate to human + create ticket with context", "Multichannel", "Capture CSAT/feedback", "Surface KB gaps"],
  nonfunctional: ["Low latency (live chat)", "High groundedness (brand risk)", "Abuse/injection resistance", "Cost/conversation budget"],
  flow: [{ t: "User (web/app)" }, { t: "Input guard", s: "abuse·injection" }, { t: "Hybrid retrieve KB", accent: true }, { t: "Coverage gate → answer/escalate" }, { t: "Ticket + feedback" }],
  ingestion: ["Help-center articles + past resolved tickets → chunk → embed → index with product/category metadata and 'last reviewed' date."],
  retrieval: ["Hybrid + rerank, scoped to the product/locale. Coverage gate decides answer vs escalate."],
  orchestration: ["Grounded, on-brand prompt with citations and abstain. Bias toward escalation when confidence is low. Bounded conversation memory."],
  safety: ["Stronger input guards (external users): abuse, jailbreak, injection. Output: groundedness, no internal-only info, policy/brand checks."],
  evaluation: ["Golden set + deflection metric (correct auto-answer vs correct escalation) + faithfulness; A/B on escalation threshold."],
  observability: ["Trace + CSAT, deflection rate, escalation precision, cost/conversation; alert on faithfulness drops."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Cache common FAQs", "↓ cost/latency", "Staleness"], ["Small model for FAQ", "↓ cost", "Eval-gated"], ["Stream answers", "↓ perceived latency", "Mid-stream guard harder"]] },
  failures: ["Confident wrong answer → coverage gate, bias to escalate", "External injection/abuse → input guards", "Bad handoff loses context → pass transcript+retrieval to ticket", "KB stale → review dates + re-index"],
  tenmin: ["Clarify channels + cost-of-wrong-answer; draw RAG + coverage gate + escalation; stress external-user safety; cover deflection/CSAT eval and the human handoff; close with tradeoffs."],
  questions: [{ q: "How do you balance deflection (auto-answering) against giving wrong answers?", difficulty: "Senior", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
    model: "Set a coverage/faithfulness gate and bias toward escalation; measure escalation precision and CSAT alongside deflection so you don't optimize deflection at the cost of trust.",
    senior: "I treat a confident wrong answer as more expensive than an escalation and tune thresholds accordingly, A/B testing the threshold on real traffic. Every escalation feeds the KB-gap and eval loop.",
    followups: ["How do you hand off to a human without losing context?"], redflag: "Maximizing deflection regardless of correctness." },
    { q: "How do you defend a customer-facing bot against abuse and prompt injection?", difficulty: "Senior", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
      model: ["External users mean stronger input guards: rate limiting, abuse/jailbreak detection, and injection checks. Treat the KB content as untrusted too (indirect injection). Output guard blocks internal-only info, PII and off-brand content before replying."],
      senior: "A senior assumes hostile input by default, keeps the bot scoped to a narrow domain (out-of-scope → escalate), and limits blast radius since it's internet-facing.",
      followups: ["How do you stop it answering off-topic questions?"], redflag: "Treating an external bot with the same trust as an internal tool." },
    { q: "What's your evaluation and rollout plan for this bot?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["Golden set + deflection and escalation-precision metrics offline; shadow/canary on real traffic; A/B the escalation threshold; monitor faithfulness and CSAT. Roll out gradually, gated on quality."],
      senior: "A senior ships behind a canary with a kill-switch back to human-only, and treats every escalation/thumbs-down as a new eval case and KB-gap.",
      followups: ["How do you A/B the escalation threshold safely?"], redflag: "Big-bang launch with no canary or rollback." }],
});

quickSD({
  num: "03", title: "Document Q&A for Legal / Finance / HR", path: "systemdesign/document-qa-regulated.html",
  kw: "legal finance hr document qa regulated compliance audit citations high stakes system design",
  lead: "RAG for high-stakes, regulated domains: precise citations, strict abstain, auditability, and zero tolerance for confident fabrication. Accuracy and traceability dominate.",
  pills: [{ t: "High-stakes", cls: "amber" }],
  intro: ["Same RAG skeleton, but the cost of a wrong answer is legal/financial. Design priorities flip to precision, mandatory citations, conservative abstain, and full auditability — speed and cost are secondary."],
  clarify: ["What's the consequence of an incorrect answer (legal liability)?", "Is human verification required before action?", "Retention/audit and data-residency requirements?", "How precise must citations be (clause/line level)?"],
  functional: ["Answer only with precise, clause-level citations", "Abstain readily; never guess", "Full audit log of question, context, answer", "Versioned documents (which version was cited)"],
  nonfunctional: ["Very high faithfulness; near-zero unsupported claims", "Auditability/retention compliance", "Data residency / on-prem option", "Reproducibility (same Q → same cited answer)"],
  flow: [{ t: "Question" }, { t: "Retrieve (precise chunks)", accent: true }, { t: "Strict grounded answer + clause citation" }, { t: "Faithfulness gate" }, { t: "Audit log" }],
  ingestion: ["Version-aware ingestion (cite the exact document version); fine-grained chunks for clause-level citation; rich metadata (jurisdiction, effective date)."],
  retrieval: ["Hybrid with sparse weighted up (exact legal/financial terms matter); rerank; return precise spans for citation."],
  orchestration: ["Very strict grounding: answer only from context, quote + cite the clause, abstain otherwise. Low temperature for reproducibility."],
  safety: ["Mandatory citation presence; faithfulness gate blocks unsupported claims; PII/confidentiality controls; access control by matter/client."],
  evaluation: ["Expert-labeled golden set; faithfulness + citation-correctness as hard gates; track unsupported-claim rate (target ~0)."],
  observability: ["Full immutable audit trail (question, retrieved version, answer, citations); monitor abstain rate and citation accuracy."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Larger model", "↑ accuracy", "↑ cost — justified here"], ["More verification passes", "↑ faithfulness", "↑ latency — acceptable"]] },
  failures: ["Confident fabrication → strict grounding + faithfulness gate + abstain", "Citing wrong version → version-aware ingestion", "Missing exact term → weight sparse retrieval", "Audit gap → immutable logging"],
  tenmin: ["Open by naming the stakes; emphasize precise citations, strict abstain, version-awareness and audit; treat faithfulness as a hard gate; accept higher cost/latency for accuracy; close with failure modes centered on fabrication."],
  questions: [{ q: "How do you design RAG where a confident wrong answer is unacceptable?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "medium", region: "Both",
    model: "Mandatory clause-level citations, a faithfulness gate that blocks unsupported claims, a conservative abstain policy, version-aware citations, and a full audit trail. Bias every tradeoff toward precision over coverage.",
    senior: "I'd add a verification pass (a second model checks each claim against its cited span) and human-in-the-loop before any action. I accept higher cost and latency and a higher abstain rate as the correct trade in a regulated domain.",
    followups: ["Would you ever let it answer without a citation?", "How do you make it reproducible?"], redflag: "Optimizing for coverage/UX in a domain where wrong answers create liability." },
    { q: "How do you handle document versions and effective dates in a legal/finance Q&A?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "medium", region: "Both",
      model: ["Version-aware ingestion: keep each document version with effective/expiry dates in metadata, cite the exact version used, and filter retrieval to the version in force for the query's context (e.g. 'as of' date)."],
      senior: "A senior treats 'which version applied when' as a first-class requirement — citing the wrong version is as bad as a wrong answer in regulated domains — and keeps superseded versions queryable for audit.",
      followups: ["How do you answer 'what was the policy last year'?"], redflag: "Indexing only the latest version and losing historical accuracy." },
    { q: "Why add a separate verification pass here, and how does it work?", difficulty: "Advanced", round: "system design", seniority: "Senior/Staff", frequency: "emerging", region: "Both",
      model: ["A second model checks each claim in the answer against its cited source span and blocks/flags unsupported claims. In high-stakes domains the extra cost/latency is worth near-zero fabrication."],
      senior: "A senior accepts a higher abstain rate and latency as the correct trade, and routes anything that fails verification to human review rather than to the user.",
      followups: ["What's the latency/cost impact and is it acceptable?"], redflag: "Skipping verification to keep latency low in a liability-heavy domain." }],
});

quickSD({
  num: "04", title: "Code Assistant Agent", path: "systemdesign/code-assistant-agent.html",
  kw: "code assistant agent repo rag tools tests sandbox system design copilot",
  lead: "An agent that answers questions and makes changes across a codebase: repo-aware retrieval, tools (read/search/edit/run tests) in a sandbox, with guardrails on execution.",
  pills: [{ t: "Agent + RAG", cls: "purple" }],
  intro: ["RAG over code + an action-taking agent. Retrieval must be code-aware (symbols, files), and execution (running tests, editing) must be sandboxed and bounded."],
  clarify: ["Read-only Q&A or does it edit code?", "Can it run code/tests? In what sandbox?", "Repo size and languages?", "How are changes reviewed (PR + human)?"],
  functional: ["Answer questions about the repo with file/line citations", "Propose edits as diffs", "Run tests in a sandbox", "Open a PR for human review"],
  nonfunctional: ["No unsandboxed execution", "Bounded cost/steps per task", "Latency tolerable for dev workflow", "No secret leakage from repo"],
  flow: [{ t: "Task" }, { t: "Code-aware retrieve", s: "symbols·files", accent: true }, { t: "Plan + edit (diff)" }, { t: "Run tests (sandbox)" }, { t: "PR for review" }],
  ingestion: ["Index repo by file + symbol; chunk along function/class boundaries; keep path metadata; re-index on commit."],
  retrieval: ["Hybrid + symbol search; expand to surrounding file context (parent-doc style)."],
  orchestration: ["Planner-executor or graph: locate → plan edit → apply diff → run tests → iterate (bounded). Tools: search, read, edit, run_tests."],
  safety: ["Execution only in an ephemeral sandbox; no network/secrets; bounded steps & cost; all changes via PR (human merges); treat repo content as untrusted (injection in comments)."],
  evaluation: ["Task success = tests pass / correct diff on a benchmark of tasks; track steps and cost; regression on a fixed task set."],
  observability: ["Trace plan + tool calls + diffs; monitor task success rate, sandbox failures, cost/task."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Scope retrieval to relevant files", "↓ cost/latency", "Risk missing context"], ["Cap iterations", "↓ cost", "May not finish hard tasks"]] },
  failures: ["Unsafe execution → sandbox + no secrets", "Endless edit-test loops → step budget", "Bad edits merged → PR + human review", "Injection via code comments → untrusted content"],
  tenmin: ["Clarify edit-vs-readonly and execution; draw code-aware retrieval + planner-executor + sandbox + PR gate; stress sandboxing and bounded loops; cover task-success eval; close with failure modes."],
  questions: [{ q: "How do you safely let an agent run and modify code?", difficulty: "Senior", round: "system design", seniority: "Senior", frequency: "emerging", region: "Both",
    model: "Run only in an ephemeral, network/secret-free sandbox; bound steps and cost; surface changes as diffs and require a human-reviewed PR to merge; treat repo content as untrusted.",
    senior: "Safety comes from the environment, not the model: the sandbox can't reach secrets or prod, the agent can't merge, and every action is traced. I'd benchmark task success on a fixed set so improvements are measured.",
    followups: ["How do you bound cost on hard tasks?", "How is repo content an injection vector?"], redflag: "Letting the agent execute with prod credentials or auto-merge." },
    { q: "How do you do code-aware retrieval over a large repo?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "emerging", region: "Both",
      model: ["Index by file and symbol (functions/classes), chunk along code boundaries rather than fixed sizes, keep path metadata, and re-index on commit. Combine semantic search with symbol/keyword lookup, then expand to surrounding file context."],
      senior: "A senior scopes retrieval to the relevant files/modules to keep context small and cheap, and notes that code has strong structure (imports, call graphs) you can exploit beyond plain text chunking.",
      followups: ["How do you keep the index fresh on every commit?"], redflag: "Treating code as plain prose and fixed-size chunking it." },
    { q: "How would you evaluate a code assistant agent?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["Benchmark of tasks with objective checks: do the generated tests pass / does the diff produce correct behavior? Track task success rate, steps and cost per task, and sandbox failure rate."],
      senior: "A senior uses tests-pass as the ground-truth signal (objective, unlike prose), and runs the benchmark as CI so model/prompt changes don't regress task success.",
      followups: ["What's your ground-truth signal?"], redflag: "Judging by whether the code 'looks reasonable'." }],
});

quickSD({
  num: "05", title: "SQL Data Agent", path: "systemdesign/sql-data-agent.html",
  kw: "sql data agent analytics text to sql semantic layer governance read only system design",
  lead: "A natural-language analytics agent over a data warehouse: schema-aware text-to-SQL with a semantic layer, read-only governance, and result validation. Scales the SQL agent project to an enterprise design.",
  pills: [{ t: "Agent + governance", cls: "amber" }],
  intro: ["Text-to-SQL at organizational scale. The hard parts are correctness on a big/ambiguous schema (semantic layer helps), governance (read-only, row-level security), and trust in results."],
  clarify: ["Warehouse (Snowflake/BigQuery/Postgres)?", "Is there a semantic layer / metric definitions?", "Row-level security needs?", "Who consumes results — analysts or end users?"],
  functional: ["NL → governed SQL with results + the SQL shown", "Use a semantic layer / approved metrics", "Charts/summaries", "Save/share queries"],
  nonfunctional: ["Read-only + RLS enforced", "Query cost/row limits", "Correctness on the schema", "Auditability"],
  flow: [{ t: "Question" }, { t: "Schema + semantic layer", accent: true }, { t: "Generate governed SQL" }, { t: "Validate + RLS + limits" }, { t: "Execute + summarize" }],
  ingestion: ["Index schema, column descriptions, and metric/semantic definitions; embed example question→SQL pairs for few-shot."],
  retrieval: ["Retrieve relevant tables/columns/metrics for the question to ground SQL generation (RAG over the schema)."],
  orchestration: ["Generate SQL grounded in retrieved schema + semantic layer; self-check against schema; optionally repair on execution error (bounded)."],
  safety: ["Read-only role + row-level security; validate SELECT-only; enforce LIMIT/cost guards; parameterize; show SQL for transparency; audit every query."],
  evaluation: ["Execution accuracy vs golden question→result set; safety (no unsafe/over-cost query passes); track repair rate."],
  observability: ["Trace question→SQL→result; monitor accuracy, query cost, error/repair rate; alert on cost spikes."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Semantic layer / approved metrics", "↑ correctness", "Upfront modeling effort"], ["Cost/row limits", "↓ runaway cost", "Truncated results"]] },
  failures: ["Wrong joins/metrics → semantic layer + schema RAG + validation", "Runaway/expensive query → cost guards + LIMIT", "RLS bypass → enforce at DB role, not prompt", "Hallucinated columns → ground + validate"],
  tenmin: ["Clarify warehouse + semantic layer + governance; draw schema-RAG + governed generation + validation + RLS; stress read-only governance and correctness; cover accuracy eval; close with cost/failure modes."],
  questions: [{ q: "How do you get correct SQL on a large, ambiguous schema?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
    model: "RAG over the schema + column/metric descriptions to ground generation, a semantic layer of approved metrics, few-shot example pairs, and a self-check/repair loop against execution errors. Validate before running.",
    senior: "The semantic layer is the highest-leverage investment — it constrains the model to correct, governed definitions and removes most ambiguity. I measure execution accuracy on a golden set and treat governance (read-only, RLS) as non-negotiable.",
    followups: ["How do you enforce row-level security?", "How do you handle ambiguous metric definitions?"], redflag: "Free text-to-SQL with no schema grounding or governance." },
    { q: "How do you enforce governance (read-only, row-level security) on a data agent?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "medium", region: "Both",
      model: ["Enforce at the database, not the prompt: a read-only role, row-level security tied to the requesting user, query validation (SELECT-only), and cost/row limits. Show the generated SQL for transparency and audit-log every query."],
      senior: "A senior makes unsafe queries impossible by construction (least-privilege role) so governance holds regardless of what the model emits, and attributes query cost back to users.",
      followups: ["What if a user needs write access?"], redflag: "Relying on the prompt to keep the agent read-only." },
    { q: "How do you stop runaway-cost queries on a warehouse?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
      model: ["Enforce LIMITs, query timeouts, and scanned-bytes/cost caps; validate before executing; prefer a semantic layer with pre-approved, bounded queries. Monitor and alert on cost spikes."],
      senior: "A senior sets a per-query cost ceiling and a dry-run/estimate step, because a single bad generated query can scan terabytes on systems billed by data scanned.",
      followups: ["How do you estimate cost before running?"], redflag: "No limits, letting a generated query scan the whole warehouse." }],
});

quickSD({
  num: "06", title: "Multi-Agent Business Workflow", path: "systemdesign/multi-agent-workflow.html",
  kw: "multi agent business workflow orchestration supervisor handoff state human in the loop system design",
  lead: "Multiple specialized agents coordinating a business process (e.g. intake → research → draft → review) with a supervisor, shared state, and human checkpoints. When and how to actually use multi-agent.",
  pills: [{ t: "Advanced", cls: "purple" }],
  intro: ["A coordinated set of agents. The senior move is justifying multi-agent at all (vs one agent or a workflow) and designing the coordination, shared state and human checkpoints so it's reliable, not chaotic."],
  clarify: ["What are the distinct steps/roles?", "Are steps fixed (→ workflow) or dynamic (→ agents)?", "Where must a human approve?", "What's the SLA and cost ceiling?"],
  functional: ["Decompose a process into specialized agents", "Supervisor/orchestrator routes and aggregates", "Shared, checkpointed state", "Human approval at key steps", "Audit of decisions"],
  nonfunctional: ["Bounded total cost/steps", "Observability across agents", "Reliability (retry a step, not the whole run)", "Clear failure isolation"],
  flow: [{ t: "Request" }, { t: "Supervisor / planner", accent: true }, { t: "Specialist agents", s: "research·draft·check" }, { t: "Shared state + checkpoints" }, { t: "Human approval → output" }],
  ingestion: ["Per-agent knowledge/tools as needed (some agents are RAG-backed, some tool-backed)."],
  retrieval: ["Each specialist retrieves within its domain; shared state carries intermediate results, not raw context dumps."],
  orchestration: ["Stateful graph (LangGraph-style): supervisor node routes to specialists; explicit shared state; checkpointing for retry/resume; human-in-the-loop nodes for approval. Bound total steps/cost."],
  safety: ["Per-agent least privilege; guardrails at each boundary; human approval before external/irreversible actions; treat inter-agent messages as untrusted."],
  evaluation: ["End-to-end process success rate + per-agent quality; cost/steps per run; track where runs fail (which agent/step)."],
  observability: ["Trace the whole graph (which agent did what, state transitions); monitor success rate, step count, cost, human-intervention rate."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Fewer agents / collapse to workflow", "↓ cost & failure surface", "Less flexibility"], ["Parallel independent agents", "↓ latency", "Coordination complexity"]] },
  failures: ["Coordination chaos / loops → supervisor + bounded steps + checkpoints", "Error cascade → isolate + retry single step", "Cost blowup → global budget", "Over-engineering → justify vs single agent/workflow"],
  tenmin: ["First justify multi-agent vs a workflow; draw supervisor + specialists + shared state + human checkpoints; stress bounded cost, checkpointing and observability; cover end-to-end eval; close by noting where you'd simplify."],
  questions: [{ q: "When is multi-agent actually worth it over a single agent or a workflow?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "emerging", region: "Both",
    model: "When the process has genuinely distinct skills/roles and dynamic routing between them; otherwise a single agent or deterministic workflow is cheaper and more reliable. Multi-agent adds coordination cost and failure surface.",
    senior: "I default to the simplest thing: fixed steps → workflow; one dynamic skill → one agent; multiple distinct dynamic skills with routing → multi-agent with a supervisor, shared state and checkpoints. I'd explicitly say where I'd collapse agents to reduce failure surface — that restraint is the signal.",
    followups: ["How do you stop agents looping between each other?", "How do you checkpoint shared state?"], redflag: "Reaching for multi-agent because it's impressive, with no justification." },
    { q: "How does the supervisor coordinate agents and manage shared state?", difficulty: "Advanced", round: "system design", seniority: "Senior/Staff", frequency: "emerging", region: "Both",
      model: ["A supervisor (orchestrator) routes work to specialist agents, aggregates results, and decides next steps. Shared state is explicit and checkpointed (a graph), passing intermediate results — not raw context dumps — between agents."],
      senior: "A senior bounds total steps/cost globally, checkpoints so a single agent can be retried without re-running everything, and traces across agents so failures are attributable.",
      followups: ["How do you bound total cost across all agents?"], redflag: "Agents passing full conversation history to each other with no shared-state design." },
    { q: "Where do you put human-in-the-loop in a multi-agent business workflow?", difficulty: "Senior", round: "system design", seniority: "Senior/Staff", frequency: "medium", region: "Both",
      model: ["At irreversible or high-impact steps: before external sends, financial actions, or publishing. The graph pauses at a human-approval node, then resumes from the checkpoint."],
      senior: "A senior maps approval points to business risk (not every step), and designs the resume-from-checkpoint flow so waiting for a human doesn't lose state or block unrelated work.",
      followups: ["How do you resume cleanly after approval?"], redflag: "Full autonomy on irreversible business actions with no approval gate." }],
});

quickSD({
  num: "07", title: "Production RAG Platform", path: "systemdesign/production-rag-platform.html",
  kw: "production rag platform multi tenant shared infrastructure gateway eval harness observability system design",
  lead: "Not one app — the shared platform many RAG apps run on: ingestion service, retrieval service, model gateway, eval harness, and observability, multi-tenant. The platform-engineer design.",
  pills: [{ t: "Platform/Staff", cls: "blue" }],
  intro: ["A reusable internal platform so product teams ship RAG/agent features without rebuilding ingestion, retrieval, gateways, eval and observability each time. Multi-tenancy, standardization and self-serve are the themes."],
  clarify: ["How many teams/use-cases will onboard?", "Shared vs isolated data per tenant?", "Self-serve or platform-team-managed onboarding?", "What standards must every app meet (eval, guardrails)?"],
  functional: ["Ingestion-as-a-service (connectors, chunk, embed, index)", "Retrieval API (hybrid + rerank)", "Model gateway (routing, caching, fallback, rate limits)", "Shared eval harness + golden-set tooling", "Central observability"],
  nonfunctional: ["Multi-tenant isolation", "Self-serve onboarding", "Cost attribution per tenant", "Reliability/SLOs", "Enforced guardrail/eval standards"],
  flow: [{ t: "Tenant apps" }, { t: "Model gateway", s: "route·cache·fallback·limit", accent: true }, { t: "Retrieval svc", s: "hybrid·rerank·ACL" }, { t: "Ingestion svc" }, { t: "Eval + Observability" }],
  ingestion: ["Ingestion service with pluggable connectors + standardized chunk/embed/index; per-tenant namespaces; incremental sync; tracks lineage."],
  retrieval: ["Retrieval API: hybrid + rerank with tenant-scoped indexes/filters; tunable per app; consistent metrics."],
  orchestration: ["Model gateway: routing (small/large), caching (exact + semantic), fallback providers, rate limiting, prompt registry/versioning, cost attribution."],
  safety: ["Centralized guardrail middleware (injection, PII, policy) every app inherits; tenant isolation enforced at retrieval and gateway."],
  evaluation: ["Shared eval harness: golden-set management, LLM-judge library, CI integration; every onboarded app must pass baseline eval + guardrail standards."],
  observability: ["Central tracing + dashboards per tenant: cost, latency, quality, drift; cost attribution and quota alerts."],
  costlatency: { headers: ["Lever", "Effect", "Tradeoff"], rows: [["Shared cache at gateway", "↓ cost across tenants", "Cross-tenant cache isolation needed"], ["Central small-model routing", "↓ cost", "Per-app quality tuning harder"], ["Shared infra", "↓ cost & duplication", "Noisy-neighbor / isolation work"]] },
  failures: ["Noisy neighbor → per-tenant quotas/rate limits", "Cross-tenant leak → strict namespace isolation + cache keys", "Gateway is SPOF → HA + fallback", "Teams bypass standards → enforce via the platform, not docs"],
  tenmin: ["Frame as platform not app; draw gateway + retrieval svc + ingestion svc + eval + observability, multi-tenant; stress isolation, self-serve, cost attribution and enforced standards; cover platform-level SLOs; close with multi-tenant failure modes."],
  questions: [{ q: "How do you design a multi-tenant RAG platform that enforces quality and isolation?", difficulty: "Senior", round: "system design", seniority: "Staff", frequency: "emerging", region: "Both",
    model: "Shared ingestion/retrieval/gateway services with per-tenant namespaces and quotas; a model gateway for routing/caching/fallback/cost-attribution; a shared eval harness and guardrail middleware that every onboarded app must pass; central observability. Isolation enforced at retrieval and cache keys.",
    senior: "I'd make standards executable: an app can't onboard without passing baseline eval and inheriting guardrails — enforcement lives in the platform, not a wiki. I design for cost attribution and noisy-neighbor control from day one, and keep the gateway HA since it's the critical path.",
    followups: ["How do you attribute cost per tenant?", "How do you prevent a shared cache leaking across tenants?"], redflag: "A single shared index for all tenants with no isolation story." },
    { q: "What does the model gateway do, and why centralize it?", difficulty: "Senior", round: "system design", seniority: "Staff", frequency: "emerging", region: "Both",
      model: ["A single entry point for all model calls that handles routing (small vs large), caching, provider fallback, rate limiting, prompt-version registry, and cost attribution per tenant/feature. Centralizing means every app inherits these without rebuilding them."],
      senior: "A senior makes the gateway highly available (it's the critical path), and uses it to enforce org-wide policy — no app talks to a provider directly, so guardrails, caching and cost controls are guaranteed.",
      followups: ["Isn't the gateway a single point of failure?"], redflag: "Each team calling providers directly with duplicated, inconsistent logic." },
    { q: "How do you enforce quality and guardrail standards across many tenant apps?", difficulty: "Senior", round: "system design", seniority: "Staff", frequency: "emerging", region: "Both",
      model: ["Make standards executable, not documentation: a shared eval harness and guardrail middleware that every app must pass/inherit to onboard. An app can't go live without meeting a baseline faithfulness and passing adversarial/injection checks."],
      senior: "A senior puts enforcement in the platform (CI gate + inherited middleware) rather than a wiki, so standards can't be skipped, and tracks per-tenant quality centrally.",
      followups: ["What's your onboarding gate?"], redflag: "Relying on teams to 'follow the guidelines' voluntarily." }],
});

export default SYSDESIGN;
