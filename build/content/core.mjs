import { code, table, flow, diagram, callout } from "../render.mjs";
import { SRC } from "./sources.mjs";

export const CORE = [];

// ───────────────────── 04 · CORE GENAI FOUNDATIONS ─────────────────────
CORE.push({
  kind: "learn", group: "core", num: "04", title: "Core GenAI Foundations", path: "pages/core-foundations.html",
  kw: "llm tokens context window temperature decoding sampling next token prediction hallucination foundations transformer attention",
  data: {
    eyebrow: "Core Track",
    title: "Core GenAI Foundations",
    lead: "The minimum mental model an interviewer expects you to have absolutely solid before anything else: what an LLM is, tokens, the context window, decoding parameters, and why hallucination is structural — not a bug.",
    audience: "Engineers who use LLM APIs but want crisp, correct first-principles answers.",
    goal: "Answer 'what is an LLM, really?' and 'why does it hallucinate?' without hand-waving.",
    learn: ["Next-token prediction & why models are stateless", "Tokens, context window, and their cost/latency impact", "Temperature, top-p, and when to change them", "Why hallucination is intrinsic and how RAG/grounding mitigate it"],
    pills: [{ t: "Beginner→Intermediate" }, { t: "Screening staple", cls: "blue" }],
    whyInterviews: ["Screening rounds almost always open here. Getting tokens, context windows and decoding right signals you understand the substrate; getting hallucination right sets up every RAG/eval/guardrails answer later."],
    answer60: "“An LLM is a next-token predictor: given a sequence of tokens it outputs a probability distribution over the next token, sampled repeatedly. It's <strong>stateless</strong> — the only memory is what's in the context window this call. Temperature controls randomness of sampling. Hallucination is intrinsic because the model optimizes for plausible continuations, not truth — which is exactly why we ground it with retrieval.”",
    answer3: [
      "Start with the mechanism: a transformer takes tokenized text, and through self-attention produces a probability distribution over the vocabulary for the next token. We sample one, append it, repeat. That's it — there's no database lookup, no reasoning engine, just learned statistical structure.",
      "Three consequences interviewers care about: (1) <strong>statelessness</strong> — each API call only knows the tokens you send, which is why chat history and RAG context must be re-sent every time; (2) the <strong>context window</strong> bounds how much it can attend to, and tokens cost money and latency; (3) <strong>hallucination</strong> is structural — when the most plausible continuation isn't the true one, the model still produces it confidently. Grounding (RAG), constrained decoding, and evaluation are how we manage that.",
    ],
    concept: [
      "<h3>Next-token prediction</h3><p>The model is a function from a token sequence to a distribution over the next token. Decoding repeats this. Everything else — chat, RAG, agents — is scaffolding around this single primitive.</p>",
      code("python", "# A chat call is just: tokens in -> next tokens out, one shot.\nfrom anthropic import Anthropic\nclient = Anthropic()\nresp = client.messages.create(\n    model=\"claude-opus-4-8\",\n    max_tokens=512,\n    messages=[{\"role\": \"user\", \"content\": \"Summarize this contract: ...\"}],\n)\nprint(resp.content[0].text)\n# The model retains NOTHING after this call. State = what you send next time."),
      "<h3>Tokens & the context window</h3><p>Text is split into tokens (~4 chars / 0.75 words in English). The context window is the max tokens (input + output) per call. Bigger context = more cost and latency, and quality can degrade in the middle of very long contexts (the 'lost in the middle' effect).</p>",
      "<h3>Decoding parameters</h3>",
      table(["Parameter", "What it does", "Raise it when", "Lower it when"], [
        ["temperature", "Scales randomness of sampling", "Brainstorming, creative copy", "Extraction, classification, JSON output"],
        ["top_p (nucleus)", "Sample from smallest set covering p mass", "Want diversity without long tail", "Want determinism"],
        ["max_tokens", "Output length cap", "Long generations", "Control cost/latency"],
      ]),
      callout("key", "Hallucination is intrinsic", "The model maximizes plausibility, not truth. You cannot prompt it away entirely. You <em>manage</em> it: ground answers in retrieved sources, require citations, validate outputs, and evaluate groundedness. This is the thread that connects RAG, guardrails and eval."),
    ],
    diagram: diagram(
      "flowchart LR\n  P[Prompt tokens] --> T[Transformer]\n  T --> D[Distribution over next token]\n  D --> S[Sample one token]\n  S --> A[Append]\n  A --> T",
      flow([{ t: "Prompt tokens" }, { t: "Transformer", s: "self-attention", accent: true }, { t: "Next-token distribution" }, { t: "Sample + append → loop" }], "lr"),
      "The generation loop: there is no step where the model 'looks up a fact'."
    ),
    tradeoffs: { headers: ["Lever", "Helps", "Costs"], rows: [
      ["Bigger context window", "More grounding, fewer chunks dropped", "Higher latency & token cost, 'lost in middle'"],
      ["Lower temperature", "Consistency, easier eval", "Less diversity/creativity"],
      ["More few-shot examples", "Better format adherence", "Token cost, slower"],
    ] },
    questions: [
      { q: "Why does an LLM hallucinate, and how do you reduce it in production?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "It generates the most probable continuation, which isn't always true. Reduce it by grounding answers in retrieved context (RAG), requiring citations and 'I don't know' when context is insufficient, lowering temperature for factual tasks, and validating outputs.",
        senior: "I treat it as a measurable property, not a vibe. I add a groundedness/faithfulness metric (does every claim trace to a retrieved chunk?), gate responses on a confidence/coverage signal, and monitor hallucination rate in production via sampled LLM-as-judge checks. Prompting helps at the margin; architecture and evaluation are what actually move the number.",
        followups: ["How do you measure groundedness?", "What if retrieval returns nothing relevant?", "Does a bigger model hallucinate less?"],
        redflag: "“You just tell it not to hallucinate in the prompt.” Signals no production experience.",
        task: "Add a 'cite or say I don't know' instruction to your RAG app and measure unsupported-claim rate before/after." },
      { q: "What is the context window and why does it matter for cost and quality?", difficulty: "Beginner", round: "screening", seniority: "Mid", frequency: "high", region: "Both",
        model: "It's the max tokens (input + output) the model can process per call. It matters because everything — system prompt, history, retrieved context — competes for it; you pay per token and latency grows with it.",
        senior: "I budget the window deliberately: cap history, compress or rerank retrieved context to the top-k that fits, and watch for 'lost in the middle' degradation in long contexts. Bigger windows aren't free quality — placement and relevance of context matter more than raw size.",
        followups: ["How do you decide how many chunks to put in context?", "What's 'lost in the middle'?"],
        redflag: "Thinking the model 'remembers' previous calls without you resending context." },
      { q: "When would you change temperature, and to what?", difficulty: "Beginner", round: "screening", seniority: "Mid", frequency: "medium", region: "Both",
        model: "Low (0–0.3) for extraction, classification, JSON, and anything you'll evaluate deterministically; higher (0.7–1.0) for ideation/creative tasks where diversity helps.",
        senior: "For production features I default to low temperature for reproducibility and easier evaluation, and pair it with structured output. I only raise it where diversity is the product (e.g. generating multiple draft options) and then sample N and rank.",
        followups: ["How does temperature interact with evaluation reproducibility?"],
        redflag: "Using high temperature for structured/extraction tasks." },
      { q: "What is a token, and why should an engineer care?", difficulty: "Beginner", round: "screening", seniority: "Mid", frequency: "high", region: "Both",
        model: ["A token is a chunk of text the model processes (~4 characters / ~0.75 words in English). Models read and write in tokens, not words.", "Why it matters:", "<ul><li>You pay per token (input + output) — cost is token-driven.</li><li>The context window is measured in tokens.</li><li>Latency grows with tokens generated.</li></ul>"],
        senior: "A senior estimates token cost before shipping (counts tokens of a representative prompt + context), and trims context as a primary cost lever — not as an afterthought.",
        followups: ["How many tokens is your typical RAG prompt?", "How does tokenization differ across languages?"],
        redflag: "Equating tokens exactly with words, or not knowing you pay per token." },
      { q: "Does a bigger model always give better answers?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: ["No. A bigger model is more capable but slower and more expensive. For many tasks (classification, extraction, simple RAG answering) a smaller model is good enough and far cheaper/faster.", "Choose by measuring quality on your eval set against cost and latency budgets."],
        senior: "A senior routes by task difficulty — small model for easy traffic, large for hard cases — and proves the smaller model holds quality on the eval set before downgrading. Bigger ≠ better, cheaper-that-passes-eval wins.",
        followups: ["How do you decide the routing threshold?", "When is fine-tuning a small model better than a big model?"],
        redflag: "'Always use the biggest model to be safe' with no cost/latency consideration." },
    ],
    hands: ["Write a script that calls an LLM with temperature 0 and 1 on the same factual prompt 5×; observe variance. Then count tokens of a long document with a tokenizer and estimate cost."],
    portfolio: ["This is the 'why' behind your <a href='../projects/build-first-rag.html'>RAG app</a>: you ground because the model hallucinates. Say that explicitly in your README."],
    mistakes: ["Saying the model 'remembers' across calls", "Treating hallucination as a prompt bug", "Using high temperature for extraction"],
    checklist: ["I can explain next-token prediction in one sentence", "I can explain why grounding reduces hallucination", "I know when to change temperature"],
    sources: ["Section 8 (LLM fundamentals) and section 9 of the source material."],
  },
});

// ───────────────────── 05 · PROMPTING & LLM APIS ─────────────────────
CORE.push({
  kind: "learn", group: "core", num: "05", title: "Prompting & LLM APIs", path: "pages/prompting-llm-apis.html",
  kw: "prompt engineering system prompt few shot chain of thought structured output json function calling streaming llm api fastapi",
  data: {
    eyebrow: "Core Track",
    title: "Prompting & LLM APIs",
    lead: "The practitioner layer: how to talk to an LLM API like an engineer — system vs user roles, structured output, function/tool calling, streaming, and the prompt patterns that actually move quality (not prompt-magic folklore).",
    audience: "Engineers who'll be building LLM features behind an API.",
    goal: "Design reliable prompts and integrate LLM APIs with structured output, streaming and tool calling.",
    learn: ["Roles: system / user / assistant and how to use them", "Prompt patterns that work: instruction + context + examples", "Structured / JSON output and why schemas beat parsing", "Function/tool calling basics", "Streaming and its UX/latency tradeoffs"],
    pills: [{ t: "Intermediate" }, { t: "Coding round", cls: "blue" }],
    whyInterviews: ["Every applied loop probes whether you can get <em>reliable, parseable, grounded</em> output from a model — not whether you know clever prompt tricks. Structured output and tool calling come up constantly in coding rounds."],
    answer60: "“I structure prompts as role + clear instruction + context + (optional) few-shot examples + an explicit output contract. For anything a program consumes I force structured output via a schema/tool-call rather than parsing free text. I stream when the user is waiting on long output, and I keep prompts versioned because they're config, not code comments.”",
    answer3: [
      "I think in terms of an output contract. The system prompt sets role and rules; the user message carries the task and context; for machine-consumed output I use the provider's structured-output / tool-calling so the model returns validated JSON instead of prose I have to regex.",
      "Prompt quality levers that actually matter: be explicit about the task and constraints, put the most important instructions early and late, provide 1–3 high-quality few-shot examples for format adherence, and ask for 'I don't know' when context is insufficient. I treat prompts as versioned config so I can A/B and roll back — never as buried string literals.",
    ],
    concept: [
      "<h3>The output contract</h3><p>Decide first: is a human or a program reading this? Programs get schemas; humans get prose. Don't parse free text when the API can return validated JSON.</p>",
      code("python", "from pydantic import BaseModel\nfrom anthropic import Anthropic\n\nclass Triage(BaseModel):\n    category: str\n    urgency: int  # 1-5\n    needs_human: bool\n\nclient = Anthropic()\n# Tool calling = the reliable way to get structured output\nresp = client.messages.create(\n    model=\"claude-opus-4-8\", max_tokens=300,\n    tools=[{\"name\": \"emit_triage\", \"description\": \"Return the triage decision\",\n            \"input_schema\": Triage.model_json_schema()}],\n    tool_choice={\"type\": \"tool\", \"name\": \"emit_triage\"},\n    messages=[{\"role\": \"user\", \"content\": email_text}],\n)\ndecision = Triage(**resp.content[0].input)  # validated, no regex"),
      "<h3>Prompt patterns that earn their keep</h3>",
      table(["Pattern", "Use for", "Note"], [
        ["Clear instruction + constraints", "Everything", "The biggest single lever"],
        ["Few-shot examples", "Format/style adherence", "1–3 good examples > 10 mediocre"],
        ["Chain-of-thought / 'think step by step'", "Reasoning tasks", "Hide the reasoning from end users; keep it for the model"],
        ["'Say I don't know if unsure'", "Grounded QA", "Cuts hallucination"],
        ["Structured output / tool call", "Machine-consumed output", "Validate with a schema"],
      ]),
      callout("warn", "Prompts are config, not constants", "Version prompts, store them outside code, and A/B test changes. A prompt edit is a production change — treat it like one (this connects to <a href='llmops-observability.html'>LLMOps</a>)."),
    ],
    diagram: diagram(
      "flowchart TD\n  U[User request] --> B{Machine or human reads output?}\n  B -->|Machine| S[Tool call / JSON schema -> validate]\n  B -->|Human| P[Prose + stream tokens]\n  S --> V{Valid?}\n  V -->|No| R[Repair / retry]\n  V -->|Yes| Done[Use it]",
      flow([{ t: "Request" }, { t: "Output contract", s: "schema if machine-read", accent: true }, { t: "Validate / repair" }, { t: "Use or stream" }], "lr"),
      "Decide the output contract before writing the prompt."
    ),
    tradeoffs: { headers: ["Choice", "Pro", "Con"], rows: [
      ["Structured output (tool call)", "Reliable, validated", "Slightly more setup; rare schema-fit failures"],
      ["Free-text + parsing", "Flexible", "Brittle, breaks silently"],
      ["Streaming", "Fast perceived latency", "Harder to validate/guardrail mid-stream"],
      ["Many few-shot examples", "Format adherence", "Token cost, latency"],
    ] },
    questions: [
      { q: "How do you get reliable structured (JSON) output from an LLM?", difficulty: "Intermediate", round: "coding", seniority: "Senior", frequency: "high", region: "Both",
        model: "Use the provider's structured-output / function-calling with a JSON schema (e.g. Pydantic), set tool_choice to force the tool, and validate the result. On validation failure, retry with the error fed back. Don't regex free text.",
        senior: "I define the schema as the single source of truth (Pydantic model → json schema → validation), force the tool call, and add a bounded repair loop that returns the validation error to the model. I also keep temperature low and log schema-failure rate as a quality metric — a rising rate usually means a prompt or schema drift, not a model problem.",
        followups: ["What do you do on a validation failure?", "How does this interact with streaming?"],
        redflag: "Parsing JSON out of prose with string operations and no validation.",
        task: "Build the triage extractor above; add a repair loop and log failure rate." },
      { q: "When do you use streaming, and what does it complicate?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: "Stream when a human waits on long output — it cuts perceived latency. It complicates output validation/guardrails (you may emit text before you've seen it all) and makes structured parsing harder.",
        senior: "I stream user-facing prose, but for machine-consumed or safety-critical output I either don't stream or buffer-and-validate before release. A common pattern: stream tokens to the UI but run guardrail checks on the completed message and be ready to retract/replace.",
        followups: ["How do you guardrail a streamed response?"],
        redflag: "Streaming safety-critical structured output with no post-validation." },
      { q: "Few-shot vs zero-shot — how do you decide?", difficulty: "Beginner", round: "screening", seniority: "Mid", frequency: "medium", region: "Both",
        model: "Start zero-shot. Add few-shot examples when you need consistent format/style or the task is ambiguous. Use a small number of high-quality, diverse examples.",
        senior: "I add examples only to fix a specific failure I've observed, and I measure the token-cost/quality trade. Often a clearer instruction or a schema beats adding examples.",
        followups: ["What's the downside of too many examples?"],
        redflag: "Stuffing 20 examples 'to be safe' without measuring." },
      { q: "How do you design a system prompt vs a user prompt?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: ["<ul><li><strong>System prompt</strong> — stable role, rules, output contract, safety constraints. Set once, rarely changes per request.</li><li><strong>User prompt</strong> — the task and the per-request data (the question, the retrieved context).</li></ul>", "Keep instructions in the system prompt and untrusted/variable content in the user turn."],
        senior: "A senior keeps untrusted content (retrieved docs, user text) clearly separated from authoritative instructions — that separation is also the first line of defense against prompt injection.",
        followups: ["Where do you put retrieved RAG context?", "How does this relate to injection defense?"],
        redflag: "Mixing instructions and untrusted data together so injected text carries authority." },
      { q: "Walk me through calling an LLM API cleanly from Python (FastAPI).", difficulty: "Intermediate", round: "coding", seniority: "Senior", frequency: "high", region: "Both",
        model: ["<ul><li>Wrap the provider client in a small service module (not scattered calls).</li><li>Use a Pydantic schema for structured output; validate the response.</li><li>Set timeouts and retries with backoff; handle rate limits.</li><li>Log the request/response (with token counts) for observability; never log secrets/PII raw.</li><li>Expose it as a typed FastAPI endpoint.</li></ul>"],
        senior: "A senior treats the LLM as an unreliable network dependency — timeouts, retries, fallback model, and a clean seam so the provider is swappable — and makes prompts versioned config, not literals.",
        followups: ["How do you make this testable?", "How do you add a fallback model?"],
        redflag: "Calling the SDK inline everywhere with no validation, timeout, or error handling.", task: "Build the FastAPI endpoint in the hands-on task below." },
    ],
    hands: ["Build a FastAPI endpoint that takes text and returns validated JSON via tool calling, with a repair-on-invalid loop and request logging."],
    portfolio: ["Structured output + a repair loop is a great thing to show in your <a href='../projects/email-triage.html'>email triage agent</a> — it demonstrates production discipline."],
    mistakes: ["Regexing JSON out of prose", "Burying prompts as string literals with no versioning", "Streaming output you then can't validate"],
    checklist: ["I can force + validate structured output", "I know when to stream and its risks", "I version my prompts"],
    sources: ["Sections 3 (Python for AI engineers), 13 (coding patterns) of the source material."],
  },
});

// ───────────────────── 06 · EMBEDDINGS & VECTOR SEARCH ─────────────────────
CORE.push({
  kind: "learn", group: "core", num: "06", title: "Embeddings & Vector Search", path: "pages/embeddings-vector-search.html",
  kw: "embeddings vector database cosine similarity hnsw ann faiss qdrant pgvector hybrid search bm25 reranking semantic search recall",
  data: {
    eyebrow: "Core Track",
    title: "Embeddings & Vector Search",
    lead: "The retrieval substrate under RAG and agent memory: what an embedding is, how vector databases find neighbors fast (ANN/HNSW), and why production retrieval is almost always <em>hybrid</em> search plus a reranker — not pure cosine similarity.",
    audience: "Engineers building retrieval into LLM apps.",
    goal: "Explain embeddings, choose a vector store, and design hybrid retrieval with reranking.",
    learn: ["What embeddings are and why cosine similarity works", "ANN indexes (HNSW) and the recall/latency tradeoff", "Choosing a vector DB (pgvector vs Qdrant vs FAISS)", "Hybrid search (dense + BM25) and reranking", "Why semantic-only retrieval fails on names, IDs and keywords"],
    pills: [{ t: "Core for RAG", cls: "purple" }, { t: "High frequency", cls: "red" }],
    whyInterviews: ["Retrieval quality is the single biggest driver of RAG quality, so interviewers probe it hard. 'Why is your RAG returning irrelevant chunks?' is a near-guaranteed question, and the answer lives here."],
    answer60: "“An embedding maps text to a vector so that semantically similar text lands nearby; we retrieve by nearest-neighbor (cosine) search, made fast with an ANN index like HNSW. In production I rarely use pure semantic search — I combine it with keyword/BM25 (hybrid) so exact terms, names and IDs aren't missed, then rerank the top candidates with a cross-encoder for precision.”",
    answer3: [
      "An embedding model maps text into a high-dimensional vector where distance encodes semantic similarity. To search millions of vectors we don't scan linearly — we use approximate nearest-neighbor indexes (HNSW is the common one), trading a little recall for big latency wins, tunable via parameters like ef_search.",
      "The production reality: dense embeddings are great at meaning but weak at exact tokens — part numbers, names, acronyms. So I run <strong>hybrid search</strong>: dense + sparse (BM25), fuse the scores (e.g. reciprocal rank fusion), then <strong>rerank</strong> the top ~50 with a cross-encoder to get the top ~5 that actually go in context. Choice of store depends on scale and ops: pgvector if I already run Postgres and scale is moderate; Qdrant/Weaviate for larger, filter-heavy workloads; FAISS for in-process/offline.",
    ],
    concept: [
      "<h3>Embeddings & similarity</h3><p>Text → vector. Cosine similarity (angle between vectors) measures semantic closeness. Normalize vectors and cosine ≈ dot product.</p>",
      code("python", "from sentence_transformers import SentenceTransformer\nmodel = SentenceTransformer(\"BAAI/bge-small-en-v1.5\")\nvecs = model.encode([\"reset my password\", \"how do I change my login\"], normalize_embeddings=True)\nimport numpy as np\nprint(float(vecs[0] @ vecs[1]))  # high cosine -> semantically close"),
      "<h3>ANN indexes</h3><p>Exact nearest-neighbor over millions of vectors is too slow. HNSW builds a navigable small-world graph; you trade exactness for speed. Key knobs: index build (M, ef_construction) and query (ef_search) — higher ef_search = better recall, more latency.</p>",
      "<h3>Hybrid search + reranking</h3>",
      table(["Stage", "Method", "Why"], [
        ["Recall (dense)", "Embedding ANN search", "Captures meaning / paraphrase"],
        ["Recall (sparse)", "BM25 / keyword", "Catches exact terms, IDs, names"],
        ["Fusion", "Reciprocal Rank Fusion", "Combine both candidate lists"],
        ["Precision", "Cross-encoder reranker", "Reorder top-k for the context window"],
      ]),
      callout("key", "Hybrid + rerank is the default", "If you only remember one thing: production retrieval = dense + sparse, fused, then reranked. Pure cosine-only retrieval is the #1 cause of 'my RAG returns junk'."),
    ],
    diagram: diagram(
      "flowchart LR\n  Q[Query] --> D[Dense ANN]\n  Q --> S[BM25 sparse]\n  D --> F[Fuse RRF]\n  S --> F\n  F --> R[Cross-encoder rerank]\n  R --> C[Top-k -> context]",
      flow([{ t: "Query" }, { t: "Dense + Sparse", s: "two recall channels", accent: true }, { t: "Fuse (RRF)" }, { t: "Rerank (cross-encoder)" }, { t: "Top-k → context" }], "lr"),
      "Two-channel recall → fuse → rerank → small precise context."
    ),
    tradeoffs: { headers: ["Vector store", "Best when", "Watch out"], rows: [
      ["pgvector (Postgres)", "Already on Postgres, moderate scale, want SQL filters + transactions", "Tune index (HNSW); very large scale gets harder"],
      ["Qdrant / Weaviate / Milvus", "Large scale, rich metadata filtering, hybrid built-in", "Another service to run/monitor"],
      ["FAISS", "In-process, offline, research, batch", "No persistence/filters/multi-tenant out of the box"],
      ["Managed (Pinecone etc.)", "Don't want to run infra", "Cost, vendor lock-in, data residency"],
    ] },
    questions: [
      { q: "Your RAG returns irrelevant chunks. Walk me through how you debug retrieval.", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Inspect what's actually retrieved for failing queries. Check chunking (too big/small?), embedding model fit for the domain, whether exact terms are missed (add BM25/hybrid), and whether a reranker would fix ordering. Measure retrieval with recall@k / MRR on a labeled set.",
        senior: "I separate retrieval failures from generation failures first — log the retrieved chunks per query. Common root causes in order: no hybrid search (misses IDs/names), bad chunking that splits the answer, wrong/under-trained embedding model for the domain, and missing reranking. I fix with a labeled query set and recall@k as the target metric, so changes are measured, not guessed.",
        followups: ["How do you build the labeled retrieval set?", "When would you fine-tune the embedding model?", "Dense vs sparse for a parts catalog?"],
        redflag: "Jumping to 'use a bigger LLM' for what is a retrieval problem.",
        task: "Add hybrid search + a reranker to your RAG app; report recall@5 before/after.", source: SRC.ragas },
      { q: "What is an ANN index and what do you trade for speed?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Approximate nearest neighbor (e.g. HNSW) finds close-enough neighbors without scanning everything. You trade a little recall for large latency gains; ef_search tunes that trade.",
        senior: "I pick parameters from an SLO: set a latency budget, then raise ef_search until recall@k plateaus within budget. I also account for build time and memory (M, ef_construction) and re-index strategy as the corpus grows.",
        followups: ["How does HNSW differ from IVF?", "What happens to recall as you scale to 100M vectors?"],
        redflag: "Believing ANN is exact / lossless.", source: SRC.hnsw },
      { q: "Dense vs sparse retrieval — when does each win?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Dense (embeddings) wins on paraphrase and meaning; sparse (BM25) wins on exact tokens — IDs, names, codes, rare terms. Hybrid combines both.",
        senior: "I default to hybrid because real corpora mix both needs. For a legal/finance/parts domain with lots of exact identifiers, I weight sparse higher; for conversational FAQ, dense. I validate the weighting on a labeled set rather than guessing.",
        followups: ["How do you fuse the two score lists?"],
        redflag: "Insisting embeddings alone are always sufficient." },
      { q: "How do you choose a vector database?", difficulty: "Intermediate", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "By scale, filtering needs, ops appetite and existing stack. pgvector if you already run Postgres at moderate scale; a dedicated store (Qdrant/Weaviate/Milvus) for large, filter-heavy workloads; FAISS for in-process/offline; managed if you don't want infra.",
        senior: "I start from constraints: data residency/compliance, metadata filtering, multi-tenancy, and whether the team can operate another stateful service. I avoid premature adoption of a heavy vector DB — pgvector covers a surprising amount and keeps one fewer system to run.",
        followups: ["Why not always use a managed service?", "How do you handle multi-tenant isolation?"],
        redflag: "Picking the trendiest DB with no reference to scale or ops.", source: SRC.pgvector },
    ],
    hands: ["Index 1,000 docs in pgvector or Qdrant. Implement dense, BM25, and hybrid retrieval; add a cross-encoder reranker; measure recall@5 on 20 labeled queries across the three."],
    portfolio: ["A README section 'Why hybrid + rerank' with a before/after recall@5 number is a standout signal in your <a href='../projects/build-first-rag.html'>RAG project</a>."],
    mistakes: ["Pure semantic search in production", "Ignoring chunking when retrieval is bad", "No labeled set, so retrieval changes are guesses", "Adopting a heavy vector DB before you need it"],
    checklist: ["I can explain HNSW and the recall/latency knob", "I can justify hybrid + rerank", "I can measure recall@k / MRR", "I can choose a vector store from constraints"],
    sources: ["Sections 4–5 of the source material; RAG deep-dive corpus."],
  },
});

export default CORE;
