import { code, table, flow, diagram, callout } from "../render.mjs";
import { SRC } from "./sources.mjs";

export const CORE2 = [];

// ───────────────────── 07 · RAG MASTERY ─────────────────────
CORE2.push({
  kind: "learn", group: "core", num: "07", title: "RAG Mastery", path: "pages/rag-mastery.html",
  kw: "rag retrieval augmented generation chunking strategies grounded generation citations hallucination reduction rag evaluation hybrid search reranking context",
  data: {
    eyebrow: "Core Track",
    title: "RAG Mastery",
    lead: "The single most-tested GenAI skill. This is the end-to-end pipeline — chunk → embed → retrieve → rerank → ground → generate with citations → evaluate — plus the failure modes and tradeoffs that separate a demo from a production RAG system.",
    audience: "Engineers who will build and defend a RAG system in interviews.",
    goal: "Design, debug and evaluate a production RAG pipeline and answer any RAG follow-up.",
    learn: ["The full RAG pipeline and where each stage fails", "Chunking strategies and how to choose", "Grounded generation, citations and 'I don't know'", "Hallucination reduction that actually works", "How to evaluate RAG (retrieval + generation)", "When RAG beats fine-tuning"],
    pills: [{ t: "Flagship topic", cls: "red" }, { t: "Every applied loop", cls: "red" }],
    whyInterviews: ["RAG is how most companies put their own data into an LLM without fine-tuning. It's the default architecture, so it's the default interview. Expect a full pipeline walkthrough plus deep follow-ups on chunking, retrieval quality and evaluation."],
    answer60: "“RAG grounds an LLM in your data: chunk documents, embed them, retrieve the most relevant chunks for a query (hybrid search + reranking), put them in the prompt, and generate an answer with citations — instructing the model to say 'I don't know' if the context doesn't support an answer. It beats fine-tuning when knowledge changes often or must be sourced/auditable. The hard parts are chunking and retrieval quality, and you must evaluate both retrieval and generation.”",
    answer3: [
      "RAG has two halves people conflate. <strong>Indexing (offline):</strong> load → chunk → embed → store in a vector DB with metadata. <strong>Serving (online):</strong> embed the query → retrieve (ideally hybrid dense+sparse) → rerank → assemble a grounded prompt → generate with citations → validate.",
      "The quality of a RAG system is dominated by retrieval, and retrieval is dominated by chunking and hybrid+rerank. So I instrument both stages: retrieval metrics (recall@k, MRR, nDCG on a labeled set) and generation metrics (faithfulness/groundedness, answer relevance). For trust I require citations and an explicit 'insufficient context → say so' path.",
      "On RAG vs fine-tuning: RAG for knowledge that changes, needs attribution, or is large; fine-tuning for behavior/format/style. They compose — you can fine-tune tone and RAG the facts. I almost always reach for RAG first because it's cheaper to update and auditable.",
    ],
    concept: [
      "<h3>The pipeline</h3>",
      flow([{ t: "Load" }, { t: "Chunk", s: "size + overlap + structure", accent: true }, { t: "Embed" }, { t: "Vector store + metadata" }], "lr"),
      flow([{ t: "Query" }, { t: "Retrieve (hybrid)", accent: true }, { t: "Rerank" }, { t: "Grounded prompt" }, { t: "Generate + cite" }, { t: "Validate" }], "lr"),
      "<h3>Chunking strategies</h3>",
      table(["Strategy", "How", "Best for"], [
        ["Fixed-size + overlap", "N tokens, ~10–20% overlap", "Default baseline; uniform prose"],
        ["Structure-aware", "Split on headings/sections/markdown", "Docs with clear structure (manuals, wikis)"],
        ["Recursive", "Split by paragraph→sentence to fit size", "Mixed content"],
        ["Sentence-window / parent-doc", "Retrieve small, expand to surrounding context", "Precise retrieval + enough context to answer"],
        ["Semantic chunking", "Split where embedding similarity drops", "When topic boundaries matter, cost OK"],
      ]),
      callout("key", "Chunking is the highest-leverage RAG decision", "Too big → diluted embeddings and wasted context. Too small → the answer gets split across chunks. Parent-document / sentence-window retrieval often wins: embed small for precise matching, return the larger surrounding passage for enough context to answer."),
      "<h3>Grounded generation & citations</h3>",
      code("text", "System: Answer ONLY using the <context>. Cite sources as [n].\nIf the context does not contain the answer, say \"I don't have enough\ninformation to answer that.\" Do not use prior knowledge.\n\n<context>\n[1] {chunk_1}  (source: handbook.pdf p.12)\n[2] {chunk_2}  (source: policy.md)\n</context>\n\nQuestion: {user_question}"),
      "<h3>Hallucination reduction that works</h3><ul><li>Grounding + 'cite or abstain' (above)</li><li>Hybrid retrieval + reranking so the right context is present</li><li>Coverage gate: if top retrieval score &lt; threshold, abstain or escalate</li><li>Faithfulness check (LLM-as-judge) on a sample of responses</li></ul>",
    ],
    diagram: diagram(
      "flowchart TD\n  subgraph Offline[Indexing]\n    L[Load] --> C[Chunk] --> E[Embed] --> V[(Vector DB)]\n  end\n  subgraph Online[Serving]\n    Q[Query] --> RT[Hybrid retrieve] --> RR[Rerank] --> PR[Grounded prompt] --> G[LLM] --> A[Answer + citations]\n  end\n  V --> RT\n  A --> EV[Eval: faithfulness, recall@k]",
      flow([{ t: "Indexing (offline)", s: "load·chunk·embed·store", accent: true }, { t: "Serving (online)", s: "retrieve·rerank·ground·generate" }, { t: "Evaluate both halves" }], "lr"),
      "Two halves, two evaluation surfaces. Don't conflate retrieval and generation failures."
    ),
    tradeoffs: { headers: ["Decision", "Option A", "Option B", "Rule of thumb"], rows: [
      ["Get knowledge in", "RAG", "Fine-tune", "RAG for changing/auditable facts; fine-tune for behavior"],
      ["Chunk size", "Small", "Large", "Small for precision; expand context via parent-doc"],
      ["Retrieval", "Semantic only", "Hybrid + rerank", "Hybrid + rerank in production"],
      ["Context amount", "Many chunks", "Few reranked chunks", "Few, high-precision chunks beat stuffing"],
    ] },
    questions: [
      { q: "Walk me through a production RAG pipeline end to end.", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Offline: load, chunk (with overlap/structure), embed, store with metadata. Online: embed query, hybrid retrieve, rerank, assemble a grounded prompt with citations and an abstain instruction, generate, validate. Evaluate retrieval (recall@k) and generation (faithfulness) separately.",
        senior: "I emphasize the production seams: metadata for filtering and access control, a reranker to keep context small and precise, citations for auditability, an abstain path when retrieval is weak, caching of embeddings and frequent queries, and observability (trace each query's retrieved chunks). And I'd state my eval plan up front because that's what makes it maintainable.",
        followups: ["Where does it most commonly fail?", "How do you handle access control / multi-tenant data?", "How do you keep the index fresh?"],
        redflag: "Describing only 'embed → cosine search → stuff into prompt' with no reranking, citations, or evaluation.",
        task: "Build the full pipeline in your <a href='../projects/build-first-rag.html'>RAG app</a> including citations and an eval harness." },
      { q: "How do you choose a chunking strategy?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Start with fixed-size + overlap as a baseline; move to structure-aware for well-structured docs; use parent-document/sentence-window when you need precise matching but enough surrounding context to answer. Validate with retrieval metrics.",
        senior: "I treat chunking as an experiment driven by a labeled query set, not a default. I check whether answers span chunk boundaries (a sign chunks are too small or overlap too low) and whether embeddings are diluted (chunks too big). Parent-document retrieval is my common landing spot: embed sentences/small chunks for precision, return the parent section for context.",
        followups: ["What overlap do you use and why?", "How does chunk size interact with the embedding model's max length?"],
        redflag: "A fixed '512 tokens always' answer with no reference to document structure or measurement." },
      { q: "RAG vs fine-tuning — when do you pick which?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "RAG for knowledge that changes, is large, or must be sourced/audited. Fine-tuning for consistent behavior, format, tone, or domain phrasing. They compose.",
        senior: "Default to RAG because updates are a re-index, not a retraining run, and answers are auditable. I'd only fine-tune when prompting+RAG can't hit a behavioral/format/latency target, and even then prefer lightweight (LoRA) fine-tuning. I make the call on cost-to-update and need-for-attribution, not hype.",
        followups: ["Can you do both?", "What about fine-tuning the embedding model instead?"],
        redflag: "'Fine-tune the LLM on our docs' as the default for a knowledge problem." },
      { q: "How do you evaluate a RAG system?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Separately. Retrieval: recall@k, MRR, nDCG on a labeled query→relevant-doc set. Generation: faithfulness/groundedness (claims supported by context), answer relevance, and correctness vs a golden set. Combine with LLM-as-judge for scale.",
        senior: "I build a small golden dataset early and treat eval as CI: every retrieval/prompt change runs it. I split metrics so I know whether a regression is retrieval or generation. For groundedness I use an LLM-as-judge with a calibrated rubric and spot-check it against human labels. In production I sample live traffic for the same checks.",
        followups: ["How big should the golden set be?", "How do you trust LLM-as-judge?", "RAGAS-style metrics?"],
        redflag: "'We eyeballed a few answers and it looked good.'",
        task: "Add recall@k + a faithfulness judge to your RAG app and wire it into CI." },
      { q: "Retrieval returns good chunks but the answer is still wrong. What's happening?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: "It's a generation problem: the prompt may not constrain the model to the context, context ordering/overflow ('lost in the middle'), conflicting chunks, or the model ignoring the abstain instruction. Tighten grounding instructions, reduce/rerank context, and check for context overflow.",
        senior: "I confirm the right chunks are present, then look at: prompt not forcing context-only answers, too many chunks causing dilution, contradictory sources with no resolution strategy, and missing citations so I can't tell what it used. Fixes: fewer reranked chunks, explicit cite-or-abstain, and a conflict-resolution instruction. I verify with a faithfulness metric, not by re-reading one answer.",
        followups: ["What's 'lost in the middle' and how do you mitigate it?"],
        redflag: "Blaming the model and swapping to a bigger one without isolating retrieval vs generation.", source: SRC.lostMiddle },
      { q: "How do you choose chunk size?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["There's no universal number — you tune it against a labeled query set. Rules of thumb:", "<ul><li>Smaller chunks (≈200–400 tokens) → precise matching, but answers may get split across chunks.</li><li>Larger chunks (≈800–1000 tokens) → more context per chunk, but diluted embeddings and wasted context window.</li><li>Respect the embedding model's max input length.</li><li>Start ~400–512 tokens with overlap, then measure recall@k and adjust.</li></ul>"],
        senior: "A senior decouples the retrieval unit from the context unit — embed small chunks for precise matching but return the larger parent passage for enough context to answer (parent-document retrieval) — so they don't have to compromise.",
        followups: ["How does chunk size interact with overlap?", "Why not just use huge chunks now that windows are large?"],
        redflag: "A fixed number with no measurement and no awareness of the size/precision trade.", task: "Sweep 3 chunk sizes on your golden set and compare recall@5." },
      { q: "How do you handle overlapping chunks?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Overlap means consecutive chunks share some text (typically ~10–20%). It stops an answer that sits on a chunk boundary from being cut in half.", "<ul><li>Too little overlap → boundary answers get split and missed.</li><li>Too much overlap → duplicate content inflates the index, cost, and can return near-duplicate chunks.</li></ul>"],
        senior: "A senior notes overlap is a band-aid for fixed-size splitting and prefers structure-aware or parent-document chunking where overlap matters less, and de-duplicates near-identical retrieved chunks before they hit the context.",
        followups: ["What overlap % do you use and why?", "How do you avoid returning duplicate chunks?"],
        redflag: "Adding large overlap everywhere 'to be safe' without considering index bloat." },
      { q: "Why use reranking, and why a cross-encoder?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["First-pass retrieval (vector/keyword) is fast but imprecise, so it returns many roughly-relevant chunks. A reranker reorders the top candidates by true relevance and keeps the best few for the prompt.", "A cross-encoder reads the query and a chunk together, so it judges relevance far more accurately than comparing separate embeddings — at the cost of speed, which is why you only rerank the top ~50, not the whole corpus."],
        senior: "A senior uses reranking to keep the context small and high-precision (cheaper, less 'lost in the middle') and measures the recall@k / faithfulness lift to justify the extra latency.",
        followups: ["Why not cross-encode the whole corpus?", "How much latency does reranking add?"],
        redflag: "Putting 20 un-reranked chunks into the prompt and hoping the model sorts it out." },
      { q: "How do you add citations, and why do they matter?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Pass each retrieved chunk with an id and source metadata (file, page, URL), instruct the model to cite the id it used for each claim (e.g. [1]), and map those ids back to sources in the response.", "Citations matter because they make answers auditable, let users verify, and let you detect hallucination (a claim with no citation is suspect)."],
        senior: "A senior validates that cited chunks actually support the claim (not just that a citation exists) and uses citation presence as a guardrail — block or flag answers that make uncited factual claims.",
        followups: ["How do you verify the citation actually supports the claim?", "How do you cite a specific page or clause?"],
        redflag: "Treating citations as decoration without checking they back the claim.", task: "Add id-tagged context + citation mapping to your RAG app." },
      { q: "What can go wrong in production RAG?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: ["The common production failures:", "<ul><li>Retrieval misses (semantic-only, bad chunking) → wrong/empty context.</li><li>Hallucination when context is weak and there's no abstain path.</li><li>Stale index — documents changed but weren't re-indexed.</li><li>Cross-permission data leaks — retrieving docs a user shouldn't see.</li><li>Indirect prompt injection via a poisoned document.</li><li>Latency/cost blowups from huge contexts.</li></ul>"],
        senior: "A senior maps each to a mitigation (hybrid+rerank, abstain gate, freshness pipeline, access control at retrieval, untrusted-content handling, caching) and instruments them so failures are visible in traces, not discovered by users.",
        followups: ["How do you detect a stale index?", "How do you stop cross-tenant leaks?"],
        redflag: "Only describing the happy path with no failure modes.", task: "<a href='../projects/customer-support-rag.html'>Customer Support RAG Bot</a> exercises most of these." },
    ],
    hands: [callout("key", "▶ Now build it", "<p>Next in the learning path: <strong><a href='../projects/build-first-rag.html'>Build Your First RAG App →</a></strong>. This page is the theory; that project is where you make it real and get something for your portfolio.</p>"),
      "Extend your RAG app: structure-aware chunking, hybrid retrieval + reranker, citations, an abstain path, and an eval harness (recall@k + faithfulness). Record the before/after numbers."],
    portfolio: ["This page maps directly to <a href='../projects/build-first-rag.html'>Build First RAG App</a>, <a href='../projects/pdf-qna.html'>PDF Q&A</a> and <a href='../projects/customer-support-rag.html'>Customer Support RAG Bot</a>. Your README should show the pipeline diagram, your eval numbers, and your chunking rationale."],
    mistakes: ["Semantic-only retrieval", "Chunking by a fixed number with no measurement", "No citations / no abstain path", "Evaluating generation while ignoring retrieval (or vice-versa)", "Stuffing 20 chunks into context"],
    checklist: ["I can draw the full pipeline and name each failure mode", "I can justify my chunking with metrics", "I can evaluate retrieval and generation separately", "I can defend RAG vs fine-tuning"],
    sources: ["RAG deep-dive corpus; sections 9, 11, 20 of the source material."],
  },
});

// ───────────────────── 08 · AGENTIC AI ─────────────────────
CORE2.push({
  kind: "learn", group: "core", num: "08", title: "Agentic AI", path: "pages/agentic-ai.html",
  kw: "agents tool calling function calling react planner executor langgraph stateful agent memory guardrails multi-agent loop autonomy",
  data: {
    eyebrow: "Core Track",
    title: "Agentic AI",
    lead: "When an LLM stops just answering and starts <em>doing</em> — calling tools, planning multi-step work, and maintaining state. Covers tool/function calling, ReAct, planner-executor, LangGraph-style stateful agents, agent memory, and the guardrails that keep them safe.",
    audience: "Engineers building or explaining tool-using agents.",
    goal: "Design an agent, choose the right control pattern, and reason about reliability and safety.",
    learn: ["The agent loop: perceive → decide → act → observe", "Tool/function calling done reliably", "ReAct vs planner-executor vs stateful graphs", "Agent memory (short-term, long-term, episodic)", "Why agents fail and how to guardrail them", "When NOT to use an agent (workflow vs agent)"],
    pills: [{ t: "Flagship topic", cls: "red" }, { t: "Rising fast", cls: "purple" }],
    whyInterviews: ["Agents are the current frontier of hiring demand. Interviewers want to know you can build one that's reliable, not a flashy demo that loops forever or calls the wrong tool — so they probe control flow, failure handling and guardrails."],
    answer60: "“An agent is an LLM in a loop with tools: it decides which tool to call, the runtime executes it, the result goes back into context, and it repeats until done. Tool calling is the mechanism; ReAct (reason+act) and planner-executor are control patterns; LangGraph models the agent as a stateful graph with checkpoints and human-in-the-loop. The hard part is reliability — bounded loops, validated tool calls, and guardrails — and knowing when a deterministic workflow beats an agent.”",
    answer3: [
      "Mechanism first: the model is given tool schemas; it emits a structured tool call; the runtime executes the real function (DB query, API call, search); the result is appended to context; loop until the model emits a final answer. That loop is the agent.",
      "Control patterns: <strong>ReAct</strong> interleaves reasoning and acting step-by-step — flexible, good for open-ended tasks, but can wander. <strong>Planner-executor</strong> makes a plan up front then executes steps — more predictable, easier to debug. <strong>Stateful graphs (LangGraph)</strong> model nodes/edges with explicit state, checkpointing and human-in-the-loop — the production-grade option when you need control, retries and observability.",
      "Reliability is where senior signal lives: bound the loop (max steps), validate every tool call against a schema, make tools idempotent and least-privilege, add timeouts/retries, and put guardrails on both tool inputs and final outputs. And I always ask whether an agent is even needed — if the steps are known, a deterministic workflow is cheaper, faster and safer.",
    ],
    concept: [
      "<h3>The agent loop</h3>",
      flow([{ t: "LLM decides", s: "tool call or final answer", accent: true }, { t: "Runtime executes tool" }, { t: "Observation → context" }, { t: "Loop / finish" }], "lr"),
      code("python", "tools = [{\"name\": \"search_orders\", \"description\": \"Look up a customer's orders\",\n          \"input_schema\": {\"type\": \"object\", \"properties\": {\"customer_id\": {\"type\": \"string\"}}, \"required\": [\"customer_id\"]}}]\nmsgs = [{\"role\": \"user\", \"content\": \"Where is order for cust_42?\"}]\nfor _ in range(MAX_STEPS):                       # <-- bounded loop\n    r = client.messages.create(model=MODEL, max_tokens=512, tools=tools, messages=msgs)\n    if r.stop_reason != \"tool_use\":\n        break                                    # final answer\n    call = next(b for b in r.content if b.type == \"tool_use\")\n    result = run_tool(call.name, call.input)     # validate input first!\n    msgs += [{\"role\": \"assistant\", \"content\": r.content},\n             {\"role\": \"user\", \"content\": [{\"type\": \"tool_result\", \"tool_use_id\": call.id, \"content\": result}]}]"),
      "<h3>Control patterns</h3>",
      table(["Pattern", "How it works", "Strengths", "Weaknesses"], [
        ["ReAct", "Reason→act→observe each step", "Flexible, adaptive", "Can loop/wander, harder to predict cost"],
        ["Planner-executor", "Plan all steps, then execute", "Predictable, debuggable", "Bad plan early = bad run; less adaptive"],
        ["Stateful graph (LangGraph)", "Nodes/edges + explicit state + checkpoints", "Control, retries, HITL, observability", "More upfront engineering"],
        ["Multi-agent", "Specialized agents + supervisor", "Separation of concerns", "Coordination cost, more failure surface"],
      ]),
      "<h3>Agent memory</h3><ul><li><strong>Short-term:</strong> the conversation/scratchpad in the context window.</li><li><strong>Long-term:</strong> facts persisted across sessions (often a vector store) — retrieved like RAG.</li><li><strong>Episodic / state:</strong> the structured state the graph carries between steps (checkpointed).</li></ul>",
      callout("warn", "Workflow vs agent", "If you know the steps in advance, build a deterministic workflow (cheaper, faster, testable). Reach for an agent only when the path is genuinely dynamic. 'We used an agent because it's cool' is a red flag; 'we used a workflow because the steps were fixed' is senior judgment."),
    ],
    diagram: diagram(
      "flowchart TD\n  U[User goal] --> P{Plan or react?}\n  P --> A[LLM decides action]\n  A --> G1[Validate + guardrail tool input]\n  G1 --> T[Execute tool]\n  T --> O[Observation]\n  O --> A\n  A --> F[Final answer]\n  F --> G2[Guardrail output]",
      flow([{ t: "Goal" }, { t: "Decide action", accent: true }, { t: "Validate + guardrail input" }, { t: "Execute tool" }, { t: "Observe → loop" }, { t: "Guardrail output" }], "lr"),
      "Guardrails sit on tool inputs AND final outputs; the loop is bounded."
    ),
    tradeoffs: { headers: ["Decision", "Choose", "Because"], rows: [
      ["Known steps", "Workflow", "Cheaper, deterministic, testable"],
      ["Dynamic path", "Agent (ReAct/graph)", "Needs runtime decisions"],
      ["Need control + retries + HITL", "Stateful graph", "Explicit state & checkpoints"],
      ["Many distinct skills", "Multi-agent", "Separation of concerns — if coordination is worth it"],
    ] },
    questions: [
      { q: "How does an agent actually work — explain tool/function calling.", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "You give the model tool schemas. It emits a structured tool call; your runtime executes the real function and returns the result into context; the model decides the next action or finishes. The model never runs code itself — your runtime does.",
        senior: "I stress that the model only <em>proposes</em> calls — the runtime is the trust boundary, so I validate arguments against the schema, enforce least privilege and idempotency, add timeouts/retries, and bound the loop. That framing (model proposes, runtime disposes) is what makes agents safe.",
        followups: ["What stops it from looping forever?", "How do you handle a tool that fails?", "How do you prevent it calling a destructive tool wrongly?"],
        redflag: "Thinking the LLM executes the tool/code itself.",
        task: "Build a 2-tool agent with a bounded loop and input validation (see <a href='../projects/build-first-agent.html'>Build First Agent App</a>).", source: SRC.anthropicTools },
      { q: "ReAct vs planner-executor — when do you use each?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: "ReAct for open-ended, adaptive tasks where steps emerge from observations. Planner-executor when the task decomposes cleanly up front and you want predictability and easier debugging.",
        senior: "In practice I lean planner-executor or an explicit graph for production because predictability, cost control and observability matter more than raw flexibility. Pure ReAct is great for exploration but its variable step count makes cost/latency and testing harder. A hybrid — plan, then ReAct within steps — is common.",
        followups: ["How do you bound cost in ReAct?", "How do you test a planner-executor agent?"],
        redflag: "Treating ReAct as the only/always-best pattern." },
      { q: "Why do agents fail in production and how do you make them reliable?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: "Common failures: infinite/looping behavior, wrong tool or wrong arguments, cascading errors from a bad early step, cost blowups, and prompt injection via tool outputs. Fixes: bounded loops, schema-validated tool calls, idempotent least-privilege tools, retries/timeouts, and guardrails on inputs and outputs.",
        senior: "I add explicit state and checkpointing (so I can retry a step, not the whole run), per-step and per-run budgets, human-in-the-loop on high-risk actions, and full tracing so I can see the decision path. I also treat tool outputs as untrusted input (injection surface). Reliability is engineering around the loop, not a better prompt.",
        followups: ["How do you trace an agent run?", "Where do you put human-in-the-loop?", "How is tool output an injection vector?"],
        redflag: "No mention of bounding the loop, budgets, or treating tool output as untrusted." },
      { q: "What kinds of memory does an agent need?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Mid–Senior", frequency: "medium", region: "Both",
        model: "Short-term (the in-context conversation/scratchpad), long-term (facts persisted across sessions, retrieved like RAG), and structured state the agent carries between steps (checkpointed in a graph).",
        senior: "I'm deliberate about what's worth persisting — over-stuffing long-term memory adds noise and cost. Long-term memory is just RAG over the agent's own history with the same retrieval-quality concerns; structured state is what lets me checkpoint and resume. I separate 'what the agent knows' from 'where it is in the task'.",
        followups: ["How do you avoid memory becoming noise?", "How do you checkpoint state?"],
        redflag: "Treating 'memory' as one vague blob." },
      { q: "When would you NOT build an agent?", difficulty: "Intermediate", round: "system design", seniority: "Senior", frequency: "medium", region: "Both",
        model: "When the steps are known in advance — then a deterministic workflow (with maybe one LLM call per step) is cheaper, faster, more testable and more reliable than an autonomous loop.",
        senior: "I default to the least-agentic thing that works. Most 'agent' problems are really pipelines with one or two LLM decisions. I escalate to a full agent only when branching is genuinely data-dependent at runtime. Showing that restraint signals production maturity.",
        followups: ["Give an example you'd build as a workflow not an agent."],
        redflag: "Defaulting to agents for everything.", source: SRC.anthropicAgents },
      { q: "How do you prevent an agent from looping forever?", difficulty: "Intermediate", round: "technical deep-dive", seniority: "Senior", frequency: "high", region: "Both",
        model: ["<ul><li>Hard cap on steps (max iterations) — stop and return a best-effort or escalate when hit.</li><li>Per-run token/cost budget.</li><li>Detect no-progress loops (same tool + same args repeated) and break.</li><li>Timeouts on each tool call.</li></ul>"],
        senior: "A senior treats the bound as a product decision (what happens when we hit the cap — degrade, escalate, or fail honestly?) and logs how often runs hit the limit as a signal that the task or tools need redesign.",
        followups: ["What do you return when the cap is hit?", "How do you detect a no-progress loop?"],
        redflag: "Relying on the model to 'know when to stop' with no hard limit." },
      { q: "How do you secure an agent's tools?", difficulty: "Advanced", round: "system design", seniority: "Senior", frequency: "high", region: "Both",
        model: ["Safety comes from what the tools can do, not from the prompt:", "<ul><li>Least privilege — each tool gets the minimum capability (e.g. read-only DB, send-mail only to allow-listed addresses).</li><li>Validate every tool argument against a schema before executing.</li><li>Make tools idempotent; add timeouts and rate limits.</li><li>Require human approval for destructive/irreversible actions.</li><li>Treat tool outputs as untrusted input (indirect injection).</li></ul>"],
        senior: "A senior assumes the model can be manipulated and designs so even a hijacked agent can't do damage — plus audit-logs every action with the reasoning that triggered it.",
        followups: ["How do you make a SQL tool safe?", "Where do you put human-in-the-loop?"],
        redflag: "Giving the agent broad write/delete access and trusting it to behave.", source: SRC.owaspLLM },
      { q: "How do you evaluate an agent?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "medium", region: "Both",
        model: ["Define task cases with expected outcomes and measure:", "<ul><li>Task success rate (did it achieve the goal / produce the right final answer?).</li><li>Correct tool selection and argument accuracy.</li><li>Average steps and cost per task (efficiency).</li><li>Tool-error and recovery rate.</li></ul>", "Include adversarial cases (ambiguous goals, injection via tool output)."],
        senior: "A senior evaluates the trajectory, not just the final answer — checking the agent took a sensible path — and runs the suite as CI so reliability doesn't regress.",
        followups: ["How do you evaluate the path, not just the result?", "How big is your task set?"],
        redflag: "Only checking the final answer 'looked right' on a couple of runs.", task: "Add a task-success eval to your <a href='../projects/build-first-agent.html'>agent app</a>." },
      { q: "How does LangGraph help with stateful agents?", difficulty: "Advanced", round: "technical deep-dive", seniority: "Senior", frequency: "emerging", region: "Both",
        model: ["LangGraph models the agent as a graph of nodes/edges with explicit shared state. That gives you:", "<ul><li>Explicit state passed between steps (not buried in the prompt).</li><li>Conditional routing — next node depends on results.</li><li>Checkpointing — retry/resume a single step, not the whole run.</li><li>Human-in-the-loop nodes for approval.</li></ul>"],
        senior: "A senior values it for control and observability but notes the same can be done in disciplined plain Python — the graph is a convenience for managing state and retries, not magic.",
        followups: ["What does the checkpointer give you operationally?", "When is plain code better?"],
        redflag: "Using it because it's popular, unable to say what problem it solves.", source: SRC.langgraph },
      { q: "What are common multi-agent failure modes?", difficulty: "Advanced", round: "system design", seniority: "Senior/Staff", frequency: "emerging", region: "Both",
        model: ["<ul><li>Coordination chaos / agents looping messages between each other.</li><li>Error cascade — one agent's mistake corrupts everything downstream.</li><li>Cost blowup from many agents each making many calls.</li><li>Diffused responsibility — no clear owner of the final output.</li><li>Context loss when passing work between agents.</li></ul>"],
        senior: "A senior first questions whether multi-agent is even justified (vs one agent or a workflow), then mitigates with a supervisor, bounded total steps, checkpointed shared state, and tracing across agents.",
        followups: ["When is multi-agent actually worth it?", "How do you stop inter-agent loops?"],
        redflag: "Proposing many agents because it sounds sophisticated, with no coordination or cost story.", task: "<a href='../systemdesign/multi-agent-workflow.html'>Multi-Agent Business Workflow</a> design." },
    ],
    hands: [callout("key", "▶ Now build it", "<p>Next in the learning path: <strong><a href='../projects/build-first-agent.html'>Build Your First Agent App →</a></strong>. Turn this loop into a working, reliable agent you can demo and explain in interviews.</p>"),
      "Build a tool-using agent (bounded loop, schema-validated tools, output guardrail). Then re-implement one task as a deterministic workflow and compare cost, latency and reliability."],
    portfolio: ["Maps to <a href='../projects/build-first-agent.html'>Build First Agent App</a>, <a href='../projects/sql-agent.html'>SQL Agent</a>, <a href='../projects/email-triage.html'>Email Triage</a> and <a href='../projects/research-assistant.html'>Research Assistant</a>. In your README, show the loop, the guardrails, and one place you deliberately chose a workflow over an agent."],
    mistakes: ["Unbounded loops / no step budget", "Trusting tool outputs (injection)", "Over-privileged tools", "Using an agent where a workflow suffices", "No tracing, so failures are unexplainable"],
    checklist: ["I can explain the loop and 'model proposes, runtime disposes'", "I can compare ReAct / planner / graph / multi-agent", "I can list agent failure modes and fixes", "I know when a workflow beats an agent"],
    sources: ["Teach-agents course; sections 11, 20 of the source material; LangGraph deep-dive."],
  },
});

export default CORE2;
